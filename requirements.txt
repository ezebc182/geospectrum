fastapi==0.115.0
uvicorn[standard]==0.30.0
httpx==0.27.0
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.0.0

# INPRES: parseo del feed XML (https://www.inpres.gob.ar/mapa/sismos.xml).
# defusedxml y no xml.etree de la stdlib porque el feed es de un tercero: el
# parser de la stdlib bloquea XXE pero NO las bombas de entidades
# (billion laughs), verificado en Python 3.12.
# beautifulsoup4/lxml estaban acá para el scraper de HTML que este feed
# reemplaza; INPRES sirve la tabla vacía y la rellena por JS desde este XML.
defusedxml==0.7.1

# Observability
prometheus-client==0.20.0
python-json-logger==2.0.7

# Storage opcional
psycopg2-binary==2.9.9
asyncpg==0.29.0

# Object storage (feedback-screenshot-attachment) — cliente S3-compatible
# síncrono para firmar URLs prefirmadas hacia Cloudflare R2 (presign es
# cómputo local, sin I/O de red: ver design.md Decision 1, no se justifica
# aioboto3). Versión resuelta al instalar en este venv el 2026-09-03.
boto3==1.43.88

# Auth (multi-user-auth)
passlib[bcrypt]==1.7.4
bcrypt==4.2.0
python-jose[cryptography]==3.3.0
# pydantic==2.9.0 no trae soporte de EmailStr por defecto (requiere el extra
# [email] o el paquete email-validator instalado explícitamente) — confirmado
# en tasks.md 1.9 al correr los tests: sin esto, la collection de
# tests/unit/test_auth_service.py falla con ImportError.
email-validator==2.2.0

# Auth (google-oauth) — cliente OAuth2/OIDC para login con Google (ver
# openspec/changes/google-oauth/design.md). 1.7.2 es la última versión
# estable en PyPI al momento de esta tarea (design.md refería 1.3.2 como
# conocida al momento del diseño; se fija la versión real disponible).
# Authlib no depende directamente de fastapi/starlette (solo de
# cryptography y joserfc), por lo que no hay conflicto de pines con
# fastapi==0.115.0 ya fijado arriba.
Authlib==1.7.2
# itsdangerous: dependencia de starlette.middleware.sessions.SessionMiddleware
# (firma la cookie "oauth_state" que usa Authlib para el `state`/`nonce` del
# handshake OAuth — ver design.md Decision 2). Starlette la trae como extra
# opcional (no instalada por fastapi/starlette por defecto porque
# SessionMiddleware es un módulo opcional), así que se fija explícita acá.
itsdangerous==2.2.0

# Auth (account-settings) — TOTP (RFC 6238) para 2FA con QR + backup codes
# (ver openspec/changes/account-settings/design.md, Decision 7). 2.10.0 es la
# última versión estable en PyPI al momento de esta tarea (design.md refería
# 2.9.0 como conocida al momento del diseño; se fija la versión real
# disponible, mismo criterio ya aplicado a Authlib==1.7.2 en google-oauth).
# NO se agrega `qrcode`: el QR se renderiza client-side en dashboard/ a partir
# del otpauth:// URI (Decision 7) — el backend nunca genera una imagen PNG.
pyotp==2.10.0

# Geometría (areas-of-interest / AOI-1) — punto-en-polígono para filtrar eventos
# por área de interés. Ver docs/superpowers/specs/
# 2026-07-27-plate-boundaries-usgs-style-design.md ("Decisiones heredadas",
# #1 y #2): la imagen timescale/timescaledb:latest-pg15 NO trae PostGIS
# (`postgis` no figura en pg_available_extensions, verificado contra la base
# corriendo), así que la geometría se guarda como GeoJSON en JSONB y el
# punto-en-polígono lo resuelve Shapely en Python. Se elige Shapely y no
# ray-casting propio porque maneja multipolígonos y agujeros —el catálogo
# curado tiene cinturones partidos en el antimeridiano (Decisión heredada #5).
#
# 2.1.2 es la última estable en PyPI al momento de esta tarea. Se elige la
# serie 2.x —y no la 1.8.x— porque requiere numpy>=1.21 y es compatible con
# numpy 2.x, que es lo que el venv de desarrollo tiene realmente instalado
# (2.5.1), por delante del `numpy==1.26.4` pineado más abajo. Instala desde
# wheel precompilada (no compila C), así que no reproduce el problema de
# toolchain que tiene obspy en deploy/docker/Dockerfile.
shapely==2.1.2

# Utils
python-dateutil==2.9.0
pytz==2024.1

# Seismic data processing
obspy==1.4.1
numpy==1.26.4
scipy==1.13.1
matplotlib==3.9.0
Pillow==10.4.0

# Real-time stream
websockets==12.0
redis==5.0.4
sentry-sdk[fastapi]==2.18.0

# Testing infra
testcontainers[redis,postgres]==4.5.1
pytest-mock==3.14.0
freezegun==1.5.1

# Development
pytest==8.3.0
pytest-asyncio==1.3.0
pytest-cov==5.0.0
pytest-httpx==0.30.0
black==24.10.0
ruff==0.6.0
mypy==1.11.0
types-defusedxml==0.7.0.20240218
