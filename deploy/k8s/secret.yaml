# IMPORTANTE: Este es un ejemplo. En producción usar Sealed Secrets o External Secrets
apiVersion: v1
kind: Secret
metadata:
  name: geospectrum-secrets
  namespace: geospectrum
type: Opaque
stringData:
  # TimescaleDB credentials (si se usa)
  TIMESCALEDB_USER: "seismic"
  TIMESCALEDB_PASSWORD: "changeme"  # CAMBIAR EN PRODUCCIÓN

  # Sentry DSN (si se usa)
  SENTRY_DSN: ""
