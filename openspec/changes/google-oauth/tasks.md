# Tasks: Login/registro con Google (OAuth) como alternativa al email/password existente

Convención de trazabilidad: cada tarea de testing referencia el escenario exacto de `specs/auth/spec.md` que cubre, usando el formato `[Requirement / Scenario]`.

## Phase 1: Foundation (migración, dependencia, config, modelos)

- [x] 1.1 **Prerequisito externo — bloqueante solo para pruebas end-to-end reales, NO para el resto de esta fase.** RESUELTO: el usuario ya generó en Google Cloud Console proyecto, pantalla de consentimiento OAuth y credenciales tipo "Web application" (Client ID + Client Secret nuevo, regenerado por motivos de seguridad), y ya las configuró en `.env` del backend (confirmado por el orquestador; verificado en esta tarea vía `grep -c "^GOOGLE_CLIENT" .env` = 2 líneas presentes, sin exponer valores). `redirect_uri` a confirmar carácter a carácter cuando se sete `GOOGLE_REDIRECT_URI` — no bloquea Phase 1
- [x] 1.2 Crear `deploy/sql/migrations/003_add_google_oauth.sql` con el SQL exacto de `design.md` (Interfaces / Contracts → Migración `003_add_google_oauth.sql`): `ALTER TABLE users ALTER COLUMN password_hash DROP NOT NULL;`, `ALTER TABLE users ADD COLUMN IF NOT EXISTS google_id TEXT UNIQUE;`, `CREATE INDEX IF NOT EXISTS idx_users_google_id ON users (google_id);`, con el bloque de rollback documentado en comentario al pie (incluye la advertencia condicional de que `SET NOT NULL` falla si existen filas con `password_hash IS NULL`, mismo formato que `002_add_role_hierarchy.sql`). No editar `001_create_users_table.sql` ni `002_add_role_hierarchy.sql` (ya aplicados)
- [x] 1.3 Aplicar la migración 003 contra el Postgres real del perfil `storage` en `deploy/docker/docker-compose.yml` (`docker exec timescaledb`, puerto 5433, mismo mecanismo que 001/002) y confirmar con `\d users` que `password_hash` quedó nullable, `google_id` existe como `TEXT UNIQUE`, y el índice `idx_users_google_id` se creó. No depende de 1.1 — DONE: confirmado antes/después con `\d users` (usuario/db: `seismic`/`seismic`), migración aplicada exitosamente, columnas e índice verificados
- [x] 1.4 Fijar la versión exacta de `Authlib` a instalar: verificar en PyPI la última versión estable compatible con `fastapi`/`starlette` ya pineados en `requirements.txt` (resuelve la Open Question de `design.md` sobre la versión — el design referencia `1.3.2` como conocida al momento del diseño, pero esta tarea debe confirmar/actualizar el número real usado). Agregar `Authlib==<version-confirmada>` a `requirements.txt`, junto al resto de dependencias de runtime (no depende de 1.1) — DONE: última estable en PyPI es `1.7.2` (no `1.3.2` referida en design.md); Authlib no depende directamente de fastapi/starlette (solo `cryptography`+`joserfc>=1.6.0`), sin conflicto con `fastapi==0.115.0`/`starlette<0.39.0,>=0.37.2`. Fijado `Authlib==1.7.2` en `requirements.txt`
- [x] 1.5 Instalar las dependencias nuevas (`pip install -r requirements.txt`) y confirmar que no hay conflictos de versión con `fastapi`, `starlette`, `python-jose` ya instalados — DONE: instalado en el venv existente de la raíz (`source venv/bin/activate`), `Authlib-1.7.2` + `joserfc-1.7.4` instalados, `pip check` → "No broken requirements found"
- [x] 1.6 Agregar a `src/config/settings.py` los campos `google_client_id: Optional[str] = None`, `google_client_secret: Optional[str] = None`, `google_redirect_uri: Optional[str] = None`, y la property `google_oauth_configured` exactamente como en `design.md` Decision 6 (`return bool(self.google_client_id and self.google_client_secret and self.google_redirect_uri)`), con el comentario explicando por qué NO hay fail-fast a nivel de `Settings` (a diferencia de `auth_secret_key`) — DONE
- [x] 1.7 En `src/models/user.py`, cambiar `UserInDB.password_hash: str` a `Optional[str]` y agregar `google_id: Optional[str] = None` tanto a `UserInDB` como a `UserPublic` — DONE
- [x] 1.8 En `src/services/auth_service.py`, agregar el guard de `verify_password()` de `design.md` (Interfaces / Contracts → `AuthService.verify_password` — guard nuevo): si `password_hash is None`, retorna `False` inmediatamente sin invocar `_pwd_context.verify` (evita excepción no controlada de passlib al recibir `None`) — DONE, firma actualizada a `password_hash: Optional[str]`
- [x] 1.9 Revisar todos los call sites existentes de `password_hash` en `src/services/auth_service.py` y `src/main.py` (en particular `get_user_by_email` → `UserInDB` y el flujo de `POST /auth/login`) y confirmar por inspección que ninguno asume `password_hash: str` no-nulo fuera del guard agregado en 1.8 — es una tarea de verificación explícita (Risk #2 del proposal), no debe quedar como supuesto implícito — DONE: único call site fuera de `AuthService` mismo es `src/main.py:546` (`POST /auth/login`, `auth_service.verify_password(password, user.password_hash)`), que pasa directamente por el guard de 1.8 — si `password_hash is None`, `verify_password` retorna `False` y el flujo cae en el 401 genérico existente (líneas 551-556), sin cambios necesarios en `main.py`. Los demás usos en `auth_service.py` (`create_user()` INSERT, `get_user_by_email()` SELECT) son lectura/escritura cruda, no invocan `verify_password` con valor no verificado

## Phase 2: Backend core (AuthService — resolución de usuario Google)

- [x] 2.1 En `src/services/auth_service.py`, extraer el bloque `existing_count = await conn.fetchval("SELECT COUNT(*) FROM users")` + `actual_role = SUPERADMIN if existing_count == 0 else VIEWER` ya existente dentro de `create_user()` (líneas ~104-105) a un método privado nuevo `_determine_bootstrap_role(conn) -> UserRole`, y refactorizar `create_user()` para invocarlo — sin cambiar su comportamiento observable (mismo resultado, una sola fuente de verdad, ver `design.md` Decision 3)
- [x] 2.2 Escribir/actualizar el test de regresión en `tests/unit/test_auth_service.py` que confirma que `create_user()` sigue asignando `superadmin` en tabla vacía y `viewer` en tabla no vacía después del refactor de 2.1 — antes de continuar a 2.3, para aislar cualquier regresión introducida por la extracción del método privado
- [x] 2.3 En `src/services/auth_service.py`, implementar `resolve_or_create_google_user(self, google_id: str, email: str) -> UserPublic` con la firma y el docstring exactos de `design.md` (Interfaces / Contracts → `AuthService.resolve_or_create_google_user`): un único `conn.transaction()` que (a) hace SELECT por `google_id` y retorna si ya existe, (b) si no, SELECT por `email` — si existe fila con `password_hash IS NOT NULL` y `google_id IS NULL`, hace `UPDATE users SET google_id = $1 WHERE email = $2` (auto-link) y retorna esa fila, (c) si no existe ninguna fila con ese email, crea usuario nuevo con `password_hash NULL`, reutilizando `_determine_bootstrap_role(conn)` de 2.1 dentro de la misma transacción
- [x] 2.4 Escribir tests unitarios en `tests/unit/test_auth_service.py` para `resolve_or_create_google_user()`: usuario nuevo con tabla vacía → `superadmin` — `[Requirement: Bootstrap del primer superadmin vía Google / Scenario: El primer registro del sistema vía Google se convierte en superadmin]`
- [x] 2.5 Test: usuario nuevo con tabla no vacía → `viewer` — `[Requirement: Bootstrap del primer superadmin vía Google / Scenario: Un registro posterior vía Google siempre crea viewer]`
- [x] 2.6 Test: auto-link de email existente con `password_hash` no nulo → NO crea fila nueva, `google_id` queda seteado, `password_hash` original sin modificar — `[Requirement: Auto-link por email con usuario existente de password / Scenario: Auto-link exitoso con email verificado por Google]`
- [x] 2.7 Test: segundo login del mismo `google_id` (usuario ya vinculado) → retorna la misma fila sin duplicar ni re-vincular — `[Requirement: Login indistinto por password o Google para cuentas vinculadas / Scenario: Usuario vinculado se loguea por Google después de haberse logueado antes por password]`
- [x] 2.8 Test: `AuthService.verify_password(password, None)` retorna `False` sin excepción (cubre el guard de 1.8) — no bloqueante, puede correr en paralelo con 2.4-2.7 — DONE: el test no existía todavía en el archivo, se agregó `test_verify_password_with_none_hash_returns_false_without_exception`
- [x] 2.9 Test: `Settings.google_oauth_configured` es `True` solo si las 3 vars (`google_client_id`, `google_client_secret`, `google_redirect_uri`) están seteadas, `False` si falta cualquiera — mockeando env vars, `pytest` puro, sin Postgres

## Phase 3: Backend endpoints (main.py — routing, middleware, registro de Authlib)

- [x] 3.1 En `src/main.py`, agregar `app.add_middleware(SessionMiddleware, secret_key=settings.auth_secret_key, session_cookie="oauth_state")` (import de `starlette.middleware.sessions.SessionMiddleware`) — reutiliza `auth_secret_key` ya fail-fast garantizado, ver `design.md` Decision 2. Confirmar explícitamente que el nombre `session_cookie="oauth_state"` está seteado (evita colisión con la cookie `session` del JWT existente, `SESSION_COOKIE_NAME` en `src/api/deps.py:30`) — DONE. Nota: requirió agregar `itsdangerous` a `requirements.txt` (dependencia de `SessionMiddleware`, no traída transitivamente por Authlib/FastAPI/Starlette — detectado por `ImportError` al importar `src.main`, no estaba anticipado en design.md/1.5)
- [x] 3.2 En `src/main.py`, agregar a nivel de módulo `oauth = OAuth()` (import `from authlib.integrations.starlette_client import OAuth`), junto a las otras instancias module-level (`event_bus`, `column_writer`) — DONE
- [x] 3.3 En `lifespan()` de `src/main.py`, agregar el registro condicional de Authlib exactamente como en `design.md` (Interfaces / Contracts → `Settings` / `lifespan()` — registro condicional de Authlib): si `settings.google_oauth_configured`, llamar `oauth.register("google", client_id=..., client_secret=..., server_metadata_url="https://accounts.google.com/.well-known/openid-configuration", client_kwargs={"scope": "openid email profile"})` y setear `app.state.google_oauth_enabled = True` con `logger.info(...)`; si no, setear `app.state.google_oauth_enabled = False` con `logger.warning(...)` — el servidor MUST seguir arrancando en ambos casos (no fail-fast, a diferencia de `auth_secret_key`) — DONE
- [x] 3.4 Implementar `GET /auth/google/login` en `src/main.py` (misma sección `# Auth (multi-user-auth)` ya existente, mismo estilo de docstrings y `requests_total.labels(...)` que los endpoints de password): primera línea verifica `request.app.state.google_oauth_enabled`, retorna `503` si `False`; si `True`, llama `oauth.google.authorize_redirect(request, redirect_uri)` y retorna el `302` a Google — `[Requirement: Endpoints OAuth de Google / Scenario: GET /auth/google/login redirige a Google con los parámetros correctos]` — DONE
- [x] 3.5 Implementar el esqueleto de `GET /auth/google/callback` en `src/main.py`: primera línea verifica `app.state.google_oauth_enabled`, retorna `503` si `False`; recibe `code`/`state`/`error` como query params — DONE
- [x] 3.6 En el callback, manejar el caso "Google redirige sin `code`" (usuario canceló el consentimiento): redirect a `/login` con mensaje de error, sin `Set-Cookie`, sin tocar `users` — `[Requirement: Manejo de errores del flujo OAuth de Google / Scenario: Usuario cancela el consentimiento de Google]` — DONE
- [x] 3.7 En el callback, manejar el caso "Google redirige con `error=access_denied`" (u otro valor): mismo tratamiento que 3.6, sin excepción no controlada — `[Scenario: Google devuelve un parámetro de error explícito]` — DONE
- [x] 3.8 En el callback, envolver `oauth.google.authorize_access_token(request)` en manejo de excepciones: capturar `MismatchingStateError` de Authlib (state inválido/ausente/reutilizado) y cualquier fallo del intercambio código→token (timeout, código expirado, credenciales inválidas), redirigiendo a `/login` con error, sin `Set-Cookie`, sin tocar `users`, sin 500 — cubre `[Scenario: GET /auth/google/callback rechaza un state inválido o ausente]` y `[Scenario: El intercambio de token con Google falla]` — DONE, se captura `authlib.integrations.base_client.errors.OAuthError` (clase base; `MismatchingStateError` es subclase confirmada por inspección — `issubclass(MismatchingStateError, OAuthError) == True`), cubre ambos escenarios con un solo `except`
- [x] 3.9 En el callback, tras el intercambio exitoso, validar que el ID token es parseable y sus claims (`iss`, `aud`, `exp`) son válidos — si Authlib ya lo garantiza internamente al parsear `token["userinfo"]`, documentar explícitamente en un comentario que la validación de firma/claims la hace Authlib (JWKS de Google) y no hace falta reimplementarla; si falla, redirect a `/login` con error, sin `Set-Cookie`, sin tocar `users` — `[Scenario: El ID token de Google no puede validarse]` — DONE (comentario explícito en el código + guard `if userinfo is None`)
- [x] 3.10 En el callback, extraer `email_verified` de `token["userinfo"]["email_verified"]` y rechazar explícitamente (redirect a `/login` con error, sin `Set-Cookie`, sin tocar `users`) si es `False` o está ausente, ANTES de invocar `resolve_or_create_google_user()` — la validación vive en el endpoint, no en `AuthService` (`design.md` Decision 4) — cubre `[Requirement: Auto-link por email con usuario existente de password / Scenario: Login por Google rechazado cuando el email no está verificado por Google]` — DONE
- [x] 3.11 En el callback, tras pasar la validación de `email_verified`, llamar a `auth_service.resolve_or_create_google_user(google_id=userinfo["sub"], email=userinfo["email"])`, luego `auth_service.create_access_token(user)` (MISMO método que usa `/auth/login`, reutilización estricta) y `response.set_cookie("session", token, httponly=True, secure=True, samesite="lax", ...)` con los mismos atributos que emite `/auth/login` — cubre `[Requirement: Login/registro vía Google para email nuevo / Scenario: Registro exitoso vía Google con email nuevo]` y `[Requirement: Auto-link por email con usuario existente de password / Scenario: Auto-link exitoso con email verificado por Google]` — DONE
- [x] 3.12 **Resolver la Open Question de `design.md` sobre destino exacto del redirect**: decidir y documentar en un comentario en el código el destino concreto del `302` en éxito (ej. `dashboard/` a secas) y en error (ej. `/login?error=<código-legible>` con query param que el frontend pueda leer para mostrar mensaje) — aplicar la misma convención a las 4 ramas de error de 3.6-3.10 y a la rama de éxito de 3.11, de forma consistente — DONE. Decisión tomada por este sub-agente (ver desviación documentada en el reporte de sdd-apply): éxito → `302` a `"/"` (no existe convención de `/dashboard` en `dashboard/app/login/page.tsx` al momento de este change); error → `302` a `/login?error=<código>` con códigos estables (`google_oauth_cancelled`, `google_oauth_<valor-de-error-de-google>`, `google_oauth_token_exchange_failed`, `google_oauth_invalid_id_token`, `google_oauth_email_not_verified`), consistente en las 5 ramas
- [x] 3.13 Confirmar por inspección que `src/api/deps.py` no requiere ningún cambio (`design.md` Decision 5 — `get_current_user`/`require_role`/`require_min_role` ya son agnósticos del método de login porque solo dependen de la cookie `session` y `decode_access_token()`) — tarea de verificación explícita, no de código — DONE, confirmado por lectura completa de `src/api/deps.py`: cero cambios necesarios ni realizados
- [ ] 3.14 Levantar el servidor local (`uvicorn src.main:app --reload`) SIN credenciales de Google configuradas y confirmar manualmente con `curl`/httpie que `GET /auth/google/login` y `GET /auth/google/callback?code=x&state=y` responden `503`, y que `POST /auth/login`/`POST /auth/register` con password siguen funcionando exactamente igual que antes de este change — no depende de 1.1 (prerequisito externo). **NO verificado con servidor real**: Docker no está disponible en este entorno (`docker info` falla — mismo motivo que los 7 errors preexistentes de `tests/integration/test_redis_pubsub_bus.py`), por lo que no hay Postgres/TimescaleDB accesible y `AuthService.connect()` (fail-fast obligatorio en `lifespan()`, líneas 155-160 de `main.py`) impide que el servidor arranque, independientemente de Google OAuth. Cubierto en su lugar por 4 tests de integración nuevos en `tests/integration/test_auth_api.py` (vía `TestClient`, sin `lifespan()`, con `app.state` seteado directamente — mismo patrón que `tests/unit/test_deps.py`), que confirman los 4 comportamientos pedidos: `/auth/google/login` → 503, `/auth/google/callback` → 503, `POST /auth/register` sin cambios, `POST /auth/login` sin cambios. Pendiente de verificación real con servidor vivo en un entorno con Docker/Postgres disponible.

## Phase 4: Frontend (botón de login con Google)

- [ ] 4.1 En `dashboard/app/login/page.tsx`, agregar el botón "Iniciar sesión con Google" junto al formulario email/password existente, usando `<a href={`${API_URL}/auth/google/login`}>` o `onClick` con `window.location.href = ...` (NO `fetch` — redirect completo de navegador, confirmado como el patrón correcto en `design.md`)
- [ ] 4.2 Confirmar por inspección que `dashboard/lib/auth.ts` y `dashboard/hooks/use-auth.tsx` NO requieren ningún cambio (`design.md` confirma que `getMe()` ya es agnóstico del método de login) — tarea de verificación explícita, no de código
- [ ] 4.3 Verificación visual manual: levantar el dashboard (`npm run dev`) y confirmar que el botón "Iniciar sesión con Google" se renderiza correctamente junto al formulario existente, sin romper el layout ni el flujo de login por password

## Phase 5: Testing (integración end-to-end de los escenarios restantes)

- [ ] 5.1 En `tests/integration/test_auth_api.py`, agregar fixtures para mockear `oauth.google.authorize_access_token` (monkeypatch, nunca llamar a Google real) devolviendo un `token["userinfo"]` configurable (`sub`, `email`, `email_verified`) — infraestructura común reutilizada por 5.2-5.8
- [ ] 5.2 Test: `GET /auth/google/login` con `google_oauth_configured=False` → `503`; con `True` → `302` a URL de Google con `state` en el query — `[Requirement: Endpoints OAuth de Google / Scenario: GET /auth/google/login redirige a Google con los parámetros correctos]`
- [ ] 5.3 Test: `GET /auth/google/callback` exitoso con email nuevo y `email_verified=true` → crea fila en `users` con `google_id` seteado y `password_hash IS NULL`, responde `302` con `Set-Cookie: session` con los mismos atributos que `/auth/login`, y `GET /auth/me` posterior con esa cookie responde `200` con el `role` correcto — `[Requirement: Login/registro vía Google para email nuevo / Scenario: Registro exitoso vía Google con email nuevo]`
- [ ] 5.4 Test: `GET /auth/google/callback` con tabla `users` vacía → la fila creada tiene `role="superadmin"` — `[Requirement: Bootstrap del primer superadmin vía Google / Scenario: El primer registro del sistema vía Google se convierte en superadmin]`
- [ ] 5.5 Test: `GET /auth/google/callback` con email ya registrado con password (`password_hash` no nulo, `google_id IS NULL`) y `email_verified=true` → auto-link, NO crea fila nueva, `password_hash` original intacto, cookie emitida para la misma fila — `[Requirement: Auto-link por email con usuario existente de password / Scenario: Auto-link exitoso con email verificado por Google]`
- [ ] 5.6 Test: `GET /auth/google/callback` con email ya registrado con password y `email_verified=false` → rechazo explícito, sin `Set-Cookie`, sin modificar la fila existente, sin crear fila nueva — `[Scenario: Login por Google rechazado cuando el email no está verificado por Google]`
- [ ] 5.7 Test: usuario con `password_hash` y `google_id` ambos no nulos (ya vinculado) puede loguearse tanto por `POST /auth/login` como por `GET /auth/google/callback`, y ambos resuelven al mismo `id`/`role` — `[Requirement: Login indistinto por password o Google para cuentas vinculadas / Scenario: Usuario vinculado se loguea por password después de haber sido vinculado por Google]` + `[Scenario: Usuario vinculado se loguea por Google después de haberse logueado antes por password]`
- [ ] 5.8 Test: `GET /auth/google/callback` sin parámetro `code` (cancelación) → redirect a `/login` con error, sin `Set-Cookie`, sin tocar `users` — `[Requirement: Manejo de errores del flujo OAuth de Google / Scenario: Usuario cancela el consentimiento de Google]`
- [ ] 5.9 Test: `GET /auth/google/callback?error=access_denied` → redirect a `/login` con error, sin 500 — `[Scenario: Google devuelve un parámetro de error explícito]`
- [ ] 5.10 Test: `GET /auth/google/callback` con `state` inválido/ausente/reutilizado → redirect a `/login` con error, sin `Set-Cookie`, sin tocar `users` — `[Scenario: GET /auth/google/callback rechaza un state inválido o ausente]`
- [ ] 5.11 Test: mock del intercambio código→token de Authlib lanzando una excepción de red/timeout/credenciales inválidas → redirect a `/login` con error, sin `Set-Cookie`, sin tocar `users`, sin 500 — `[Scenario: El intercambio de token con Google falla]`
- [ ] 5.12 Test: mock de un ID token con firma/`iss`/`aud`/`exp` inválidos → redirect a `/login` con error, sin `Set-Cookie`, sin tocar `users` — `[Scenario: El ID token de Google no puede validarse]`
- [ ] 5.13 Test de no-regresión: `POST /auth/register` con la migración 003 aplicada sigue respondiendo `201` con el mismo shape que antes, y la fila creada tiene `password_hash` no nulo y `google_id IS NULL` — `[Requirement: No regresión sobre login/registro por email y password / Scenario: Registro por password sigue funcionando igual que antes de este change]`
- [ ] 5.14 Test de no-regresión: `POST /auth/login` con la migración 003 aplicada sigue respondiendo `200` con `Set-Cookie` idéntico al comportamiento pre-change — `[Scenario: Login por password sigue funcionando igual que antes de este change]`
- [ ] 5.15 Correr la suite completa (`pytest tests/`) y confirmar que ningún test de `tests/unit/test_auth_service.py`, `tests/unit/test_deps.py`, `tests/integration/test_auth_api.py` ni el resto de la suite existente se rompió por los cambios de `main.py`/`settings.py`/`user.py`/`requirements.txt` de este change
- [ ] 5.16 **Manual/E2E — bloqueado por 1.1 (prerequisito externo).** Solo si el usuario ya provisionó credenciales reales de Google Cloud Console: flujo real de extremo a extremo — click en el botón del dashboard → consentimiento real de Google → callback → sesión activa visible en el dashboard, confirmando en devtools que la cookie `session` se setea correctamente y no aparece el JWT en la URL de redirect ni en el body — verificación manual, no automatizable en CI sin credenciales de service account (fuera de alcance)

## Phase 6: Infra/Docker

- [ ] 6.1 Agregar `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI` (comentados con placeholder, ej. `# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com`) al servicio `geospectrum` en `deploy/docker/docker-compose.yml`, mismo estilo que `AUTH_SECRET_KEY`
- [ ] 6.2 Si el proyecto tiene `.env.example` (backend) y/o `dashboard/.env.example`, agregar ahí las mismas 3 variables con placeholder, siguiendo el patrón ya usado para `AUTH_SECRET_KEY`/`AUTH_TOKEN_EXPIRE_MINUTES` (ver `deploy/docker/docker-compose.yml` y el `.env.example` de `multi-user-auth`, tarea 6.3 de ese change) — confirmar si el dashboard necesita alguna de estas 3 vars (probablemente no, ya que el redirect es server-side vía backend, pero verificar antes de omitir)
- [ ] 6.3 Documentar en un comentario junto a las variables nuevas en `docker-compose.yml` que la migración 003 debe aplicarse manualmente ANTES de desplegar el código nuevo (orden de rollout de `design.md` → Migration/Rollout, punto 3), y que las credenciales de Google son opcionales al arranque (`google_oauth_configured=False` → servidor arranca igual, endpoints `/auth/google/*` responden `503`)
- [ ] 6.4 Verificar `deploy/k8s/secret.yaml` y `deploy/k8s/configmap.yaml` (mismo criterio que `multi-user-auth` tarea 6.4): si el change no tiene alcance de Kubernetes definido en proposal.md/design.md, NO agregar las variables ahí — documentar explícitamente como pendiente/fuera de alcance, con el mismo riesgo señalado (si se deployea a K8s sin las 3 vars, los endpoints `/auth/google/*` simplemente responden `503` sin tumbar el pod, a diferencia de `AUTH_SECRET_KEY` — esto es una diferencia de riesgo menor respecto al precedente de `multi-user-auth`, documentarla)
