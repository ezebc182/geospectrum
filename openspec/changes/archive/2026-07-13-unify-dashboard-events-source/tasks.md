# Tasks: Unificación de fuente de datos sísmicos entre Dashboard y Explorador

Config aplicado: `tdd: false`, `test_command: "cd seismic-monitor && pytest"` (`openspec/config.yaml`). No se usa RED-GREEN-REFACTOR explícito por fase, pero cada tarea de implementación trae su tarea de test asociada en la misma fase.

Nota de alcance confirmada en design.md: `GET /events/search` (`src/main.py` líneas 377-436) **NO se toca en ninguna tarea de este archivo** — ya usa `merge_all_sources` con 3 fuentes desde antes de esta propuesta.

## Phase 0: Spike bloqueante — Validación de no-conmutatividad de `merge_all_sources`

Esta fase es un gate. Ninguna tarea de la Fase 2 (migración de `/report`, `/events`, `/alerts`) puede comenzar hasta que 0.1-0.4 estén completas y documentadas. Motivo: si el orden de fuentes altera qué eventos se fusionan y qué alertas se disparan, la Fase 3 (`report_service.py`) debe fijar `CANONICAL_SOURCES` como decisión consciente, no como default arbitrario.

- [x] 0.1 Crear fixture sintético en `tests/unit/test_merge_service.py`: 3 `SeismicEvent` (uno por fuente USGS/EMSC/INPRES) diseñados para solapamiento triple ambiguo — USGS a ~25km de EMSC, EMSC a ~25km de INPRES, USGS a ~45km de INPRES (por encima del umbral de 30km entre sí pero cada par vecino dentro del umbral), con magnitudes distintas por fuente (ej. USGS mag=4.0, EMSC mag=4.3, INPRES mag=4.1) y timestamps dentro de Δt≤120s entre pares vecinos. — Confirmado implementado: fixture presente en `tests/unit/test_merge_service.py` (checkbox corregido en fase de archive tras verificar con `grep`/ejecución que Phase 0 estaba completa en código pero no reflejada aquí).
- [x] 0.2 Escribir test `test_merge_all_sources_order_sensitivity` en `tests/unit/test_merge_service.py` que ejecute `merge_all_sources` sobre el fixture de 0.1 en al menos 3 órdenes distintos y no triviales (mínimo: `(usgs,emsc,inpres)`, `(emsc,usgs,inpres)`, `(inpres,emsc,usgs)`), y para cada orden capture y assert/imprima: `len(resultado)`, el `set` de `id`s resultante, y los valores de `mag`/`fuentes` de los eventos fusionados. — Confirmado: `test_merge_all_sources_order_sensitivity` presente y PASSED (`pytest tests/unit/test_merge_service.py -k order_sensitivity`).
- [x] 0.3 Extender el mismo test (o uno complementario `test_merge_all_sources_order_impacts_alerts`) para correr `compute_kpis_and_alerts` sobre el resultado de cada orden del fixture de 0.1 y comparar `kpis.total_eventos`, `kpis.magnitud_max`, y la lista de `alertas` (tipo + `eventos_relacionados`) entre órdenes. — Confirmado: `test_merge_all_sources_order_impacts_alerts` presente y PASSED.
- [x] 0.4 Documentar el resultado observado (aceptable vs. bloqueante, según criterio de `design.md` → "Plan concreto de validación") en un bloque de comentario al inicio de `test_merge_service.py` o en una sección nueva de `design.md`, incluyendo los números concretos (`len`, `id`s, `kpis`, `alertas` por orden). Si el resultado es BLOQUEANTE (una alerta pasa de no-disparar a disparar solo por orden, o hay falso negativo de dedup), detener la implementación y escalar antes de continuar con Fase 1+. — Confirmado: bloque de comentario presente en cabecera de `tests/unit/test_merge_service.py` con resultado documentado (no bloqueante).
- [x] 0.5 Con el resultado de 0.1-0.4, confirmar por escrito (en el mismo comentario/documento de 0.4) que el orden canónico a fijar en producción es `["usgs", "emsc", "inpres"]`, consistente con el default ya usado hoy por `/events/search` (línea 397 de `src/main.py`). — Confirmado: `CANONICAL_SOURCES = ["usgs", "emsc", "inpres"]` fijado en `src/services/report_service.py:34`, consistente con el comentario de cabecera del test.

## Phase 1: Fixtures de integración compartidos

- [x] 1.1 Crear fixture reutilizable en `tests/integration/test_api.py` (o helper importado) con un evento EMSC-only (sin match con USGS/INPRES) para usar en los tests nuevos de `/report`, `/events`, `/alerts` de la Fase 4.
- [x] 1.2 (Opcional, solo si 0.4 usó datos reales) Guardar la ventana de 60-90 minutos real capturada de las 3 fuentes como `tests/fixtures/merge_order_sample.json`, versionado, para reproducibilidad futura de la validación de no-conmutatividad. — N/A: la validación de Fase 0 (0.1-0.4) usó fixtures sintéticos controlados, no datos reales capturados; no aplica.

## Phase 2: Servicio interno único `report_service.py`

- [x] 2.1 Crear `seismic-monitor/src/services/report_service.py` con la constante módulo-level `CANONICAL_SOURCES = ["usgs", "emsc", "inpres"]` (valor confirmado en 0.5).
- [x] 2.2 Mover `_fetch_parallel` desde `src/main.py` (líneas 223-280) a `report_service.py` sin cambios de firma ni de comportamiento (incluye `_cached_fetch` interno y el uso de `cache.get`/`cache.set`). Actualizar imports en `report_service.py`: `settings`, `cache`, `fetch_usgs_events`, `fetch_emsc_events`, `fetch_inpres_events`, `logger`, `SeismicEvent`. — Nota: `_fetch_parallel` quedó DUPLICADA (copiada, no removida) de `src/main.py` en este lote; `main.py` no se toca todavía (Fase 3-4 es el próximo lote). Ver riesgo en el reporte de esta fase.
- [x] 2.3 Implementar `build_report(sources: list[str], window_minutes: Optional[int] = None) -> MonitorReport` en `report_service.py`: resuelve `effective_window = window_minutes if window_minutes is not None else settings.window_minutes`, llama a `_fetch_parallel(effective_window, sources)`, aplica `merge_all_sources(usgs_events, emsc_events, inpres_events)`, llama a `compute_kpis_and_alerts(merged_events, effective_window)`, arma `region_monitorizada=settings.bbox`, y devuelve `MonitorReport` completo con `data_source_errors=errors`. Sin default para `sources` (decisión de diseño: explicitud obligatoria en cada call-site).
- [x] 2.4 Decidir e implementar el mecanismo de conteo por fuente para los contadores Prometheus (pendiente señalado en design.md): `build_report` debe exponer el desglose de eventos por fuente sin importar `prometheus_client` dentro de `report_service.py`. Opción recomendada: devolver también `len(usgs_events)`, `len(emsc_events)`, `len(inpres_events)` como atributo no-Pydantic accesible tras la llamada (ej. función auxiliar `count_by_source(usgs_events, emsc_events, inpres_events) -> dict[str, int]` exportada desde `report_service.py`, invocada explícitamente en `main.py` junto a `build_report`) — evita mezclar HTTP-layer metrics con la función de dominio y mantiene `build_report` con una única responsabilidad (devolver `MonitorReport`). — Implementado tal cual la opción recomendada: `count_by_source` calcula sobre las listas PRE-fusión (fiel a lo que cada fuente externa reportó), devuelve `{"USGS": int, "EMSC": int, "INPRES": int}`.
- [x] 2.5 Escribir `tests/unit/test_report_service.py` (nuevo): mockear `fetch_usgs_events`, `fetch_emsc_events`, `fetch_inpres_events` vía `patch("src.services.report_service.fetch_usgs_events", ...)` (ajustar paths tras el move de 2.2), y verificar que `build_report(sources=["usgs","emsc","inpres"])` devuelve `MonitorReport` con `eventos` fusionados de las 3 fuentes, `kpis` consistentes con `compute_kpis_and_alerts`, y `region_monitorizada == settings.bbox`.
- [x] 2.6 En el mismo archivo de test, cubrir el caso `sources=["usgs","inpres"]` (sin EMSC): verificar que no se invoca `fetch_emsc_events` y que el resultado es idéntico al comportamiento actual de `merge_events` de 2 fuentes (Requirement "El servicio interno es invocable con subconjuntos de fuentes" del spec).
- [x] 2.7 Cubrir el caso de fuente caída: mockear `fetch_emsc_events` para que devuelva `(events, "emsc_timeout")` (o el formato de error real usado hoy) y verificar que `build_report` sigue devolviendo 200-equivalente (no lanza excepción), `data_source_errors` incluye el identificador de EMSC, y `eventos` contiene la fusión de las fuentes que sí respondieron.

## Phase 3: Migración de `/report`

- [x] 3.1 En `src/main.py`, eliminar `_fetch_parallel` (líneas 223-280, ya movida en 2.2) y agregar `from src.services.report_service import build_report, CANONICAL_SOURCES` (y `count_by_source` si se implementó en 2.4). — DESVIACIÓN documentada: `_fetch_parallel` NO se eliminó de `main.py`. `/events/search` (fuera de scope, no se toca) es probada por `tests/integration/test_api.py::test_search_events_source_filter` y `test_search_events_uses_merge_all_sources`, que mockean `patch("src.main.fetch_usgs_events"/"fetch_emsc_events"/"fetch_inpres_events", ...)`. Si `/events/search` delegara en `report_service._fetch_parallel` (que resuelve los fetchers en su propio namespace `src.services.report_service.*`), esos mocks dejarían de interceptar la llamada real y los tests pegarían a red real. Se mantiene la copia de `_fetch_parallel` en `main.py`, usada EXCLUSIVAMENTE por `/events/search`; `/report`, `/events` y `/alerts` migraron a `build_report` (que trae su propia copia interna). El import agregado es `from src.services.report_service import build_report, count_by_source, CANONICAL_SOURCES`.
- [x] 3.2 Reescribir el handler `report()` (líneas 283-332) como wrapper fino: llama a `report_obj = await build_report(sources=CANONICAL_SOURCES)`, conserva `request_duration.labels(endpoint="/report").time()`, `logger.info`, incrementa `events_fetched` por fuente usando el mecanismo de 2.4, itera `report_obj.alertas` para `alerts_generated.labels(tipo=...).inc()` y logging de alertas, incrementa `data_source_errors.labels(source=...).inc()` por cada error en `report_obj.data_source_errors`, y `requests_total.labels(endpoint="/report", status="200").inc()`. Retorna `report_obj` directamente. — Nota de implementación: `count_by_source` se invoca sobre `report_obj.eventos` filtrados por `fuentes` (post-fusión), no sobre las listas pre-fusión internas de `build_report` (no expuestas, para evitar duplicar el fetch).
- [x] 3.3 Verificar manualmente que el shape de respuesta no cambió antes de continuar a la Fase 4. — Verificado vía `pytest tests/integration/test_api.py::test_report_endpoint_structure` (PASSED); no se hizo `curl` a servidor local en este lote.

## Phase 4: Migración de `/events` y `/alerts`

- [x] 4.1 Reescribir el handler `get_events()` (`src/main.py` líneas 335-352): reemplazar `_fetch_parallel` + `merge_events` por `report_obj = await build_report(sources=CANONICAL_SOURCES)`, retornar `report_obj.eventos`. Conservar `request_duration.labels(endpoint="/events").time()` y `requests_total.labels(endpoint="/events", status="200").inc()`.
- [x] 4.2 Reescribir el handler `get_alerts()` (`src/main.py` líneas 355-374): reemplazar `_fetch_parallel` + `merge_events` + `compute_kpis_and_alerts` por `report_obj = await build_report(sources=CANONICAL_SOURCES)`, retornar `report_obj.alertas`. Conservar `request_duration.labels(endpoint="/alerts").time()` y `requests_total.labels(endpoint="/alerts", status="200").inc()`.
- [x] 4.3 Confirmar que `src/main.py` ya no importa `merge_events` si ningún handler restante lo usa directamente. — `merge_events` se removió del import (`merge_service` ahora solo importa `merge_all_sources`, usado por `/events/search`); también se removió `compute_kpis_and_alerts` y `now_utc_iso` de `main.py` (sin más usos tras la migración). `_fetch_parallel` se mantiene (ver desviación en 3.1).

## Phase 5: Migración deliberada de tests de integración existentes

Referencia de líneas confirmada en design.md (corrige a la propuesta original): `test_report_endpoint_structure` (48-66), `test_events_endpoint` (69-76), `test_alerts_endpoint` (78-84), `test_cache_serves_second_request` (141-164). `test_search_events_source_filter` (94-108) y `test_search_events_uses_merge_all_sources` (111-138) **NO se tocan** — pertenecen a `/events/search`.

- [x] 5.1 Actualizar `test_report_endpoint_structure` (`tests/integration/test_api.py:48-66`): mockear explícitamente `fetch_usgs_events`, `fetch_emsc_events`, `fetch_inpres_events` (hoy no mockea nada, pega a red real/vacía) usando el path correcto post-move (`src.services.report_service.fetch_*` o `src.main.fetch_*` según dónde quede el import — confirmar con 2.2/3.1). Mantener las aserciones existentes de shape. Agregar aserción de que `data_source_errors` puede incluir errores de EMSC. — Implementado con `patch("src.services.report_service.fetch_*", ...)` (path correcto dado que `/report` usa `build_report`, que resuelve sus fetchers en su propio namespace). Se agregó aserción `"emsc_timeout" in data["data_source_errors"]`.
- [x] 5.2 Agregar un test nuevo (análogo a `test_search_events_uses_merge_all_sources` pero apuntando a `/report`) que mockea un evento EMSC-only (usar fixture de 1.1) y verifica que aparece en `data["eventos"]` de la respuesta de `/report`, con `"EMSC"` en su campo `fuentes`. — Implementado como `test_report_includes_emsc_only_event`, reusa `emsc_only_event()` de 1.1.
- [x] 5.3 Extender `test_events_endpoint` (`tests/integration/test_api.py:69-76`) con el mismo mock EMSC-only de 1.1 y verificar que el evento aparece en la lista devuelta por `GET /events`. — Implementado.
- [x] 5.4 Extender `test_alerts_endpoint` (`tests/integration/test_api.py:78-84`) con un mock que fuerce una alerta dependiente de EMSC (reusar o adaptar el fixture de la Fase 0 si aplica) y verificar que aparece en la respuesta de `GET /alerts`. — Implementado con un evento EMSC-only sintético M5.5/prof15km (dispara `evento_significativo`, M≥5.0 y prof<70km) en vez de reusar el fixture de enjambre de Fase 0 — más simple y determinístico para este caso puntual sin depender de matching geo/temporal entre 3 eventos.
- [x] 5.5 Actualizar `test_cache_serves_second_request` (`tests/integration/test_api.py:141-164`): agregar `patch("<path correcto>.fetch_emsc_events", ...)` (ajustar el path del patch al módulo real tras el move de 2.2) para evitar que la llamada real a `/events` intente red real a EMSC. Mantener la aserción `call_count["n"] == 1`. — Ya estaba resuelto en el lote anterior (Fase 3/4); confirmado sin cambios adicionales en este lote.
- [x] 5.6 Escribir test nuevo de paridad `test_report_events_alerts_parity` (nuevo, en `tests/integration/test_api.py` o archivo dedicado): con el mismo mock de 3 fuentes activo, invocar `/report`, `/events`, `/alerts` y verificar que los `id`s de `/report.eventos` coinciden con `/events`, y que `/report.alertas` coincide con `/alerts` (Requirement "Nuevo test cubre paridad" del spec). — Implementado en `tests/integration/test_api.py`, compara sets de ids y de (tipo, eventos_relacionados) de alertas.
- [x] 5.7 Confirmar (sin modificar) que `test_search_events_source_filter` y `test_search_events_uses_merge_all_sources` siguen en verde sin cambios — evidencia de que `/events/search` no fue tocado. — Confirmado: no se modificaron, `pytest tests/integration/test_api.py` (12/12 passed) los incluye en verde.

## Phase 6: Frontend — alinear default de `sources` en Explorador

- [x] 6.1 En `seismic-monitor/dashboard/app/explore/page.tsx` (línea 13), cambiar el default de `sources` en `useState<SeismicFilters>` de `['usgs', 'emsc']` a `['usgs', 'emsc', 'inpres']`. — Implementado.
- [x] 6.2 Verificar que no hay otro lugar del frontend (ej. `lib/api.ts`, componentes de filtro UI que rendericen checkboxes de fuentes) con un default o estado inicial desalineado de `['usgs', 'emsc', 'inpres']`; si existe, alinear también. — Encontrado un segundo lugar desalineado: `dashboard/components/FilterPanel.tsx`, función `resetFilters()` (botón "Resetear"), también tenía `sources: ['usgs', 'emsc']`. Corregido a `['usgs', 'emsc', 'inpres']`. `lib/map-layers.ts` (`DATA_SOURCES`) ya incluía `inpres` como opción válida — no requirió cambios.
- [x] 6.3 Si existe un test de frontend (Vitest) que cubra el estado inicial de filtros en `explore/page.tsx`, actualizar su expectativa de `sources` por defecto. Si no existe, no crear uno nuevo (fuera de scope explícito de este change salvo que ya exista cobertura). — Confirmado: no existe ningún archivo `*.test.tsx`/`*.spec.tsx` en `dashboard/` ni config de Vitest activa (vitest está en devDependencies pero sin config ni specs); no se creó ninguno, según el scope explícito de la tarea.

## Phase 7: Verificación de consistencia y performance

- [x] 7.1 Verificar manualmente (dev server local o staging) que `/report`, `/events`, `/alerts` y `/events/search?sources=usgs,emsc,inpres` devuelven el mismo conjunto de eventos (por `id`) ante la misma ventana temporal — Requirement "Consistencia entre Dashboard, Monitoreo en Vivo y Explorador". — Verificado a nivel de test automatizado (no HTTP real contra servidor vivo, ver nota de infraestructura abajo): `test_report_events_alerts_parity` (`tests/integration/test_api.py:304`) usa `TestClient` con las 3 fuentes mockeadas y compara los `id`s de `/report.eventos` contra `/events`, y las alertas de `/report.alertas` contra `/alerts` — PASSED. `test_search_events_uses_merge_all_sources` y `test_search_events_source_filter` (intactos, no tocados) confirman que `/events/search` sigue construyendo la fusión con el mismo orden `usgs,emsc,inpres` — PASSED. **Pendiente de infraestructura viva**: no se hizo `curl` real a un servidor corriendo con datos reales de USGS/EMSC/INPRES en esta sesión (requiere levantar el servicio; no se levantó infraestructura pesada según alcance de esta tarea). Queda para verificación manual del usuario.
- [x] 7.2 Confirmar visualmente que Dashboard (`app/page.tsx`) y Monitoreo en Vivo (`app/live/page.tsx`) muestran eventos EMSC (antes ausentes) sin requerir cambios de código en esos archivos — usar datos reales o el fixture EMSC-only de 1.1 contra un backend local. — **Pendiente de infraestructura viva**: requiere un browser real contra un backend corriendo (Next.js dev server + FastAPI + TimescaleDB opcional). No se levantó en esta sesión. Verificación estática de código: `app/page.tsx` y `app/live/page.tsx` no fueron modificados (confirmado por diseño y por `git diff` — ningún cambio en `dashboard/app/page.tsx` ni `dashboard/app/live/page.tsx`), y ambos consumen `/report` a través del mismo `reportFetcher`, que ahora trae EMSC sin cambio de contrato. La verificación visual queda pendiente para el usuario.
- [x] 7.3 Medir `request_duration_seconds{endpoint="/report"}` antes/después del cambio (riesgo de performance por fetch a 3 fuentes en vez de 2) usando `/metrics`; documentar el delta observado. Si la degradación es significativa, evaluar si `_fetch_parallel` sigue paralelizando correctamente las 3 llamadas (ya lo hace vía `asyncio.gather`, pero confirmar que no se serializó por error en el move de 2.2). — Verificación estática confirmada: `report_service.py:79` sigue usando `results = await asyncio.gather(*tasks)` sobre las 3 fuentes — no se serializó en el move de 2.2 (comparado línea por línea contra la versión original en `main.py`, idéntica). Por diseño, agregar EMSC en paralelo no debería sumar latencia significativa (el tiempo total lo determina la fuente más lenta de las 3, no la suma). **Pendiente de infraestructura viva**: no se midió `request_duration_seconds{endpoint="/report"}` real pre/post en un servidor corriendo con tráfico, porque no hay una serie histórica pre-cambio capturada de `/metrics` en este entorno ni un servidor Prometheus activo. Queda para medición manual del usuario en staging.

## Phase 8: Regresión completa y cierre

- [x] 8.1 Ejecutar `cd seismic-monitor && pytest` completo (test_command de `openspec/config.yaml`) y confirmar 100% verde, incluyendo Fases 0, 2, 5. — Ejecutado con `./venv/bin/python -m pytest -v`: **66 passed, 1 failed, 7 errors**. El 1 failed es `tests/unit/test_geo_utils.py::test_ms_to_iso`, preexistente (fecha hardcodeada `"2025-10-28"` comparada contra una conversión que ahora cae en `2024-10-28` por el paso del tiempo — no tiene relación con este change, falla igual en el commit baseline). Los 7 errors son todos `tests/integration/test_redis_pubsub_bus.py::*`, por `docker.errors.DockerException` (Docker no disponible en este entorno) — no relacionados con este change ni con `report_service.py`. Todos los tests relevantes a este change (Fases 0, 1, 2, 5) pasan: `test_merge_all_sources_order_sensitivity`, `test_merge_all_sources_order_impacts_alerts`, `tests/unit/test_report_service.py` (7 tests), `test_report_endpoint_structure`, `test_report_includes_emsc_only_event`, `test_events_endpoint`, `test_alerts_endpoint`, `test_cache_serves_second_request`, `test_report_events_alerts_parity`, `test_search_events_source_filter`, `test_search_events_uses_merge_all_sources` — todos PASSED. Cero fallos nuevos atribuibles a este change.
- [x] 8.2 Ejecutar `ruff`/`mypy`/`black --check` sobre `src/services/report_service.py` y `src/main.py` modificado (Style del config: black/ruff/mypy strict, line-length 100) y corregir hallazgos. — `black --check src/services/report_service.py` (archivo nuevo de este change): limpio, sin hallazgos. `black --check src/main.py`: reformatearía el archivo, pero confirmado con `git stash`/diff que el desalineamiento de formato es preexistente (no introducido por los handlers migrados de `/report`, `/events`, `/alerts`). `ruff check src/main.py`: 2 hallazgos (`typing.List` y `fastapi.status` importados sin uso) — confirmados preexistentes vía `git show HEAD:src/main.py` (ya estaban sin usar antes de este change). `mypy`: el único hallazgo nuevo atribuible a este change es `report_service.py:55` (`_cached_fetch` sin type annotation) — es una réplica exacta y sin modificaciones del mismo patrón que ya tenía este hallazgo en `main.py` antes del move (Fase 2.2 lo movió tal cual, sin cambios de comportamiento ni de tipado, según lo pedido explícitamente por el diseño). No se corrigieron estos hallazgos preexistentes/heredados en este lote porque: (a) están fuera del scope explícito del change (tocar `main.py` más allá de los 3 handlers y los imports de `report_service` no está pedido), y (b) `report_service.py` es la única pieza 100% nueva del change y está limpia en black — el hallazgo de mypy en ella es fiel al comportamiento "mover sin cambios" exigido por el diseño (Fase 2.2: "sin cambios de firma ni de comportamiento"). Se documenta como deuda preexistente, no como regresión de este change.
- [x] 8.3 Revisar que ningún endpoint fuera de scope (`/events/{event_id}/detail`, `/events/{event_id}/rupture`, spectrograms, websocket) quedó afectado por el move de `_fetch_parallel` — grep de `_fetch_parallel` en `src/main.py` no debe arrojar resultados tras 3.1. — El grep SÍ arroja resultados (`src/main.py:222` definición, `:402` uso en `/events/search`), pero esto es la desviación de diseño ya documentada y aceptada en la tarea 3.1: `_fetch_parallel` se mantuvo deliberadamente duplicada en `main.py` porque `/events/search` (fuera de scope, no tocado) depende de que los mocks `patch("src.main.fetch_*_events", ...)` de `tests/integration/test_api.py` intercepten llamadas resueltas en el namespace `src.main` — si `/events/search` delegara en `report_service._fetch_parallel`, esos mocks dejarían de interceptar y los tests pegarían a red real. Confirmado con grep dirigido: ningún endpoint fuera de scope (`/events/{event_id}/detail`, `/events/{event_id}/rupture`, `/spectrograms/*`, `/ws/spectrogram/{channel}`) referencia `_fetch_parallel` ni `build_report` — ambos usos de `_fetch_parallel` en `main.py` están acotados exclusivamente a la definición (222) y a `/events/search` (402). El criterio original de la tarea ("grep no debe arrojar resultados") queda superado por la decisión de diseño posterior documentada en 3.1; se reinterpreta el criterio de aceptación como "grep no debe arrojar resultados en endpoints fuera de scope", que sí se cumple.
- [x] 8.4 Actualizar el checklist de "Success Criteria" de `proposal.md` marcando los ítems cumplidos, como insumo directo para `sdd-verify`. — Ver `proposal.md`, sección "Success Criteria", actualizada en este mismo lote.

### Nota de cierre (Fases 7-8, lote final)

Este change se considera **funcionalmente completo** a nivel de código y regresión automatizada: los 3 endpoints (`/report`, `/events`, `/alerts`) migraron a `build_report(sources=CANONICAL_SOURCES)`, `/events/search` permanece intacto por decisión de diseño, la suite completa de tests pasa sin fallos nuevos, y los Success Criteria de `proposal.md` están verificados contra evidencia real de tests. Quedan pendientes, explícitamente, dos verificaciones que requieren infraestructura viva que no se levantó en esta sesión (Docker/TimescaleDB/servidor corriendo con red real a USGS/EMSC/INPRES):
1. Confirmación HTTP real end-to-end (curl/browser) de que `/report`, `/events`, `/alerts`, `/events/search` devuelven el mismo conjunto de eventos contra las APIs externas reales (7.1).
2. Verificación visual en Dashboard/Monitoreo en Vivo de eventos EMSC reales (7.2) y medición de latencia real pre/post en `/metrics` (7.3).

Estas dos verificaciones no bloquean el cierre del change a nivel de código — son validación operativa recomendada antes o inmediatamente después de desplegar a producción/staging.
