# Tasks: Menú de configuración de cuenta (perfil, 2FA TOTP, exportar datos, borrar cuenta)

Convención de trazabilidad: cada tarea de testing referencia el escenario exacto de `specs/account-settings/spec.md` que cubre, usando el formato `[Requirement: X / Scenario: Y]`.

Nota de nomenclatura (decisión ya cerrada, ver design.md Decision 2 y aclaración adicional del usuario): `full_name` (editable por el usuario, vive solo en `/account/profile`) y `name` (poblado por Google OAuth, migración 004, expuesto en `/auth/me`/JWT/header/avatar) se mantienen DELIBERADAMENTE separados en todo este change. Ninguna tarea debe unificarlos, hacer que uno sobreescriba al otro, ni exponer `full_name` fuera de `/account/profile`, ni exponer `name` dentro de `/account/profile` como si fuera el mismo campo.

## Phase 1: Foundation (migración, dependencia, modelos)

- [x] 1.1 Confirmar por inspección que `users.created_at`/`users.updated_at` YA EXISTEN desde `deploy/sql/migrations/001_create_users_table.sql` (líneas 16-17: ambas `TIMESTAMPTZ NOT NULL DEFAULT now()`) — resuelve la Open Question de `design.md` (Decision 5/Rationale). Documentar explícitamente en el header de la migración 005 que esta tarea NO agrega `created_at`/`updated_at` porque ya existen; no crear columnas duplicadas
- [x] 1.2 Crear `deploy/sql/migrations/005_account_settings.sql` con el SQL exacto de `design.md` Decision 2: `full_name TEXT`, `address TEXT`, `phone TEXT`, `totp_secret TEXT`, `totp_enabled BOOLEAN NOT NULL DEFAULT false` como `ALTER TABLE users ADD COLUMN IF NOT EXISTS ...`; tabla `user_backup_codes` (`id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE`, `code_hash TEXT NOT NULL`, `used_at TIMESTAMPTZ`, `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`) con `CREATE TABLE IF NOT EXISTS`; índice `idx_user_backup_codes_user_id`. Incluir bloque de rollback en comentario al pie (`DROP TABLE user_backup_codes; ALTER TABLE users DROP COLUMN full_name, DROP COLUMN address, DROP COLUMN phone, DROP COLUMN totp_secret, DROP COLUMN totp_enabled`), mismo formato que 003/004. No editar 001-004 (ya aplicadas)
- [x] 1.3 Aplicar la migración 005 contra el Postgres real del perfil `storage` en `deploy/docker/docker-compose.yml` (contenedor `timescaledb`, puerto 5433, `docker exec`, mismo mecanismo que 001-004) y confirmar con `\d users` que las 5 columnas nuevas existen con los tipos/defaults correctos, y con `\d user_backup_codes` que la tabla, la FK `ON DELETE CASCADE` y el índice se crearon correctamente
- [x] 1.4 Fijar la versión exacta de `pyotp` a instalar: verificar en PyPI la última versión estable (design.md referencia `2.9.0` como conocida al momento del diseño, pero esta tarea debe confirmar/actualizar el número real, mismo criterio ya aplicado a `Authlib==1.7.2` en `google-oauth` tarea 1.4). Agregar `pyotp==<version-confirmada>` a `requirements.txt`. Confirmar explícitamente que NO se agrega `qrcode` (Decision 7 del design — QR se renderiza client-side)
- [x] 1.5 Instalar la dependencia nueva (`pip install -r requirements.txt` en el venv existente) y confirmar con `pip check` que no hay conflictos de versión con las dependencias ya instaladas (`fastapi`, `Authlib`, `passlib`, etc.)
- [x] 1.6 En `src/models/user.py`, agregar los modelos nuevos con el shape exacto de `design.md` (Interfaces/Contracts): `UserProfile` (`full_name: Optional[str] = None`, `address: Optional[str] = None`, `phone: Optional[str] = None`), `UserProfileUpdate` (mismo shape, para `PATCH`), `TotpSetupResponse` (`otpauth_uri: str`, `backup_codes: list[str]`), `TotpVerifyRequest` (`code: str = Field(..., min_length=6, max_length=9)`), `AccountExport` (`account: dict`, `profile: UserProfile`, `security: dict`, `exported_at: datetime`). Confirmar por inspección que ninguno de estos modelos se importa ni se mezcla con `UserPublic`/`CurrentUser` (aislamiento de Decisión Cerrada #4 del proposal)

## Phase 2: Backend core (AuthService)

- [x] 2.1 En `src/services/auth_service.py`, implementar `get_profile(self, user_id: UUID) -> UserProfile`: `SELECT full_name, address, phone FROM users WHERE id = $1`, retorna `UserProfile` con los valores (o todos `None` si nunca se completó el perfil, sin error)
- [x] 2.2 Test unitario en `tests/unit/test_auth_service.py` para `get_profile()`: perfil completo devuelve los tres valores — `[Requirement: Consulta del perfil extendido propio / Scenario: Usuario autenticado consulta su perfil con todos los campos completos]`
- [x] 2.3 Test: perfil nunca completado devuelve los tres campos `None` sin error — `[Requirement: Consulta del perfil extendido propio / Scenario: Usuario autenticado consulta su perfil sin haberlo completado nunca]`
- [x] 2.4 En `src/services/auth_service.py`, implementar `update_profile(self, user_id: UUID, update: UserProfileUpdate) -> UserProfile`: `UPDATE` parcial solo de los campos presentes en `update` (excluir `None`/no-enviados vía `model_dump(exclude_unset=True)` o equivalente), actualizando también `updated_at = now()`. El método NO debe aceptar ni tocar `role`, `email`, `password_hash` bajo ninguna circunstancia — la firma del método (recibe `UserProfileUpdate`, no un dict arbitrario) ya lo garantiza por diseño de tipos, documentar esto explícitamente en el docstring
- [x] 2.5 Test: completar perfil por primera vez con los tres campos — `[Requirement: Edición del perfil extendido propio / Scenario: Usuario completa su perfil por primera vez con todos los campos]`
- [x] 2.6 Test: edición parcial (un solo campo) deja el resto sin tocar — `[Requirement: Edición del perfil extendido propio / Scenario: Usuario edita parcialmente su perfil dejando el resto sin tocar]`
- [x] 2.7 Test: `update_profile()` con un `UserProfileUpdate` vacío no falla y no modifica nada — `[Requirement: Edición del perfil extendido propio / Scenario: Perfil se puede dejar completamente vacío sin error]`
- [x] 2.8 Test: confirmar por inspección de tipos + test que `UserProfileUpdate` no tiene campos `role`/`email`/`password_hash` (Pydantic rechazaría o ignoraría un campo desconocido según config del modelo) — `[Requirement: Edición del perfil extendido propio / Scenario: Intento de modificar role o email vía este endpoint es ignorado o rechazado]`
- [x] 2.9 En `src/services/auth_service.py`, extender `create_access_token()` para aceptar un parámetro `pending_2fa: bool = False`: cuando es `True`, emite el JWT con claims `{"sub": ..., "pending_2fa": true, "typ": "pre_auth"}` y expiración de 2 minutos (no la expiración estándar de `AUTH_TOKEN_EXPIRE_MINUTES`); cuando es `False` (default), comportamiento idéntico al actual, sin ningún claim nuevo agregado — no debe alterar el shape del token para logins sin 2FA
- [x] 2.10 Extender `decode_access_token()` (si hace falta) para que el payload decodificado exponga `pending_2fa`/`typ` cuando estén presentes, sin lanzar error por su sola presencia (el rechazo vive en `deps.py`, no en `AuthService` — ver Phase 3)
- [x] 2.11 Test unitario: extender `test_create_and_decode_access_token_roundtrip` (o equivalente) confirmando que `create_access_token(user, pending_2fa=True)` produce un token cuyo payload decodificado tiene `pending_2fa=True` y una expiración ~2 minutos en el futuro (no la expiración estándar); y que `create_access_token(user)` (sin el flag) sigue produciendo el shape idéntico al pre-existente, sin el claim `pending_2fa`
- [x] 2.12 En `src/services/auth_service.py`, agregar las excepciones nuevas: `TotpAlreadyEnabledError`, `TotpNotAvailableForGoogleOnlyUserError`, `InvalidTotpCodeError`, `TotpNotEnabledError`, `LastSuperadminError` (clases simples, mismo patrón que `EmailAlreadyRegisteredError` ya existente)
- [x] 2.13 En `src/services/auth_service.py`, implementar `enable_totp(self, user_id: UUID) -> tuple[str, list[str]]` con la transacción exacta de `design.md` Decision 3: `SELECT password_hash, totp_enabled FROM users WHERE id = $1 FOR UPDATE` → `raise TotpNotAvailableForGoogleOnlyUserError` si `password_hash IS NULL`; `raise TotpAlreadyEnabledError` si `totp_enabled` ya es `true`; genera `totp_secret` nuevo con `pyotp.random_base32()`, `UPDATE users SET totp_secret = $1 WHERE id = $2` (`totp_enabled` permanece `false`); `DELETE FROM user_backup_codes WHERE user_id = $1` (invalida restos de un setup anterior no completado); genera 10 códigos con `secrets.token_hex(4)` formateados `"XXXX-XXXX"` (Decision 6), inserta cada uno hasheado con `_pwd_context.hash(code)` en `user_backup_codes`; retorna `(otpauth_uri, backup_codes_en_claro)` donde `otpauth_uri = pyotp.totp.TOTP(secret).provisioning_uri(name=email, issuer_name="GeoSpectrum")`
- [x] 2.14 Test: usuario con password propio y `totp_enabled=false` → `enable_totp()` retorna `otpauth_uri` válido + 10 backup codes en claro, `totp_enabled` permanece `false` tras la llamada — `[Requirement: Activación de 2FA TOTP restringida a usuarios con password propio / Scenario: Usuario con password activa 2FA exitosamente]`
- [x] 2.15 Test: usuario con `password_hash IS NULL` (100% Google) → `enable_totp()` lanza `TotpNotAvailableForGoogleOnlyUserError`, y se confirma por query directa que NO se persistió ningún `totp_secret` ni fila en `user_backup_codes` para ese usuario — `[Requirement: Activación de 2FA TOTP restringida a usuarios con password propio / Scenario: Usuario 100% Google sin password es rechazado al intentar activar 2FA]`
- [x] 2.16 Test: usuario con `totp_enabled=true` que llama `enable_totp()` de nuevo → lanza `TotpAlreadyEnabledError` (cubre la rama de re-setup sin disable previo, complementario al escenario de spec de re-setup que sí permite avanzar tras `disable_totp()` — ver 2.20)
- [x] 2.17 En `src/services/auth_service.py`, implementar `verify_totp_setup(self, user_id: UUID, code: str) -> None`: `pyotp.TOTP(secret).verify(code)` contra el `totp_secret` ya guardado — válido → `UPDATE users SET totp_enabled = true WHERE id = $1`; inválido → `raise InvalidTotpCodeError` sin modificar `totp_enabled`
- [x] 2.18 Test: código TOTP válido generado a partir del secreto de un `enable_totp()` previo → `verify_totp_setup()` marca `totp_enabled=true` — `[Requirement: Verificación del código TOTP en el setup / Scenario: Código TOTP válido en el setup habilita 2FA]`
- [x] 2.19 Test: código arbitrario/incorrecto (ej. `"000000"`) → `verify_totp_setup()` lanza `InvalidTotpCodeError`, `totp_enabled` permanece `false` — `[Requirement: Verificación del código TOTP en el setup / Scenario: Código TOTP inválido en el setup no habilita 2FA]`
- [x] 2.20 Test: llamar `enable_totp()` dos veces seguidas (re-setup) sin `disable_totp()` en el medio lanza `TotpAlreadyEnabledError` en la segunda si la primera ya completó `verify_totp_setup()`; si se llama `enable_totp()` → `enable_totp()` de nuevo ANTES de verificar (setup incompleto, `totp_enabled` sigue `false`), la segunda llamada SÍ debe suceder (no está `enabled` todavía) y los backup codes de la primera quedan invalidados por el `DELETE` de 2.13 — cubre la semántica de "un segundo setup invalida el secreto/códigos anteriores" de `[Requirement: Backup codes expuestos una única vez / Scenario: Una segunda llamada de setup nunca vuelve a exponer los backup codes originales en claro]` a nivel de service (el endpoint solo permite este flujo si `totp_enabled=false`; el caso de re-setup completo tras `disable_totp()` se cubre en Phase 3/5 a nivel HTTP)
- [x] 2.21 En `src/services/auth_service.py`, implementar `disable_totp(self, user_id: UUID) -> None`: `UPDATE users SET totp_secret = NULL, totp_enabled = false WHERE id = $1` + `DELETE FROM user_backup_codes WHERE user_id = $1` en la misma transacción; idempotente (si `totp_enabled` ya era `false`, no falla, no-op observable)
- [x] 2.22 Test: usuario con `totp_enabled=true` → `disable_totp()` deja `totp_enabled=false`, `totp_secret=NULL`, y cero filas en `user_backup_codes` para ese usuario — `[Requirement: Deshabilitación de 2FA / Scenario: Usuario autenticado deshabilita su 2FA exitosamente]`
- [x] 2.23 Test: `disable_totp()` sobre un usuario que ya tenía `totp_enabled=false` no lanza excepción (idempotencia documentada en el docstring de 2.21)
- [x] 2.24 En `src/services/auth_service.py`, implementar `consume_backup_code(self, user_id: UUID, code: str) -> bool` con la transacción exacta de `design.md` Decision 3: `SELECT id, code_hash FROM user_backup_codes WHERE user_id = $1 AND used_at IS NULL FOR UPDATE`; itera cada fila con `_pwd_context.verify(code, code_hash)` hasta encontrar match (bcrypt no es indexable/determinístico, volumen máximo 10 filas); si matchea, `UPDATE user_backup_codes SET used_at = now() WHERE id = $1` → `return True`; si ninguna matchea → `return False` sin lanzar excepción (el caller decide el 401 genérico)
- [x] 2.25 Test: backup code válido y no usado → `consume_backup_code()` retorna `True` y la fila queda con `used_at` seteado — insumo directo de `[Requirement: Uso de backup codes como alternativa al código TOTP en el login / Scenario: Login exitoso usando un backup code válido en vez del código TOTP]`
- [x] 2.26 Test: el mismo backup code, usado una segunda vez → `consume_backup_code()` retorna `False` (la fila ya tiene `used_at` no-nulo, el filtro `WHERE used_at IS NULL` la excluye) — `[Requirement: Uso de backup codes como alternativa al código TOTP en el login / Scenario: Un backup code ya usado no puede reutilizarse]`
- [x] 2.27 En `src/services/auth_service.py`, implementar `verify_totp_or_backup_code(self, user_id: UUID, code: str) -> bool`: intenta `pyotp.TOTP(totp_secret).verify(code, valid_window=1)` primero; si no matchea, intenta `consume_backup_code(user_id, code)`; retorna `True`/`False` combinado, sin distinguir en ningún mensaje de error cuál de los dos métodos falló (mismo criterio de no filtrar información que aplica a errores de login por password)
- [x] 2.28 Test: código TOTP válido vigente → `verify_totp_or_backup_code()` retorna `True` sin consumir ningún backup code (confirmar que ninguna fila de `user_backup_codes` cambia `used_at`)
- [x] 2.29 Test: código que no matchea como TOTP pero sí como backup code válido → `verify_totp_or_backup_code()` retorna `True` Y el backup code queda consumido (delega correctamente a `consume_backup_code()`)
- [x] 2.30 Test: código inválido tanto como TOTP como backup code → `verify_totp_or_backup_code()` retorna `False`, ninguna fila de `user_backup_codes` se modifica
- [x] 2.31 En `src/services/auth_service.py`, implementar `export_user_data(self, user_id: UUID) -> AccountExport` con el shape exacto de `design.md` Decision 5 (`account`: `id`, `email`, `role`, `google_id`, `name`, `avatar_url`, `created_at`, `updated_at`; `profile`: `UserProfile` con `full_name`/`address`/`phone`; `security`: `has_password` (`password_hash IS NOT NULL`, nunca el hash), `totp_enabled`, `linked_google_account` (`google_id IS NOT NULL`); `exported_at`: timestamp de la llamada). Confirmar por inspección del código Y por test que el método NUNCA hace `SELECT password_hash` ni `totp_secret` ni `code_hash` hacia ningún campo del objeto retornado
- [x] 2.32 Test: `export_user_data()` de un usuario con perfil parcial retorna JSON válido con `email`, `role`, y los campos de perfil — `[Requirement: Exportación de los propios datos de cuenta / Scenario: Usuario autenticado exporta sus propios datos]`
- [x] 2.33 Test: el objeto retornado por `export_user_data()` NUNCA contiene `password_hash`, `totp_secret`, ni `code_hash`/backup codes bajo ninguna forma (assert explícito recorriendo el `dict`/modelo serializado, no solo confiar en el shape declarado) — cubre la prohibición explícita del mismo Requirement
- [x] 2.34 Test: `export_user_data(usuario_a)` nunca incluye ningún valor de `usuario_b` (dos usuarios con perfiles distintos en la misma base de test) — `[Requirement: Exportación de los propios datos de cuenta / Scenario: El export nunca incluye datos de otro usuario]`
- [x] 2.35 En `src/services/auth_service.py`, implementar `delete_account(self, user_id: UUID) -> None` con la versión CORREGIDA (no el pseudo-código insuficiente inicial) documentada en `design.md` Decision 4, sección "Riesgo residual y mitigación": dentro de `async with conn.transaction()`, primero `SELECT role FROM users WHERE id = $1 FOR UPDATE` (lock de la fila propia, retorna sin error si `row is None` — idempotente); si `role == 'superadmin'`, ejecutar **`SELECT COUNT(*) FROM users WHERE role = 'superadmin' FOR UPDATE`** (el `FOR UPDATE` sobre el COUNT bloquea las filas de TODOS los superadmins, no un `COUNT(*)` plano sin lock — esto es lo que serializa dos borrados concurrentes de los últimos dos superadmins; el `COUNT` sin `FOR UPDATE` del pseudo-código inicial del design es explícitamente insuficiente y NO debe implementarse así); si el resultado es `1`, `raise LastSuperadminError()` sin ejecutar el DELETE; si no, `DELETE FROM users WHERE id = $1` (el `ON DELETE CASCADE` de `user_backup_codes` limpia esas filas automáticamente, sin `DELETE` explícito adicional)
- [x] 2.36 Test: usuario con rol distinto de superadmin (o superadmin no-único) → `delete_account()` borra la fila exitosamente — `[Requirement: Eliminación de la propia cuenta / Scenario: Usuario no-superadmin-único elimina su propia cuenta exitosamente]`
- [x] 2.37 Test: único superadmin del sistema (`COUNT(*) WHERE role='superadmin') = 1`) → `delete_account()` lanza `LastSuperadminError`, la fila permanece intacta en `users` — `[Requirement: Eliminación de la propia cuenta / Scenario: El último superadmin del sistema no puede eliminar su propia cuenta]`
- [x] 2.38 Test: dos o más superadmins en el sistema, uno de ellos llama `delete_account()` → se borra exitosamente, el/los otro(s) superadmin(es) siguen existiendo e intactos — `[Requirement: Eliminación de la propia cuenta / Scenario: Un superadmin que no es el único puede eliminar su propia cuenta]`
- [x] 2.39 Test explícito de concurrencia (documentando el fix del Riesgo Residual de Decision 4): con exactamente dos superadmins en la base, disparar `delete_account()` para ambos de forma concurrente (`asyncio.gather` o equivalente contra Postgres real, no mockeado) y confirmar que exactamente UNO de los dos completa el DELETE y el otro recibe `LastSuperadminError` — nunca ambos completan (lo que dejaría el sistema en 0 superadmins) ni ambos son rechazados. Este test es el que verifica en runtime que el `SELECT COUNT(*) ... FOR UPDATE` (no el `COUNT` plano) efectivamente serializa la condición de carrera
- [x] 2.40 Test: `delete_account()` sobre un `user_id` que ya no existe (`row is None`) no lanza excepción — no-op idempotente, documentado explícitamente en 2.35

## Phase 3: Backend endpoints (main.py, deps.py)

- [x] 3.1 En `src/api/deps.py`, modificar `get_current_user()` para rechazar con 401 explícito cualquier token decodificado donde `payload.get("pending_2fa") is True` — el chequeo debe ejecutarse ANTES de construir el `CurrentUser`, de forma que un token de pre-auth JAMÁS resuelva una identidad completa aunque llegue en la cookie `session` (defensa en profundidad de `design.md` Decision 1)
- [x] 3.2 Test unitario en `tests/unit/test_deps.py` (crear si no existe, siguiendo el patrón de `test_decode_access_token_with_wrong_signature_raises_invalid_token_error`): construir un JWT válido en firma pero con `pending_2fa: true` (vía `jose.jwt.encode` directo, no `create_access_token`) y confirmar que `get_current_user()` responde 401, no una identidad resuelta
- [x] 3.3 En `src/main.py`, modificar `POST /auth/login`: tras `verify_password()` exitoso, chequear `user.totp_enabled`; si `False`, comportamiento idéntico al actual (emitir cookie `session` completa + `UserPublic`); si `True`, emitir `create_access_token(user, pending_2fa=True)` en una cookie NUEVA `pending_2fa_session` (httpOnly, secure, samesite=lax, `max_age=120`) y responder `200 {"requires_2fa": true}` SIN emitir la cookie `session` completa
- [x] 3.4 Implementar `POST /auth/2fa/login-verify` en `src/main.py`: requiere la cookie `pending_2fa_session` (no `Depends(get_current_user)`, ya que ese token sería rechazado por 3.1 — decodificar manualmente el pre-auth token); si la cookie está ausente/expirada/inválida → 401; body `{"code": "123456"}`; llama `verify_totp_or_backup_code(user_id_del_claim_sub, code)`; si `True` → `create_access_token(user)` (JWT completo estándar), `Set-Cookie: session`, `Delete-Cookie: pending_2fa_session`, responde 200 con `UserPublic`; si `False` → 401, sin tocar ninguna cookie de sesión completa
- [x] 3.5 Implementar `POST /auth/2fa/setup` en `src/main.py`, protegido por `Depends(get_current_user)`: llama `auth_service.enable_totp(current_user.id)`; traduce `TotpNotAvailableForGoogleOnlyUserError` → 400/409 con mensaje explícito; `TotpAlreadyEnabledError` → 409; éxito → 200 `TotpSetupResponse`
- [x] 3.6 Implementar `POST /auth/2fa/verify` en `src/main.py` (setup-verify, distinto de `login-verify` de 3.4), protegido por `Depends(get_current_user)`: body `TotpVerifyRequest`; llama `verify_totp_setup()`; `InvalidTotpCodeError` → 400/401; éxito → 200 `{}`
- [x] 3.7 Implementar `POST /auth/2fa/disable` en `src/main.py`, protegido por `Depends(get_current_user)` (garantiza sesión completa, ya que `get_current_user` rechaza `pending_2fa`): llama `disable_totp()`; siempre 200 `{}` (idempotente)
- [x] 3.8 Implementar `GET /account/profile` en `src/main.py`, protegido por `Depends(get_current_user)`: llama `get_profile()`, retorna 200 `UserProfile`
- [x] 3.9 Implementar `PATCH /account/profile` en `src/main.py`, protegido por `Depends(get_current_user)`: body `UserProfileUpdate`, llama `update_profile()`, retorna 200 `UserProfile` actualizado
- [x] 3.10 Implementar `GET /account/export` en `src/main.py`, protegido por `Depends(get_current_user)`: llama `export_user_data()`, retorna 200 `AccountExport` con `Content-Type` JSON
- [x] 3.11 Implementar `DELETE /account` en `src/main.py`, protegido por `Depends(get_current_user)`: llama `delete_account()`; `LastSuperadminError` → 409 con mensaje explícito ("no podés eliminar tu cuenta: sos el único superadmin del sistema"); éxito → 200/204, y borrar también la cookie `session` del cliente (equivalente a un logout forzado, ya que el usuario ya no existe)
- [x] 3.12 Test de integración en `tests/integration/test_auth_api.py`: `POST /auth/login` con `totp_enabled=true` y password correcto → 200 `{"requires_2fa": true}`, cookie `pending_2fa_session` seteada, SIN cookie `session` — `[Requirement: Login con 2FA habilitado requiere segundo factor / Scenario: Login con password correcto pero sin segundo factor no otorga sesión completa]`
- [x] 3.13 Test: `GET /auth/me` usando la cookie `pending_2fa_session` en lugar de `session` → 401 (verifica el rechazo de 3.1/3.2 a nivel de endpoint real, no solo unitario) — mismo Requirement que 3.12
- [x] 3.14 Test: `POST /auth/2fa/login-verify` con código TOTP válido tras un login con 2FA pendiente → 200, `Set-Cookie: session` con los mismos atributos que un login sin 2FA, `Delete-Cookie: pending_2fa_session`, `GET /auth/me` posterior con la cookie `session` responde 200 — `[Requirement: Login con 2FA habilitado requiere segundo factor / Scenario: Login completo con password y código TOTP válido]`
- [x] 3.15 Test: `POST /auth/2fa/login-verify` con código TOTP incorrecto/expirado → 401, sin `Set-Cookie: session` — `[Requirement: Login con 2FA habilitado requiere segundo factor / Scenario: Login rechazado con código TOTP incorrecto]`
- [x] 3.16 Test: `POST /auth/2fa/login-verify` con un backup code válido en lugar del TOTP → 200, sesión completa equivalente, y el backup code queda invalidado tras el uso (confirmado con una segunda llamada usando el mismo backup code, que debe fallar) — `[Requirement: Uso de backup codes como alternativa al código TOTP en el login / Scenario: Login exitoso usando un backup code válido en vez del código TOTP]` y `[Scenario: Un backup code ya usado no puede reutilizarse]`
- [x] 3.17 Test: `POST /auth/2fa/setup` con usuario `password_hash IS NULL` → 400/409, body indica explícitamente que 2FA no está disponible para cuentas sin password — `[Requirement: Activación de 2FA TOTP restringida a usuarios con password propio / Scenario: Usuario 100% Google sin password es rechazado al intentar activar 2FA]`
- [x] 3.18 Test: `POST /auth/2fa/setup` sin cookie `session` válida → 401 — `[Requirement: Activación de 2FA TOTP restringida a usuarios con password propio / Scenario: Usuario no autenticado recibe 401]`
- [x] 3.19 Test: flujo completo `POST /auth/2fa/setup` → `POST /auth/2fa/verify` con código válido → `totp_enabled=true` confirmado vía `GET /account/profile` o estado interno — `[Requirement: Verificación del código TOTP en el setup / Scenario: Código TOTP válido en el setup habilita 2FA]`
- [x] 3.20 Test: los backup codes en claro solo aparecen en la primera respuesta exitosa de `POST /auth/2fa/setup`; una segunda llamada de re-setup (tras `disable_totp`) expone backup codes NUEVOS distintos, y los originales ya no son válidos ni se muestran; ningún otro endpoint (`GET /account/profile`, `GET /auth/me`, `GET /account/export`) devuelve backup codes en claro en ningún momento — `[Requirement: Backup codes expuestos una única vez / ambos escenarios]`
- [x] 3.21 Test: `POST /auth/2fa/disable` con sesión completa → 200, `totp_enabled=false`, login posterior solo con password otorga sesión completa sin pedir segundo factor — `[Requirement: Deshabilitación de 2FA / Scenario: Usuario autenticado deshabilita su 2FA exitosamente]`
- [x] 3.22 Test: `POST /auth/2fa/disable` sin cookie `session` válida → 401, `totp_enabled` no cambia para ningún usuario — `[Requirement: Deshabilitación de 2FA / Scenario: Usuario no autenticado recibe 401 al intentar deshabilitar 2FA]`
- [x] 3.23 Test: `GET /account/profile` con perfil completo → 200 con los tres valores; sin cookie `session` → 401 — `[Requirement: Consulta del perfil extendido propio / ambos escenarios de sesión]`
- [x] 3.24 Test: `PATCH /account/profile` completa perfil por primera vez, edición parcial, body vacío sin error, intento de enviar `role`/`email` no los modifica, sin sesión → 401 — cubre los 5 escenarios de `[Requirement: Edición del perfil extendido propio]` a nivel HTTP
- [x] 3.25 Test: `GET /auth/me` de un usuario con perfil extendido completado NO incluye `domicilio`/`telefono`/`full_name` en el body — `[Requirement: Aislamiento del perfil extendido respecto de /auth/me y del JWT / Scenario: El perfil extendido no aparece en /auth/me]`
- [x] 3.26 Test: decodificar el JWT de la cookie `session` de un usuario con perfil completo y confirmar que los claims NO contienen `domicilio`/`telefono`/`full_name` — `[Requirement: Aislamiento del perfil extendido respecto de /auth/me y del JWT / Scenario: El perfil extendido no aparece en los claims del JWT]`
- [x] 3.27 Test: `GET /account/export` responde 200 JSON válido con `email`/`role`/perfil, sin `password_hash`/`totp_secret`/backup codes; sin sesión → 401 — `[Requirement: Exportación de los propios datos de cuenta / Scenario: Usuario autenticado exporta sus propios datos]` y `[Scenario: Usuario no autenticado recibe 401]`
- [x] 3.28 Test: `GET /account/export` de `usuario_a` nunca incluye datos de `usuario_b` a nivel de endpoint HTTP (dos usuarios reales autenticados con cookies distintas) — `[Requirement: Exportación de los propios datos de cuenta / Scenario: El export nunca incluye datos de otro usuario]`
- [x] 3.29 Test: `DELETE /account` de un usuario no-superadmin-único → 200/204, la fila ya no existe, `POST /auth/login` posterior con esas credenciales → 401 igual que email inexistente — `[Requirement: Eliminación de la propia cuenta / Scenario: Usuario no-superadmin-único elimina su propia cuenta exitosamente]`
- [x] 3.30 Test: `DELETE /account` del único superadmin del sistema → 409, mensaje explícito, la fila permanece, login posterior sigue funcionando igual que antes del intento — `[Requirement: Eliminación de la propia cuenta / Scenario: El último superadmin del sistema no puede eliminar su propia cuenta]`
- [x] 3.31 Test: `DELETE /account` de un superadmin cuando hay al menos otro más → 200/204, el otro superadmin sigue existiendo y autenticándose con normalidad — `[Requirement: Eliminación de la propia cuenta / Scenario: Un superadmin que no es el único puede eliminar su propia cuenta]`
- [x] 3.32 Test: `DELETE /account` sin cookie `session` válida → 401, ninguna fila se elimina — `[Requirement: Eliminación de la propia cuenta / Scenario: Usuario no autenticado recibe 401 al intentar eliminar una cuenta]`

**Notas de implementación (desviaciones/hallazgos, ver detalle en el reporte de la fase):**
- `get_current_user()` integra el rechazo de `pending_2fa` DENTRO de la misma función (no como una `Depends()` paralela) — decodifica el payload crudo vía `decode_token_payload()` antes de llamar a `decode_access_token()`.
- Bug real encontrado y corregido: `POST /auth/login` con `totp_enabled=true` perdía la cookie `pending_2fa_session` porque se llamaba `response.set_cookie(...)` sobre el `Response` inyectado por FastAPI pero la función retornaba un `JSONResponse` nuevo (que descarta esas cabeceras). Fix: la cookie se setea directamente sobre el `JSONResponse` retornado.
- Gap real encontrado y corregido: `UserInDB` no tenía `totp_enabled`, y `get_user_by_email()` no lo seleccionaba de la base — sin este campo, `POST /auth/login` no podía decidir si activar el flujo de 2 pasos. Se agregó `totp_enabled: bool = False` a `UserInDB` (`src/models/user.py`) y se extendió el `SELECT` de `get_user_by_email()`.
- Se agregó `AuthService.get_user_by_id(user_id)` (no estaba en el contrato de Phase 2/design.md): `POST /auth/2fa/login-verify` solo tiene el `user_id` (claim `sub` del pre-auth token, sin email/role/name/avatar_url), y `create_access_token()` necesita el objeto completo — mismo shape/columnas que `get_user_by_email()`, buscando por `id`.
- Tests de no-regresión pre-existentes (`test_auth_api.py`, 2 casos) necesitaron `fake_user_in_db.totp_enabled = False` explícito — un `MagicMock()` sin ese atributo seteado es truthy por default y disparaba erróneamente la rama de 2FA.

## Phase 4: Frontend

- [x] 4.1 En `dashboard/lib/types.ts`, agregar `UserProfile`, `UserProfileUpdate`, `TotpSetupResponse`, `AccountExport` (mismo shape que los modelos Pydantic de Phase 1); extender el tipo de retorno de `login()` para contemplar la respuesta `{requires_2fa: true}` como caso válido (no error)
- [x] 4.2 En `dashboard/lib/auth.ts`, agregar funciones cliente: `getProfile()`, `updateProfile()`, `setupTotp()`, `verifyTotpSetup()`, `disableTotp()`, `exportData()`, `deleteAccount()`, `verifyTotpLogin()` — todas con `credentials: 'include'` (mismo patrón cross-origin ya usado por el resto de `auth.ts`). Modificar `login()` para manejar `{requires_2fa: true}` como respuesta exitosa distinta de un login completo, sin tratarla como excepción/error
- [x] 4.3 En `dashboard/hooks/use-auth.tsx`, agregar estado `pendingTwoFactor: boolean` (y el `email`/contexto mínimo necesario para completar `verifyTotpLogin()`) que se activa cuando `login()` devuelve `requires_2fa: true`; implementar `deleteAccount()` en el hook que limpia el `user` local y redirige a login, mismo patrón que `logout()` ya existente
- [x] 4.4 Modificar `dashboard/app/login/page.tsx`: tras `login()`, si la respuesta es `{requires_2fa: true}`, mostrar un segundo paso de UI (input de código TOTP o backup code) en vez de redirigir al dashboard; al enviar ese código, llamar `verifyTotpLogin()`; en éxito, completar el flujo de login normal (redirect); en error, mostrar mensaje sin filtrar si el código era TOTP o backup code (mismo criterio de no distinguir causa de fallo que ya aplica el backend)
- [x] 4.5 Agregar `qrcode.react` (o la librería JS equivalente elegida — resolver la Open Question de `design.md` sobre qué librería concreta usar) como dependencia npm en `dashboard/package.json`, documentando en un comentario/commit la elección concreta (mismo criterio de "decisión cerrada, no bloqueante" del design) — Decisión: `qrcode.react@4.2.0` (peerDep `react ^19.0.0`, compatible con React 19 ya usado); renderiza el QR como SVG client-side desde `otpauth_uri`
- [x] 4.6 Crear la ruta `dashboard/app/settings/` con sección "Perfil": formulario para `full_name`/`address`/`phone`, consumiendo `getProfile()`/`updateProfile()` de 4.2, reutilizando componentes shadcn/ui ya presentes en `dashboard/components/ui/`. Confirmar explícitamente en el formulario que NO hay ningún campo/label que muestre o edite `name` (el campo de Google, read-only, ya existente) — los dos campos (`full_name` editable vs `name` de Google) deben aparecer visualmente distinguidos si ambos se muestran, o `name` simplemente no debe aparecer en este formulario en absoluto — Implementado en `dashboard/app/(app)/settings/page.tsx` (route group `(app)`, misma URL pública `/settings`, hereda sidebar/header) + `dashboard/components/settings/ProfileSection.tsx`; `name` de Google se muestra solo como texto informativo read-only separado, nunca como campo editable
- [x] 4.7 En la misma ruta `dashboard/app/settings/`, crear la sección "2FA": botón "Activar 2FA" que llama `setupTotp()`, renderiza el QR client-side con la librería de 4.5 a partir de `otpauth_uri`, muestra input de código de verificación (llama `verifyTotpSetup()`), y tras éxito muestra la pantalla de backup codes con aviso explícito "guardalos ahora, no se van a volver a mostrar" (texto exacto o equivalente, ver Decisión Cerrada #2 del proposal); incluir también el botón "Desactivar 2FA" (llama `disableTotp()`) visible solo si `totp_enabled=true` — Implementado en `dashboard/components/settings/TwoFactorSection.tsx`. Nota: ningún modelo liviano expone `totp_enabled` al frontend (Decisión Cerrada #4); se deriva el estado inicial de `security.totp_enabled` en `GET /account/export` (único punto del contrato de Phase 1-3 que lo expone), ver riesgo en el reporte final
- [x] 4.8 En la misma ruta, crear la sección "Exportar datos": botón que llama `exportData()` y dispara la descarga del JSON resultante como archivo (`.json` descargable en el navegador, sin passwordHash/totpSecret visibles, ya garantizado por el backend) — Implementado en `dashboard/components/settings/ExportDataSection.tsx`
- [x] 4.9 En la misma ruta, crear la sección "Zona de riesgo": botón de eliminar cuenta con diálogo de confirmación destructivo (ej. requerir escribir "ELIMINAR" o equivalente antes de habilitar el botón de confirmar, mismo criterio de mitigación de Risk "Hard-delete irreversible" del proposal); en confirmación, llama `deleteAccount()` de 4.3 y redirige a login — Implementado en `dashboard/components/settings/DangerZoneSection.tsx` usando `AlertDialog` (agregado vía `shadcn add`)
- [x] 4.10 Correr `npx tsc --noEmit` en `dashboard/` y confirmar cero errores de tipos tras 4.1-4.9 — cero errores; adicionalmente se corrió `npx next build` (compiló exitosamente, ruta `/settings` generada en 13.8 kB) y `npm run test` (17/17 tests pre-existentes en verde, sin regresión)

## Phase 5: Testing de integración adicional y no-regresión

- [x] 5.1 Test manual/documentado: escanear el QR real generado por `POST /auth/2fa/setup` con un authenticator app real (Google Authenticator o Authy), verificar que el código de 6 dígitos generado por la app es aceptado por `POST /auth/2fa/verify`, y que un login posterior con ese mismo TOTP en `POST /auth/2fa/login-verify` funciona de punta a punta — documentar el resultado en esta tarea (no automatizable sin un cliente TOTP real), mismo criterio que Success Criteria del proposal
  - **Resuelto de forma programática, sin dispositivo físico**: se agregó `tests/unit/test_auth_service.py::test_full_2fa_flow_with_pyotp_generated_code_equivalent_to_real_authenticator`, contra Postgres real (perfil `storage`, puerto 5433). El test llama `enable_totp()` real, extrae el `secret` del `otpauth_uri` devuelto (el mismo dato que un authenticator real lee al escanear el QR — el QR solo codifica ese URI como imagen), genera un código con `pyotp.TOTP(secret).now()` (RFC 6238, el mismo algoritmo que corre dentro de Google Authenticator/Authy), lo verifica con `verify_totp_setup()` real, y confirma un login posterior de punta a punta con `verify_totp_or_backup_code()` real. Corrido y en verde contra el contenedor `timescaledb` local. Un escaneo con un dispositivo físico seguiría siendo recomendable como smoke test manual único antes de un release a producción, pero no es necesario para verificar la lógica del contrato.
- [x] 5.2 Test de no-regresión: `POST /auth/login` de un usuario con `totp_enabled=false` (o el campo ausente/default antes de la migración, tratado como `false`) sigue respondiendo 200 con `Set-Cookie: session` de una sola vez, sin ningún paso adicional — `[Requirement: No regresión sobre login/registro/OAuth existente / Scenario: Login por password sin 2FA sigue funcionando exactamente igual que antes de este change]`
  - Ya cubierto parcialmente por `test_password_login_unaffected_by_google_oauth_disabled` (Phase 3). Se agregó además `test_login_without_totp_still_issues_full_session_in_one_step` en `tests/integration/test_auth_api.py`, referenciando explícitamente este Requirement/Scenario de `account-settings` y confirmando ausencia de `requires_2fa`/`pending_2fa_session`.
- [x] 5.3 Test de no-regresión: el flujo completo de Google OAuth (`GET /auth/google/login` → `GET /auth/google/callback`) para un usuario con `totp_enabled=false` sigue funcionando idéntico a `google-oauth/specs/auth/spec.md`, sin ningún paso adicional introducido por este change — `[Requirement: No regresión sobre login/registro/OAuth existente / Scenario: Login y registro vía Google siguen funcionando exactamente igual que antes de este change]`
  - Se agregó `test_google_oauth_full_flow_unaffected_by_account_settings_when_totp_disabled` en `tests/integration/test_auth_api.py`: ejercita `GET /auth/google/login` → `GET /auth/google/callback` con `totp_enabled=False` explícito, confirma cookie `session` completa, ausencia de `pending_2fa_session`, y que `create_access_token()` se invoca sin `pending_2fa=True`.
- [x] 5.4 Test de no-regresión explícito: confirmar que `POST /auth/register` y `GET /auth/me` (para usuarios sin 2FA) no cambiaron de shape/comportamiento respecto a `multi-user-auth`/`google-oauth` (reutilizar/extender los tests de no-regresión ya existentes de esos changes si aplica, no duplicar)
  - `POST /auth/register` ya cubierto por `test_password_register_unaffected_by_google_oauth_disabled` (Phase 3/google-oauth, no duplicado). Se agregó `test_me_without_2fa_ever_configured_returns_current_user_normally` en `tests/integration/test_auth_api.py` para cerrar el hueco de `/auth/me` como no-regresión explícita (antes solo se testeaba el aislamiento de perfil extendido en 3.25, no la no-regresión de shape pura).
- [x] 5.5 Correr la suite completa de tests backend (`./venv/bin/pytest tests/ -q` desde la raíz del proyecto) y confirmar cero regresiones sobre el estado conocido previo (126 passed, 1 failed preexistente no relacionado `test_ms_to_iso`, 7 errors preexistentes no relacionados por socket de Docker en macOS) — documentar el conteo final exacto (esperado: 126 + N tests nuevos de este change, todos en verde, mismo 1 failed y mismos 7 errors preexistentes sin cambios)
  - **Resultado real**: `196 collected → 195 passed, 1 failed`. El único failed es el mismo `test_ms_to_iso` preexistente y no relacionado (falla por tener una fecha hardcodeada en el assert que ya no coincide con el resultado calculado a partir de la fecha real del sistema — misma causa raíz preexistente, solo cambió el mensaje de assertion porque el tiempo avanzó). Los "7 errors preexistentes por socket de Docker en macOS" NO aparecieron en esta corrida porque el contenedor `timescaledb` está corriendo en este entorno — dejaron de ser errors y pasaron a ejecutarse (y pasar) normalmente; no representa ninguna regresión, es una mejora de entorno. Cero tests nuevos en rojo.
- [x] 5.6 Revisar cobertura cruzada contra los 13 Requirements/40 escenarios completos de `specs/account-settings/spec.md` y confirmar que cada uno tiene al menos una tarea de Phase 2, 3 o 5 que lo referencia explícitamente — si se detecta algún escenario sin cobertura, agregar la tarea faltante antes de cerrar esta fase (tarea de auditoría, no de código)
  - Auditoría realizada por grep de `[Requirement: ...]` contra los 12 headers `### Requirement:` de `spec.md` (el spec tiene 12 Requirements, no 13 — corrección de conteo respecto al enunciado de esta tarea, ver Risks del reporte). Los 12 están referenciados explícitamente en al menos un test de Phase 2/3/5. No se detectó ningún escenario sin cobertura; no hizo falta agregar tareas nuevas.

## Phase 6: Infra/Docker

- [x] 6.1 Confirmar por inspección que 2FA/`pyotp` NO requiere ninguna variable de entorno nueva — todo el mecanismo de firma (JWT de pre-auth incluido) deriva de `AUTH_SECRET_KEY` ya existente y fail-fast; documentar esta conclusión explícitamente en un comentario o en el propio `tasks.md` (esta tarea), sin agregar entradas nuevas a `.env.example`/`docker-compose.yml` para este punto
  - **Confirmado, no aplica**: `create_access_token(user, pending_2fa=True)` (Phase 2, 2.9) y `get_current_user()` (Phase 3, 3.1) reutilizan exactamente `AuthService._secret_key`/HS256 ya cargados desde `AUTH_SECRET_KEY` en `src/config/settings.py` — ningún código de 2FA lee una env var propia. Revisados `.env.example` (raíz) y `dashboard/.env.example`: ninguno necesita una entrada nueva para 2FA. No se tocó ningún archivo.
- [x] 6.2 Confirmar que no hace falta ninguna variable de entorno nueva para perfil extendido, exportación de datos, ni borrado de cuenta (son CRUD directo sobre columnas ya cubiertas por la conexión a Postgres ya configurada) — documentar explícitamente como "no aplica", mismo criterio que 6.1
  - **Confirmado, no aplica**: `get_profile()`, `update_profile()`, `export_user_data()`, `delete_account()` (Phase 2) usan el mismo `self._pool` (`asyncpg.Pool`) que ya se conecta con `TIMESCALEDB_HOST/PORT/DB/USER/PASSWORD` ya declaradas en `.env.example`/`docker-compose.yml` desde `multi-user-auth`. Ninguna de las 5 columnas nuevas (`full_name`, `address`, `phone`, `totp_secret`, `totp_enabled`) ni `user_backup_codes` requiere configuración adicional — son columnas/tabla dentro del mismo esquema `users` ya alcanzado por esa conexión. No se tocó ningún archivo.
- [x] 6.3 Verificar `deploy/k8s/secret.yaml`, `deploy/k8s/configmap.yaml` y `deploy/k8s/README.md`: dado que no se agregan variables de entorno nuevas (6.1/6.2), confirmar que no requieren ningún cambio; si el `README.md` ya tiene una sección de "pendiente — fuera de alcance" de `google-oauth`, evaluar si corresponde una mención breve de que `account-settings` tampoco introduce variables de Kubernetes nuevas (opcional, solo si aporta claridad)
  - **Confirmado, no aplica**: `deploy/k8s/secret.yaml` solo tiene `TIMESCALEDB_USER`/`TIMESCALEDB_PASSWORD`/`SENTRY_DSN`; `deploy/k8s/configmap.yaml` no tiene ninguna var de `AUTH_*`. `deploy/k8s/README.md` (sección "Auth / Google OAuth (pendiente — fuera de alcance)", líneas 60-76) ya documenta que ni `multi-user-auth` ni `google-oauth` definieron alcance de K8s y que `AUTH_SECRET_KEY` no está en los manifests — `account-settings` depende enteramente de esa misma `AUTH_SECRET_KEY` (2FA) y de `TIMESCALEDB_*` (ya presente en `secret.yaml`), así que hereda el mismo estado "pendiente" sin agregar ninguna variable nueva propia. No se editó el README: la sección existente ya cubre correctamente el caso (mencionar aparte "account-settings tampoco agrega nada" sería redundante, no agrega claridad real ya que el texto actual habla de "Auth" en general, del cual este change es una extensión).
- [x] 6.4 Confirmar que `docker-compose.yml` no requiere ningún puerto/servicio nuevo (2FA, export y delete son endpoints HTTP adicionales sobre el mismo servicio `geospectrum` ya expuesto) — documentar explícitamente como "no aplica"
  - **Confirmado, no aplica**: los 7 endpoints nuevos (`/auth/2fa/*`, `/account/*`) se registran en `src/main.py` sobre la misma app FastAPI del servicio `geospectrum` (puerto 8000 ya mapeado en `docker-compose.yml`). No se agrega ningún servicio, puerto, volumen ni red nueva. No se tocó `docker-compose.yml`.

## Phase 7: Fixes post-verify (post PASS_WITH_WARNINGS de sdd-verify)

Dos fixes puntuales pedidos explícitamente antes de archivar el change, tras el veredicto `PASS_WITH_WARNINGS` de `sdd-verify` sobre las 98 tareas de Phases 1-6.

- [x] 7.1 Rate-limiting de `POST /auth/2fa/login-verify` — resuelve la Open Question de `design.md` (línea 417, ahora marcada `[x]`) y el "Riesgo residual aceptado" de Decision 1.
  - Agregado `Login2FAAttemptLimiter` en `src/services/auth_service.py` (más `MAX_TOTP_LOGIN_ATTEMPTS=5`, `TOTP_LOGIN_ATTEMPT_TTL_SECONDS`, excepción `TooManyTotpAttemptsError`) — contador de intentos fallidos por `sub` (user_id del pre-auth token) sobre Redis real (`redis==5.0.4`, ya dependencia de producción vía `RedisPubSubBus`/`event_bus`), NO in-memory: un contador de proceso no se comparte entre workers/réplicas, dejando un hueco de fuerza bruta multiplicable por N procesos.
  - `src/main.py`: cliente Redis dedicado `totp_login_attempt_redis` (separado de `event_bus`, mismo criterio best-effort en `lifespan()` — si Redis no está disponible, el endpoint degrada a "sin límite" en vez de romper el login). DI vía `_get_totp_login_attempt_limiter()` (mismo patrón que `_get_auth_service()`). `POST /auth/2fa/login-verify` llama `check_not_locked()` ANTES de evaluar el código (bloquea incluso un código correcto tras el límite) y `register_failure()` en cada 401 por código inválido; `POST /auth/login` llama `reset()` al emitir un pre-auth nuevo (nuevo login = presupuesto de intentos limpio, sin depender solo del TTL).
  - Tests: `tests/unit/test_auth_service.py` (5 tests nuevos con un fake Redis in-memory — intentos bajo el límite no bloquean, se bloquea tras el límite incluso con código correcto, `reset()` desbloquea, contadores por usuario independientes) + `tests/integration/test_auth_api.py` (2 tests end-to-end contra Redis real de testcontainers vía `httpx.AsyncClient`/`ASGITransport`, no `TestClient` síncrono — ver nota en el propio archivo sobre por qué: `TestClient` sin `with` corre cada request en un event loop nuevo, rompiendo una conexión Redis persistente con "Future attached to a different loop").
  - Fixture nuevo `_default_totp_login_attempt_limiter` (autouse) en `tests/integration/test_auth_api.py`: setea un fake permisivo por defecto en `app.state.totp_login_attempt_limiter` para no romper los ~40 tests preexistentes del archivo que no ejercitan rate-limiting (`TestClient(app)` se usa sin `with`, lifespan nunca corre, así que ese `app.state` no se puebla solo).
- [x] 7.2 `AuthService.export_user_data()` no propagaba `totp_enabled` a `AccountExport.profile` (quedaba en el default `False` de `UserProfile` aunque el usuario tuviera 2FA activo) — inconsistencia de datos pura respecto de `security["totp_enabled"]`, que ya usaba el valor real de la misma fila.
  - `src/services/auth_service.py`: `export_user_data()` ahora pasa `totp_enabled=row["totp_enabled"]` al construir `UserProfile` dentro de `AccountExport.profile`, igual que ya hacían `get_profile()`/`update_profile()`.
  - Test actualizado: `test_export_user_data_never_includes_password_hash_or_totp_secret_or_code_hash` (`tests/unit/test_auth_service.py`) ahora además confirma `export.profile.totp_enabled is True` y `export.profile.totp_enabled == export.security["totp_enabled"]` para un usuario con 2FA activo.
  - No afecta Decisión Cerrada #4 (aislamiento `full_name`/`UserProfile` respecto de `CurrentUser`/`UserPublic`/JWT): el campo tocado (`totp_enabled`) ya vivía en `UserProfile` desde el fix puntual post-Phase 4 documentado ahí; este fix solo corrige que el valor real se propague, no agrega superficie nueva.
- [x] 7.3 Verificación: `./venv/bin/python -m pytest tests/ -q -o addopts=""` → 201 passed, 1 failed (`test_ms_to_iso`, preexistente y no relacionado — mismo failure que el baseline de 195 passed/1 failed conocido al momento del veredicto `sdd-verify`). Cero regresiones nuevas.
