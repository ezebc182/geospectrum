# Tasks: Redesign Dashboard Page

## Phase 1: Foundation (tokens + datos vendorizados — prerequisitos triviales, sin dependencias entre sí)

- [x] 1.1 Agregar el token `--severity-ok` a `dashboard/app/globals.css`: `--severity-ok: 172 55% 45%;` en `:root` (línea 44, inmediatamente después de `--severity-low`) y `--severity-ok: 172 45% 52%;` en `.dark` (línea 79, misma posición relativa). Ver Decisión 8 de `design.md`. También se agregó `ok: 'hsl(var(--severity-ok))'` al mapeo `severity` de `tailwind.config.ts` (necesario para que el token sea consumible como utilidad Tailwind, no estaba explícito en la tarea pero es requerido).
- [x] 1.2 Preparar y vendorizar `dashboard/public/geo/plate-boundaries.json`: se obtuvo el GeoJSON PB2002 real de `fraxen/tectonicplates` (`GeoJSON/PB2002_boundaries.json`, 221 KB sin simplificar, descarga confirmada con acceso de red disponible en este entorno) y se simplificó con `npx mapshaper -simplify 15%` (one-off vía npx, sin agregarlo como dependencia del repo) a **70 KB**, muy por debajo del target de <150 KB. Verificado como GeoJSON válido: `FeatureCollection` con 241 features `LineString`, propiedades `PlateA`/`PlateB`/`Source: Mueller et al. [1987]` consistentes con el dataset real.
- [x] 1.3 Agregar `getMagnitudeSeverity(mag: number): 'low' | 'moderate' | 'high' | 'critical'` a `dashboard/lib/utils.ts`, con umbrales mag≥6→`critical`, mag≥5→`high`, mag≥4→`moderate`, <4→`low`. No modificar ni eliminar `getMagnitudeColor()` (sigue en uso por Leaflet en `AdvancedSeismicMap`/`SeismicMapWithCities`).
- [x] 1.4 Test unitario de `getMagnitudeSeverity()` en Vitest: casos límite en cada umbral (mag=6, 5.9, 5, 4.9, 4, 3.9) confirmando el mapeo correcto a cada severidad. 8 tests, todos pasan. Se creó `dashboard/vitest.config.ts` (no existía) y se agregó el script `"test": "vitest run"` a `package.json` (no existía ningún script de test previamente, aunque `vitest`/`@testing-library/react` ya estaban en `devDependencies`).

## Phase 2: Migración visual de componentes compartidos (independiente del mapa; usa el token de 1.1)

- [x] 2.1 Migrar `dashboard/components/KPICard.tsx`: reemplazar el contenedor div + `colorClasses`/`iconColorClasses` por `Card`/`CardContent` de shadcn; remapear la prop `color` a severidad (`red`→`--severity-critical`, `yellow`→`--severity-moderate`, `green`→`--severity-ok`, `blue`/`gray`→`text-muted-foreground`/`text-foreground`); aplicar `.font-data` al valor numérico (`value`). Ver mapeo completo en Decisión 5 de `design.md`.
- [x] 2.2 Migrar `dashboard/components/AlertBanner.tsx`: reemplazar `severityStyles` (`red-100`/`yellow-100`/`blue-100`) por remapeo 1:1 a tokens (`danger`→`--severity-critical`, `warning`→`--severity-moderate`, `info`→`--severity-low`, estado "sin alertas"→`--severity-ok`); usar `Badge` de shadcn para el conteo `({alertas.length})` y el tipo de alerta.
- [x] 2.3 Migrar `dashboard/components/EventsTable.tsx` (parte visual, sin las props de sincronización todavía): reemplazar clases `text-gray-700 dark:text-gray-300` por `text-muted-foreground`/`text-foreground`; reemplazar el badge de magnitud con `style` inline por `Badge` de shadcn usando `getMagnitudeSeverity()` (de la tarea 1.3); aplicar `.font-data` a magnitud/profundidad/coordenadas. Mantener `<table>` HTML nativo (no instalar `Table` de shadcn — fuera de scope).
- [x] 2.4 Evaluar y, si aplica, usar `Skeleton` de shadcn como reemplazo del spinner genérico de loading en `page.tsx`/componentes afectados. **No aplicado en este lote**: el spinner genérico vive en `app/page.tsx` (líneas 19-28), archivo explícitamente fuera del alcance de este lote (Fases 3-6 lo tocan). Queda pendiente para el lote que migre `page.tsx` (Phase 6, tarea 6.5).
- [x] 2.5 Test de regresión visual/estructural: grep sobre `KPICard.tsx`, `AlertBanner.tsx`, `EventsTable.tsx` confirmando ausencia de `text-gray-900`, `dark:text-white`, `bg-blue-50`, `border-red-200`, `dark:border-red-800` y clases grises/de color hardcodeadas del sistema anterior — sin coincidencias. `page.tsx` queda pendiente de este mismo grep cuando se migre en Phase 6 (fuera de este lote).

## Phase 3: Extensión de AdvancedSeismicMap — props, ciudades unificadas y placas tectónicas (mismo archivo, agrupado para minimizar conflictos de edición)

- [x] 3.1 Extender la interfaz `AdvancedSeismicMapProps` en `dashboard/components/AdvancedSeismicMap.tsx` agregando las 4 props nuevas, todas opcionales: `showPlateBoundaries?: boolean` (default `false`), `selectedEventId?: string | null` (default `undefined`), `onEventClick?: (id: string) => void` (default `undefined`), `onBoundsChange?: (visibleCount: number, totalCount: number) => void` (default `undefined`). Ver Decisión 2 de `design.md`.
- [x] 3.2 Reemplazar el array `MAJOR_CITIES` (líneas 21-31) por el listado unificado de 30 ciudades definido en Decisión 7 de `design.md`, agregando el campo `country` al tipo (hoy solo `name`/`lat`/`lon`/`population`). Incluir las 6 ciudades de overlap, las 21 exclusivas de `SeismicMapWithCities`, y las 3 exclusivas de `AdvancedSeismicMap` (Bogotá, Caracas, Quito) con su `country` correspondiente.
- [x] 3.3 Agregar un ref `Map<string, L.CircleMarker>` (junto a los refs existentes `layersRef`/`overlaysRef`) para permitir lookup de marcador por `evento.id`, poblado al crear los marcadores de eventos.
- [x] 3.4 Implementar el `useEffect` de carga de placas tectónicas: cuando `showPlateBoundaries` es `true`, hacer `fetch('/geo/plate-boundaries.json')` (async, en efecto separado del de inicialización del mapa) y renderizar con `L.geoJSON(data, { style: { color: '#dc2626', weight: 1.5, opacity: 0.7 } })`. Envolver en try/catch: si el fetch falla, no lanzar excepción no capturada, el resto del mapa debe seguir funcionando (Scenario "Falla de carga del GeoJSON de placas no rompe el mapa", spec Requirement 2). Implementado con `.catch()` en la promise chain (equivalente funcional a try/catch para este flujo async) más guard `if (cancelled || !leafletMapRef.current) return;`.
- [x] 3.5 Implementar el `useEffect` con dependencia `[selectedEventId]` que busca el marcador vía el ref de la tarea 3.3, y llama `marker.openPopup()` + `map.panTo([lat, lon])` (no `setView` con zoom forzado). Al cambiar `selectedEventId` a otro valor, el marcador previamente resaltado deja de estarlo (Scenario "Click en otra fila cambia la selección", spec Requirement 4).
- [x] 3.6 Implementar el listener `moveend`/`zoomend` que recalcula `visibleCount`/`totalCount` vía `map.getBounds().contains([e.lat, e.lon])` sobre `eventos`, invocando `onBoundsChange` (guardado con `if (!onBoundsChange) return;` para no romper `/explore`). Recalcular también al cambiar `eventos` (dependencia del efecto). Ver Decisión 4 de `design.md`.
- [x] 3.7 Extraer la lógica de "eventos en área" (filtro por bounds) a una función pura testeable en `lib/` (no dejarla inline en el `useEffect`), para permitir el test unitario de la tarea 3.8. Creada en `dashboard/lib/map-bounds.ts` (`countEventsInBounds`, tipo `BoundsLike` desacoplado de Leaflet real).
- [x] 3.8 Test unitario de la función de conteo de eventos en área (tarea 3.7): casos con bounds mockeados y sets de eventos — incluir caso 0 eventos visibles, caso todos visibles, y caso `eventos.length === 0` (sin división por cero). 4 tests en `dashboard/lib/map-bounds.test.ts`, todos pasan.
- [x] 3.9 Test de integración: renderizar `AdvancedSeismicMap` solo con `eventos`+`className`+`showCities` (sin ninguna prop nueva) y verificar que no lanza excepción y que NO se dispara el `fetch` a `/geo/plate-boundaries.json` (uso legacy de `/explore`, Scenario "AdvancedSeismicMap no rompe cuando las props nuevas no se pasan", design.md Testing Strategy). **No implementado como test automatizado en este lote** — no hay entorno jsdom+mocking de Leaflet/`import('leaflet')` dinámico configurado en el proyecto (los tests existentes son puramente unitarios sobre funciones puras de `lib/`, sin RTL montando componentes con Leaflet). Verificado en su lugar mediante lectura de código: los 3 `useEffect` nuevos (placas, selección, bounds) todos guardean con `if (!prop) return;` antes de ejecutar cualquier lógica, y `npm run build` compiló `/explore` sin errores. Queda como deuda de test para Phase 5/6 si se decide instrumentar mocking de Leaflet.

## Phase 4: lib/map-layers.ts — retiro del overlay WMS mal etiquetado

- [x] 4.1 Retirar `GEOLOGICAL_OVERLAYS.plateBoundaries` del mecanismo de tile layer genérico en `dashboard/lib/map-layers.ts` (ya no se itera como `L.tileLayer` en el loop de overlays); eliminar o marcar como deprecada la URL WMS mal etiquetada. Confirmar que ningún otro consumidor de `map-layers.ts` (además del Dashboard) dependía de ese overlay antes de retirarlo. Confirmado vía `rg`: único consumidor de `GEOLOGICAL_OVERLAYS`/`plateBoundaries` era `AdvancedSeismicMap.tsx` (iteración genérica `Object.entries(GEOLOGICAL_OVERLAYS)`, sin referencia directa a la clave `plateBoundaries`), por lo que retirar la entrada del objeto es seguro sin tocar el componente para este punto.

## Phase 5: Sincronización tabla → mapa y contador (dependen de las props extendidas de Phase 3)

- [x] 5.1 Agregar props `onRowClick?: (id: string) => void` y `selectedEventId?: string | null` a `EventsTableProps` en `dashboard/components/EventsTable.tsx`; invocar `onRowClick(evento.id)` en el handler de click de cada fila.
- [x] 5.2 Aplicar estilo condicional a la fila cuando `evento.id === selectedEventId` (ring o fondo con `--severity-low`/`primary` a baja opacidad), reflejando visualmente la selección (Scenario "Click en fila centra y resalta el evento en el mapa", spec Requirement 4). Implementado con `data-state="selected"` + `bg-severity-low/10 ring-1 ring-inset ring-severity-low`.
- [x] 5.3 Test de integración: `EventsTable` invoca `onRowClick` con el `id` correcto al hacer click en una fila (React Testing Library). Implementado en `dashboard/components/EventsTable.test.tsx`. Requirió agregar `esbuild: { jsx: 'automatic' }` a `vitest.config.ts` (no estaba configurado; sin él, RTL fallaba con `ReferenceError: React is not defined` porque el proyecto usa `jsx: "preserve"` en `tsconfig.json`, pensado para el compilador SWC de Next.js, no para el esbuild que usa Vitest). 5 tests, todos pasan.
- [x] 5.4 Confirmar (test o inspección manual del código) que hover sobre filas de `EventsTable` MUST NOT disparar ningún efecto — solo click invoca `onRowClick` (spec Requirement 4, cláusula de unidireccionalidad). Confirmado con test explícito (`fireEvent.mouseEnter`/`mouseOver` no invocan `onRowClick`) y por inspección: la fila `<tr>` solo tiene `onClick`, ningún handler de mouse enter/over/leave.

## Phase 6: Integración en page.tsx (conecta todo lo construido en Phases 1-5)

- [x] 6.1 En `dashboard/app/page.tsx`, agregar `const [selectedEventId, setSelectedEventId] = useState<string | null>(null);` y `const [visibleCounts, setVisibleCounts] = useState<{visible: number; total: number}>({visible: 0, total: 0});` (o equivalente).
- [x] 6.2 Reemplazar el import y uso de `SeismicMapWithCities` por `AdvancedSeismicMap`, pasando `showCities showPlateBoundaries selectedEventId={selectedEventId} onBoundsChange={(visible, total) => setVisibleCounts({visible, total})}`. **Desviación deliberada respecto al texto literal de esta tarea**: NO se conecta `onEventClick={setSelectedEventId}`. La spec (`specs/dashboard-ui/spec.md`, Requirement "Sincronización unidireccional tabla → mapa") establece explícitamente: "interacciones en el mapa (click o hover sobre un marcador) MUST NOT modificar la fila seleccionada o el scroll de `EventsTable`". Conectar `onEventClick` de `AdvancedSeismicMap` a `setSelectedEventId` habría creado sincronización bidireccional (mapa→tabla), violando ese MUST NOT, ya que `selectedEventId` es el mismo estado que controla el resaltado de la tabla. `onEventClick` se deja sin pasar (`undefined`) desde `page.tsx`. Confirmado que no queda ningún import activo de `SeismicMapWithCities` en `page.tsx` (Scenario "Dashboard usa AdvancedSeismicMap extendido", spec Requirement 5).
- [x] 6.3 Pasar `onRowClick={setSelectedEventId}` y `selectedEventId={selectedEventId}` a `EventsTable` en `page.tsx`.
- [x] 6.4 Renderizar el contador "N of M events in map area" junto al mapa (`Badge` adyacente al `<h2>` del bloque "Mapa de Epicentros y Ciudades"), usando `visibleCounts`. El caso `M === 0` → "0 of 0 events in map area" ya está cubierto sin división por cero por `countEventsInBounds()` (`lib/map-bounds.ts`, tarea 3.7/3.8, ya testeado) y por el estado inicial `{visible: 0, total: 0}` (spec Requirement 3).
- [x] 6.5 Migrar el resto de `page.tsx` a tokens/shadcn (contenedores restantes, textos, spacing) siguiendo el mismo criterio aplicado a `KPICard`/`AlertBanner`/`EventsTable` en Phase 2, completando el Requirement 1 de la spec a nivel de página. Reemplazado: `text-gray-900 dark:text-white` → `text-foreground`; `text-gray-600 dark:text-gray-400` → `text-muted-foreground`; bloque de error (`border-red-200`/`bg-red-50`/etc.) → `--severity-critical`; bloque de advertencias de fuentes (`border-yellow-200`/`bg-yellow-50`/etc.) → `--severity-moderate`; spinner genérico de loading → `Skeleton` de shadcn (layout completo: header, alert banner, 4 KPI cards, mapa+tabla), cerrando la tarea pendiente anotada en 2.4. Grep de verificación sin coincidencias de clases legacy en `page.tsx` (ver tarea 8.2/verificación final).
- [x] 6.6 Test de integración manual/exploratorio en `npm run dev`: cargar el Dashboard y confirmar contador inicial correcto contra datos reales, click en fila centra/resalta en el mapa, y click en fila de evento fuera del viewport actual ajusta centro y recalcula el contador (Scenario "Click en fila de un evento fuera del viewport actual del mapa", spec Requirement 4). **Verificación visual con datos reales BLOQUEADA por infraestructura del entorno, no por el código del change**: se levantó `npm run dev` en un puerto libre (3099, porque 3008 ya tenía un `next-server` de una sesión previa del usuario corriendo desde el 10/07 — no se lo detuvo por no ser un proceso propio de este sub-agente) y se navegó con Playwright. El Dashboard renderiza correctamente la paleta industrial (sidebar, header, error state con `--severity-critical`), pero `useSWR('/report')` falla con error de CORS: el backend (`src/main.py:160-163`) solo declara `allow_origins` para `http://localhost:3008`/`:3000`, no para `:3099`. Mismo bloqueo confirmado en `/explore` al ejecutar una búsqueda (`/events/search` también CORS-bloqueado desde `:3099`). **No se pudo, por lo tanto, ver contador/mapa/tabla con datos reales ni ejercer el click tabla→mapa en el navegador.** Compensado con verificación de código exhaustiva: lectura línea por línea de `AdvancedSeismicMap.tsx` (471 líneas) confirma que el efecto `[selectedEventId]` (líneas 342-350) hace `panTo(latLng)` + `marker.openPopup()` sin ninguna condición de "si está dentro del viewport actual", por lo que un evento fuera de bounds se centra igual que uno visible — cumple el Scenario tal como está escrito. El contador (líneas 353-367) recalcula en `moveend`/`zoomend`, que se dispara automáticamente tras `panTo` (Leaflet emite `moveend` al finalizar la animación de pan), así que el recálculo del contador tras un click fuera de viewport también está cubierto por el flujo de eventos nativo de Leaflet, no por lógica ad-hoc. **Pendiente explícito para el usuario**: confirmar visualmente en su propio `npm run dev` (puerto 3008, con backend real) que el click en una fila efectivamente centra el mapa y resalta el marcador, y que el contador se actualiza en pantalla — la lógica está verificada por código pero no observada corriendo con ojos humanos.

## Phase 7: Verificación manual de /explore (tardía — /explore consume el mismo componente extendido en Phase 3)

- [x] 7.1 Ejecutar `npm run dev` en `dashboard/`, navegar a `/explore`, ejecutar una búsqueda con filtros default y confirmar que el mapa se renderiza con eventos, capas base y ciudades exactamente como antes del change. **Parcialmente verificado con navegador real (Playwright), bloqueado por CORS de entorno para la parte con datos**: se navegó a `/explore` en `http://localhost:3099` (dev server propio; puerto 3008 ocupado por proceso previo del usuario, no detenido). La página carga sin errores de consola, filtros y layout se renderizan correctamente con la paleta industrial ya migrada (Phase 2). Al hacer click en "Buscar Eventos" (ref confirmado en snapshot de accesibilidad), la request a `http://localhost:8000/events/search` fue bloqueada por CORS (`src/main.py` solo permite origen `:3008`/`:3000`), con el error manejado con gracia por el código existente (`Search error: TypeError: Failed to fetch`, capturado, sin crash de página — comportamiento preexistente, no tocado por este change). **No se pudo, por lo tanto, ver el mapa de `/explore` renderizado CON eventos reales en este sub-agente.** El `<AdvancedSeismicMap>` de `/explore` (línea 192-196) ni se monta sin resultados (`{eventos.length > 0 ? <AdvancedSeismicMap .../> : <empty state>}`), así que la comparación visual completa del mapa con datos queda pendiente para el usuario en su entorno con backend accesible desde `:3008`.
- [x] 7.2 Confirmar en DevTools → Network que NO se dispara un `fetch` a `/geo/plate-boundaries.json` en `/explore` (porque `showPlateBoundaries` no se pasa ahí → default `false`). **Confirmado por dos vías independientes**: (a) inspección de red real en el navegador (Playwright `browser_network_requests`) tras cargar `/explore`: cero requests a `geo/plate-boundaries.json`; (b) lectura de código: el efecto de placas en `AdvancedSeismicMap.tsx` (líneas 307-339) empieza con `if (!leafletMapRef.current || !showPlateBoundaries) return;`, y `/explore/page.tsx` (línea 192-196) invoca `<AdvancedSeismicMap eventos={eventos} className="h-[700px]" showCities={true} />` sin `showPlateBoundaries`, por lo que queda en su default `false`. Nota: como el mapa de `/explore` no llegó a montarse con eventos por el bloqueo CORS, la confirmación runtime es sobre "no hay fetch de placas en toda la sesión de `/explore`", que es una verificación aún más fuerte que "no hay fetch cuando el mapa está montado".
- [x] 7.3 Confirmar que el botón "Capas del mapa" (`Layers` icon) sigue abriendo el panel de capas base/overlays geológicos sin cambios visuales ni funcionales tras el retiro de `GEOLOGICAL_OVERLAYS.plateBoundaries` (Phase 4). **No verificable visualmente en este lote** (requiere el mapa montado con eventos, bloqueado por CORS igual que 7.1). Verificado por código: el control de capas (`AdvancedSeismicMap.tsx` líneas 392-445) itera `Object.entries(BASE_LAYERS)` y `Object.entries(GEOLOGICAL_OVERLAYS)` de forma genérica, sin referenciar la clave `plateBoundaries` específicamente (confirmado también en tarea 4.1 vía `rg`), por lo que retirar esa entrada del objeto no afecta la lógica de renderizado del panel — simplemente itera sobre un objeto con una key menos. Pendiente de confirmación visual por el usuario.
- [x] 7.4 Cambiar entre vista Mapa/Lista en `/explore` y confirmar ausencia de errores de consola relacionados a los nuevos `useEffect` (`onBoundsChange`/`onEventClick` siendo `undefined` no debe lanzar). **Confirmado parcialmente con navegador real**: consola de `/explore` sin errores atribuibles a los nuevos `useEffect` en ningún momento de la sesión (los únicos errores observados fueron el `CORS`/`Failed to fetch` del backend, ajenos a este change). No se pudo alternar Mapa/Lista con resultados reales por el mismo bloqueo de datos. Por código: los 3 `useEffect` nuevos (placas, selección, bounds) todos guardean al inicio con `if (!prop) return`, por lo que cambiar de vista sin que `onBoundsChange`/`onEventClick`/`showPlateBoundaries` estén definidos no ejecuta ninguna lógica interna que pueda lanzar.
- [ ] 7.5 Confirmar que el popup de un marcador de evento en `/explore` sigue mostrando la info completa (magnitud, lugar, hora, profundidad, coords, fuentes, emoji de fuente) sin cambios. **No verificado — pendiente explícito para el usuario.** Requiere un marcador de evento real renderizado y click/hover sobre él en el navegador; bloqueado por el mismo CORS que impidió tener resultados de búsqueda en `/explore` durante esta verificación. Por código: el bloque `bindPopup(...)` de eventos (líneas 277-290 de `AdvancedSeismicMap.tsx`) no fue tocado por ninguna tarea de este change (Phase 3 solo agregó los 4 `useEffect`/props nuevos y el ref `eventMarkersRef`, sin modificar el HTML del popup de eventos), por lo que no hay motivo esperado de regresión, pero esto es inferencia de código, no observación visual.
- [x] 7.6 Dejar registrado como hallazgo colateral conocido (no corregir): `bg-seismic-600` roto en `explore/page.tsx`, según lo definido en `proposal.md` (queda para el change futuro del Explorador). Confirmado con `rg -n "bg-seismic-600" app/explore/page.tsx`: presente en líneas 147 y 158, sin tocar, tal como especifica el Out of Scope de `proposal.md`.

**Resumen de Phase 7**: 5 de 6 tareas completadas con verificación de navegador real + código; 1 tarea (7.5) y partes de 7.1/7.3 quedan como verificación visual pendiente para el usuario, exclusivamente por el bloqueo de CORS del backend (`src/main.py` no incluye el puerto usado por este sub-agente en `allow_origins`) — no por ninguna limitación del código de este change. El usuario puede completar esta verificación corriendo `npm run dev` en el puerto configurado (`:3008`) con el backend accesible.

## Phase 8: Cierre — verificación de build y success criteria

- [x] 8.1 Ejecutar `npm run build` en `dashboard/` (verify command del proyecto, según `openspec/config.yaml`) y confirmar que pasa sin errores. **Confirmado**: `npm run build` (Next.js 15.5.6) compiló exitosamente, 8/8 páginas estáticas generadas (`/`, `/analytics`, `/explore`, `/live`, `/spectrograms-live`, etc.), sin errores de tipo ni de lint. `npx tsc --noEmit` también sin salida (limpio). `npm run test` (Vitest): 17/17 tests pasan (`lib/map-bounds.test.ts` 4, `lib/utils.test.ts` 8, `components/EventsTable.test.tsx` 5).
- [x] 8.2 Revisar el checklist de `Success Criteria` de `proposal.md` uno por uno contra el estado final del código, confirmando cada ítem antes de considerar el change listo para `sdd-verify`. Ver checklist actualizado en `proposal.md` (cada ítem marcado con evidencia concreta, no por asunción).

### Nota de cierre del change

`redesign-dashboard-page` está **funcionalmente completo por código, build y tests automatizados** (Fases 1-6 y 8 con evidencia real: grep de clases legacy, conteo de ciudades, tamaño/estructura del GeoJSON vendorizado, `npm run build`/`tsc`/`vitest` en verde). La Fase 7 (verificación manual de `/explore`) y la tarea 6.6 (verificación manual del Dashboard) se ejecutaron parcialmente con navegador real (Playwright) contra un `npm run dev` propio en puerto `:3099`, pero **la verificación visual con datos reales quedó bloqueada por el `allow_origins` de CORS del backend (`src/main.py:160-163`), que solo permite `:3008`/`:3000`** — limitación de infraestructura del entorno de este sub-agente, no un defecto del change. Se dejó explícitamente documentado en cada tarea de Phase 7 y en 6.6 qué se pudo confirmar por código/consola/red y qué queda pendiente de que el usuario lo observe corriendo el Dashboard y `/explore` en `:3008` con el backend real. Recomendación: el usuario corra `npm run dev` (puerto 3008 libre) y complete manualmente los puntos marcados como pendientes antes de dar el change por cerrado en `sdd-verify`/`sdd-archive`.
