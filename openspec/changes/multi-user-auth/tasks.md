# Tasks: Autenticación Multi-Usuario con Roles

Convención de trazabilidad: cada tarea de testing referencia el escenario exacto de `specs/auth/spec.md` que cubre, usando el formato `[Requirement / Scenario]`.

## Phase 1: Foundation (modelo de datos, config, dependencias)

- [x] 1.1 Crear `deploy/sql/migrations/001_create_users_table.sql` con el DDL de `design.md` (`CREATE TABLE IF NOT EXISTS users (id UUID PRIMARY KEY DEFAULT gen_random_uuid(), email TEXT NOT NULL UNIQUE, password_hash TEXT NOT NULL, role TEXT NOT NULL CHECK (role IN ('admin','viewer')), created_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_at TIMESTAMPTZ NOT NULL DEFAULT now())` + `CREATE INDEX IF NOT EXISTS idx_users_email ON users (email)`); documentar en un comentario al pie el rollback `DROP TABLE IF EXISTS users;`. No se encontró Alembic ni carpeta de migraciones previa en el proyecto — este archivo establece la convención (`deploy/sql/migrations/NNN_description.sql`) para el equipo
- [x] 1.2 Aplicar la migración 001 contra el Postgres/TimescaleDB del perfil `storage` en `deploy/docker/docker-compose.yml` (entorno local de desarrollo) y confirmar con `\d users` que la tabla, el constraint `CHECK` y el índice quedaron creados
- [x] 1.3 Agregar `passlib[bcrypt]==1.7.4`, `bcrypt==4.2.0` y `python-jose[cryptography]==3.3.0` a `requirements.txt`, en la sección correspondiente (junto al resto de dependencias de runtime, no en "Development")
- [x] 1.4 Agregar `testcontainers[postgres]` al extra ya existente `testcontainers[redis]==4.5.1` en `requirements.txt` (queda `testcontainers[redis,postgres]==4.5.1`) para poder levantar Postgres efímero en los tests de integración de Phase 5
- [x] 1.5 Instalar las dependencias nuevas (`pip install -r requirements.txt`) y confirmar que no hay conflictos de versión con lo ya instalado (en particular `pydantic==2.9.0`, que `python-jose` no debería tocar)
- [x] 1.6 Agregar a `src/config/settings.py` los campos `auth_secret_key: Optional[str] = None` y `auth_token_expire_minutes: int = 1440`, siguiendo exactamente el patrón `Optional[str]` ya usado para `timescaledb_password`
- [x] 1.7 Crear `src/models/user.py` con los modelos Pydantic exactos del `design.md`: `UserRole(str, Enum)` (`ADMIN = "admin"`, `VIEWER = "viewer"`), `UserCreate` (`email: EmailStr`, `password: str`, `role: UserRole = UserRole.VIEWER`), `UserPublic` (`id: UUID`, `email: EmailStr`, `role: UserRole`), `UserInDB` (incluye `password_hash: str`), `CurrentUser` (`id: UUID`, `email: EmailStr`, `role: UserRole`)
- [x] 1.8 Agregar validación de longitud mínima de password (8 caracteres) en `UserCreate` vía `Field(min_length=8)` o `field_validator`, para soportar `[Requirement: Registro de usuario / Scenario: Registro rechazado por password que no cumple la política mínima]` (422 si `len(password) < 8`)
- [x] 1.9 Confirmar que `pydantic[email]` (soporte de `EmailStr`) está disponible transitivamente o agregar `email-validator` explícito a `requirements.txt` si `pydantic==2.9.0` no lo trae por defecto

## Phase 2: Backend core (auth_service.py, deps.py)

- [x] 2.1 Crear `src/services/auth_service.py` con clase `AuthService`: constructor recibe `dsn: str`, atributo `_pool: Optional[asyncpg.Pool]`, métodos `connect()`/`close()` idempotentes — mismo patrón que `TimescaleColumnWriter` en `src/services/timescale_service.py` (`asyncpg.create_pool(dsn, min_size=1, max_size=5)`)
- [x] 2.2 En `AuthService`, implementar `hash_password(password: str) -> str` y `verify_password(password: str, password_hash: str) -> bool` usando `passlib.context.CryptContext(schemes=["bcrypt"], deprecated="auto")`
- [x] 2.3 En `AuthService`, implementar `create_user(email: str, password: str, role: UserRole) -> UserPublic`: hashea el password, ejecuta `INSERT INTO users (email, password_hash, role) VALUES ($1, $2, $3) RETURNING id, email, role` parametrizado (nunca f-strings), captura la violación de unicidad de `asyncpg.UniqueViolationError` y la relanza como una excepción de dominio propia (ej. `EmailAlreadyRegisteredError`)
- [x] 2.4 En `AuthService`, implementar `get_user_by_email(email: str) -> Optional[UserInDB]` con `SELECT id, email, password_hash, role FROM users WHERE email = $1` parametrizado
- [x] 2.5 En `AuthService`, implementar `create_access_token(user: UserInDB | UserPublic) -> str` usando `jose.jwt.encode` con claims `{"sub": str(user.id), "email": user.email, "role": user.role.value, "iat": <now>, "exp": <now + auth_token_expire_minutes>}`, firmado HS256 con `settings.auth_secret_key`
- [x] 2.6 En `AuthService`, implementar `decode_access_token(token: str) -> CurrentUser`, que levanta excepciones de dominio distintas para firma inválida vs. token expirado (`jose.exceptions.ExpiredSignatureError` vs `JWTError`), para soportar `[Requirement: Expiración de sesión / Scenario: Token expirado es rechazado]` (la causa debe ser distinguible internamente aunque el mensaje público sea genérico)
- [x] 2.7 Escribir tests unitarios en `tests/unit/test_auth_service.py`: round-trip de `hash_password`/`verify_password` (password correcto pasa, incorrecto falla); `create_access_token`/`decode_access_token` con token válido; token con firma alterada manualmente levanta la excepción esperada; token con `exp` en el pasado (usar `freezegun`, ya en `requirements.txt`) levanta la excepción de expiración distinta a la de firma inválida
- [x] 2.8 Crear `src/api/__init__.py` (el directorio `src/api/` existe pero está vacío — confirmar que no tiene `__init__.py` y crearlo si falta, para que sea un paquete importable)
- [x] 2.9 Crear `src/api/deps.py` con `get_current_user(request: Request) -> CurrentUser`: lee la cookie `session` de `request.cookies.get("session")`, si está ausente levanta `HTTPException(401, detail="not authenticated")`; si está presente, llama a `auth_service.decode_access_token()` y en caso de excepción (firma inválida o expirado) levanta `HTTPException(401)` sin dejar propagar la excepción original (nunca un 500) — cubre `[Requirement: Perfil del usuario autenticado / Scenario: Usuario no autenticado recibe 401]` y `[Scenario: Cookie con JWT corrupto o con firma inválida recibe 401]`
- [x] 2.10 En `src/api/deps.py`, implementar `require_role(role: UserRole)` como factory que retorna una dependencia: primero resuelve `get_current_user` (401 si no hay sesión), luego compara `current_user.role == role` y levanta `HTTPException(403, detail="insufficient role")` si no coincide — cubre los 3 escenarios de `[Requirement: Roles admin y viewer]` (permite si coincide, 403 si no, 401 si no hay sesión y la falta de autenticación se resuelve antes que la de autorización)
- [x] 2.11 Definir cómo `deps.py` obtiene la instancia de `AuthService` (inyección vía `app.state.auth_service` seteado en `lifespan`, leído con `Depends` que accede a `request.app.state.auth_service` — mismo patrón que se use para acceder a `column_writer`/`event_bus` existentes en `main.py`; verificar el patrón exacto ya usado ahí antes de escribir `deps.py`)
- [x] 2.12 Escribir tests unitarios en `tests/unit/test_deps.py` (o agregar a `test_auth_service.py`) para `get_current_user` y `require_role` usando un `Request` mockeado o `TestClient`, sin necesitar Postgres real (mockear `AuthService.decode_access_token`)

## Phase 3: Backend endpoints (routing en main.py)

- [x] 3.1 En `src/main.py`, dentro de `lifespan()`, instanciar `AuthService(dsn=settings.timescaledb_dsn)` y llamar a `.connect()`/`.close()` en el mismo punto donde se conecta/cierra `column_writer`. **Decisión confirmada por el usuario: fail-fast, NO best-effort.** Si `settings.auth_secret_key` es `None`/vacío, levantar una excepción explícita (ej. `RuntimeError("AUTH_SECRET_KEY is required — refusing to start with an unsigned/predictable JWT secret")`) que aborte el arranque del proceso ANTES de aceptar requests — no aplicar el patrón best-effort de Redis acá, porque una clave de firma faltante o predecible permite forjar tokens válidos (vulnerabilidad crítica, no una degradación aceptable de servicio). Documentar esta decisión en un comentario en el código, junto a la validación.
- [x] 3.2 Guardar la instancia en `app.state.auth_service` para que `src/api/deps.py` pueda resolverla vía `Depends`
- [x] 3.3 Implementar `POST /auth/register` en `src/main.py` (o en un router aparte `src/api/auth_router.py` incluido vía `app.include_router` si el proyecto prefiere separar routers — verificar si `main.py` ya usa `include_router` en algún lado antes de decidir): recibe `UserCreate`, llama a `auth_service.create_user()`, retorna `201` con `UserPublic`; captura `EmailAlreadyRegisteredError` y retorna `409` con body `{"error": "email already registered"}` — cubre `[Requirement: Registro de usuario]` escenarios de éxito con rol explícito, éxito con default `viewer`, y rechazo por email duplicado
- [x] 3.4 Confirmar que la validación 422 de Pydantic (password corto, email inválido) ya funciona out-of-the-box vía `UserCreate` sin código adicional en el endpoint — verificar con un request manual antes de dar la tarea por cerrada
- [x] 3.5 Implementar `POST /auth/login` en el mismo router: recibe `{email, password}`, llama a `auth_service.get_user_by_email()` + `verify_password()`; si el usuario no existe O el password es incorrecto, retorna el mismo `401` con mensaje genérico `"invalid credentials"` en ambos casos (sin distinguir cuál falló) — cubre `[Requirement: Login]` los 3 escenarios, en particular que la respuesta de error sea indistinguible entre email inexistente y password incorrecto
- [x] 3.6 En `POST /auth/login` (éxito), setear la cookie de respuesta con `response.set_cookie("session", token, httponly=True, secure=True, samesite="lax", max_age=settings.auth_token_expire_minutes * 60)` y devolver `200` con `UserPublic`
- [x] 3.7 Implementar `POST /auth/logout`: no depende de `get_current_user` (debe funcionar incluso sin sesión activa — `[Requirement: Logout / Scenario: Logout sin sesión activa no falla]`), expira la cookie con `response.set_cookie("session", "", max_age=0)` (o `response.delete_cookie("session")`), retorna `204`
- [x] 3.8 Implementar `GET /auth/me` protegido con `Depends(get_current_user)`: retorna `200` con `CurrentUser` serializado como `{id, email, role}` — cubre `[Requirement: Perfil del usuario autenticado / Scenario: Usuario autenticado consulta su propio perfil]`
- [x] 3.9 Revisar los 13 endpoints existentes de `src/main.py` uno por uno y confirmar por inspección visual que NINGUNO ganó un `Depends()` de auth ni cambió de firma — es una verificación explícita de no-regresión de la Decisión 2 del `design.md`, no una tarea de código
- [x] 3.10 Levantar el servidor local (`uvicorn src.main:app --reload`) y probar manualmente con `curl`/httpie los 4 endpoints nuevos en secuencia: register → login (capturar cookie) → me (con cookie) → logout → me (con cookie ya expirada, debe dar 401)

## Phase 3.5: Migración a roles jerárquicos (superadmin/admin/moderador/viewer)

Contexto: cambio de alcance confirmado por el usuario DESPUÉS de que Fases 1-3 ya se implementaron y commitearon (`48cf2e4`, `28425ad`) con el modelo original de 2 roles planos. Ver `design.md` Decision 6 para la justificación completa. Esta fase resuelve también el hallazgo de seguridad pendiente documentado en el docstring de `POST /auth/register`: "cualquier caller puede elegir `role`, incluyendo admin".

- [x] 3.5.1 Migrar `UserRole` en `src/models/user.py` a 4 valores (`SUPERADMIN`, `ADMIN`, `MODERADOR`, `VIEWER`), manteniendo `str, Enum` (NO `IntEnum` — ver `design.md` Decision 6 para el porqué). Agregar `ROLE_LEVEL: dict[UserRole, int]` y `role_level(role: UserRole) -> int` en el mismo módulo
- [x] 3.5.2 Crear `deploy/sql/migrations/002_add_role_hierarchy.sql`: `ALTER TABLE users DROP CONSTRAINT users_role_check` + `ALTER TABLE users ADD CONSTRAINT users_role_check CHECK (role IN ('superadmin','admin','moderador','viewer'))`, con rollback documentado (nota explícita de que el rollback falla si ya hay filas con `role` en `superadmin`/`moderador` — hay que reasignarlas o borrarlas antes). NO editar `001_create_users_table.sql` (ya aplicado contra el Postgres real)
- [x] 3.5.3 Aplicar la migración 002 contra el Postgres real del perfil `storage` (`docker exec timescaledb`, puerto 5433) y confirmar con `\d users` que el `CHECK` constraint quedó con los 4 valores
- [x] 3.5.4 En `src/api/deps.py`, agregar `require_min_role(role: UserRole)`: factory de dependencia que exige sesión válida (`get_current_user`) y rechaza con 403 si `role_level(current_user.role) < role_level(role)` (nivel INFERIOR al mínimo — no "distinto"). El `require_role` existente (igualdad exacta) se mantiene sin cambios de comportamiento; documentar en docstrings cuándo usar cada uno
- [x] 3.5.5 En `src/main.py`, modificar `POST /auth/register`: antes del INSERT, contar filas de `users` (`SELECT COUNT(*) FROM users`); si `COUNT = 0` forzar `role=UserRole.SUPERADMIN`, si `COUNT > 0` forzar `role=UserRole.VIEWER` — en ambos casos ignorando el `role` del payload. Actualizar el docstring del endpoint para reflejar que el hallazgo de seguridad pendiente ya está resuelto (quitar la nota de "Open Questions / desviación reportada al orquestador")
- [x] 3.5.6 Decidir e implementar dónde vive el `COUNT(*)` + INSERT: mismo método `create_user`/nuevo método en `AuthService`, o lógica en el endpoint — documentar la decisión (recomendado: dentro de `AuthService.create_user` o un método dedicado, para que la operación de conteo+insert quede atómica dentro de la misma conexión/transacción y no haya race condition entre el COUNT y el INSERT bajo concurrencia)
- [x] 3.5.7 Actualizar `tests/unit/test_auth_service.py`: reemplazar cualquier uso de `UserRole.ADMIN`/`UserRole.VIEWER` que dependiera de la semántica de 2 roles si corresponde (la mayoría de los tests existentes no deberían romperse, son agnósticos al valor concreto del rol); agregar tests de `role_level()` con los 4 niveles
- [x] 3.5.8 Actualizar `tests/unit/test_deps.py`: agregar tests de `require_min_role` — acceso permitido con rol exacto al mínimo, acceso permitido con rol de nivel superior al mínimo, 403 con rol de nivel inferior, 401 sin sesión (antes que 403)
- [x] 3.5.9 Agregar tests nuevos (en `tests/unit/test_auth_service.py` o un archivo nuevo `tests/unit/test_user_registration_bootstrap.py`) para la lógica de bootstrap: tabla vacía → primer registro fuerza `superadmin` sin importar el `role` del payload (incluida su ausencia); tabla no vacía → registro fuerza `viewer` sin importar el `role` pedido (probar con `admin`, `moderador` y `superadmin` en el payload, los 3 deben resultar en `viewer`)
- [x] 3.5.10 Correr `venv/bin/python3 -m pytest tests/unit/ -v` y confirmar que toda la suite pasa, salvo el fallo preexistente ya conocido y no relacionado de `test_geo_utils.py::test_ms_to_iso`

## Phase 4: Frontend (auth.ts, login page, AuthProvider, guard)

- [ ] 4.1 Crear `dashboard/lib/auth.ts` con funciones `login(email: string, password: string): Promise<UserPublic>`, `logout(): Promise<void>`, `getMe(): Promise<UserPublic | null>` — todas usan `fetch` contra `API_BASE_URL` (mismo patrón que `dashboard/lib/api.ts`) con `credentials: 'include'` obligatorio en las tres para que el browser adjunte/reciba la cookie httpOnly cross-origin (dashboard:3000 → API:8000)
- [ ] 4.2 En `dashboard/lib/auth.ts`, definir el tipo `UserPublic` (`id`, `email`, `role: 'admin' | 'viewer'`) — reusar o extender `dashboard/lib/types.ts` si ahí es donde viven los tipos compartidos existentes (verificar convención en ese archivo antes de decidir dónde declararlo)
- [ ] 4.3 En `getMe()`, distinguir explícitamente el caso `401` (no autenticado, retornar `null`) de otros errores de red (dejar que la excepción se propague) — no tratar un 401 como error fatal, es un estado válido de "sin sesión"
- [ ] 4.4 Crear un `AuthContext` (React Context) en `dashboard/lib/auth.ts` o en un archivo nuevo `dashboard/hooks/use-auth.tsx` (verificar convención: el proyecto ya tiene `dashboard/hooks/`, confirmar antes de elegir ubicación) con estado `{ user: UserPublic | null, loading: boolean, login, logout }`
- [ ] 4.5 Modificar `dashboard/app/providers.tsx` para agregar `AuthProvider` compuesto junto a `ThemeProvider` y `TooltipProvider` ya existentes, envolviendo `children`; al montar, el provider llama a `getMe()` una vez para hidratar el estado de sesión inicial
- [ ] 4.6 Crear `dashboard/app/login/page.tsx`: formulario controlado con `email`/`password`, botón submit que llama a `login()` del `AuthContext`, muestra el mensaje de error genérico si falla (401), y redirige (`useRouter().push`) a la ruta protegida por defecto en caso de éxito
- [ ] 4.7 Implementar el guard de rutas protegidas: decidir entre `dashboard/middleware.ts` (Next.js middleware, corre en Edge, no tiene acceso directo al `AuthContext` de React) o un wrapper condicional dentro de `dashboard/app/layout.tsx` que redirige a `/login` si `user === null` y `loading === false` tras la hidratación inicial — documentar en un comentario por qué se eligió una opción sobre la otra (el `design.md` deja esto abierto explícitamente)
- [ ] 4.8 Aplicar el guard elegido en 4.7 sin romper el acceso a `/login` en sí (evitar loop de redirección) ni a rutas que deban seguir siendo públicas si las hubiera
- [ ] 4.9 Agregar al menos una diferencia visible en la UI entre `admin` y `viewer` (ej. mostrar el email/rol del usuario logueado en el sidebar, u ocultar/deshabilitar un control administrativo) en `dashboard/app/layout.tsx` o `AppSidebar` — cubre el Success Criteria del proposal "La UI refleja el rol del usuario autenticado"
- [ ] 4.10 Agregar botón/acción de logout visible en la UI (ej. en el sidebar o header) que llama a `logout()` del `AuthContext` y redirige a `/login`

## Phase 5: Testing (integración, no-regresión)

- [ ] 5.1 Crear `tests/integration/test_auth_api.py` con fixture de Postgres efímero vía `testcontainers` (extra `postgres` agregado en 1.4) — usar imagen Postgres plana, no TimescaleDB completa, aplicando la migración 001 (`deploy/sql/migrations/001_create_users_table.sql`) al arrancar el contenedor
- [ ] 5.2 Test: flujo completo `POST /auth/register` → `POST /auth/login` → `GET /auth/me` con las cookies encadenadas entre requests (usar el `cookies` jar de `httpx.Client`/`TestClient`) — `[Requirement: Registro de usuario / Scenario: Registro exitoso con rol explícito]` + `[Requirement: Login / Scenario: Login exitoso emite cookie de sesión]` + `[Requirement: Perfil del usuario autenticado / Scenario: Usuario autenticado consulta su propio perfil]`
- [ ] 5.3 Test: `POST /auth/register` sin `role` explícito asigna `viewer` por default — `[Requirement: Registro de usuario / Scenario: Registro exitoso sin rol explícito usa el default viewer]`
- [ ] 5.4 Test: `POST /auth/register` con email duplicado retorna 409 y no crea fila nueva (verificar contando filas antes/después) — `[Scenario: Registro rechazado por email duplicado]`
- [ ] 5.5 Test: `POST /auth/register` con password de menos de 8 caracteres retorna 422 — `[Scenario: Registro rechazado por password que no cumple la política mínima]`
- [ ] 5.6 Test: `POST /auth/register` con email con formato inválido retorna 422 — `[Scenario: Registro rechazado por email con formato inválido]`
- [ ] 5.7 Test: `POST /auth/login` con password incorrecto retorna 401 con mensaje genérico y sin `Set-Cookie` — `[Scenario: Login rechazado por password incorrecto]`
- [ ] 5.8 Test: `POST /auth/login` con email inexistente retorna 401 con el mismo mensaje genérico que 5.7 (comparar el body de ambos tests para confirmar que son indistinguibles) y sin `Set-Cookie` — `[Scenario: Login rechazado por email inexistente]`
- [ ] 5.9 Test: `POST /auth/logout` con sesión activa retorna 204, incluye `Set-Cookie` con `Max-Age=0`, y una request posterior a `GET /auth/me` reusando la cookie vieja retorna 401 — `[Requirement: Logout / Scenario: Logout con sesión activa limpia la cookie]`
- [ ] 5.10 Test: `POST /auth/logout` sin cookie de sesión retorna 204 sin excepción — `[Scenario: Logout sin sesión activa no falla]`
- [ ] 5.11 Test: `GET /auth/me` sin cookie retorna 401 con body indicando "not authenticated" — `[Scenario: Usuario no autenticado recibe 401]`
- [ ] 5.12 Test: `GET /auth/me` con cookie de valor arbitrario (no-JWT) y con JWT firmado con clave distinta a `settings.auth_secret_key`, ambos retornan 401 sin 500 — `[Scenario: Cookie con JWT corrupto o con firma inválida recibe 401]`
- [ ] 5.13 Test de un endpoint protegido de prueba con `require_role("admin")`: usuario `admin` accede sin 403, usuario `viewer` recibe 403, cliente sin sesión recibe 401 (antes que 403) — cubre los 3 escenarios de `[Requirement: Roles admin y viewer]` sobre `require_role` (puede requerir un endpoint de prueba temporal montado solo en el test, o testear `require_role` a nivel de dependencia directamente sin HTTP si es más simple)
- [ ] 5.14 Test: usuario `viewer` autenticado accede a `GET /auth/me` con 200 y `role="viewer"` en el body, mismo comportamiento que `admin` — `[Scenario: Viewer puede usar /auth/me igual que admin]`
- [ ] 5.15 Test de expiración: usando `freezegun`, generar un JWT con `auth_token_expire_minutes=1440`, avanzar el reloj 1 hora y confirmar `GET /auth/me` sigue en 200 — `[Requirement: Expiración de sesión / Scenario: Token dentro de su ventana de validez es aceptado]`
- [ ] 5.16 Test de expiración: avanzar el reloj más allá del `exp` y confirmar `GET /auth/me` retorna 401 — `[Scenario: Token expirado es rechazado]`
- [ ] 5.17 Test: tras recibir 401 por expiración, un nuevo `POST /auth/login` con credenciales válidas emite un JWT nuevo con `exp` recalculado, y una request subsiguiente con la cookie nueva es aceptada — `[Scenario: Usuario con sesión expirada debe volver a hacer login]`
- [ ] 5.18 Test: la respuesta `Set-Cookie` de `POST /auth/login` exitoso incluye el atributo `HttpOnly` (verificable inspeccionando el header crudo de la respuesta, no el cliente HTTP que ya parsea cookies) — `[Requirement: Cookie de sesión httpOnly / Scenario: La cookie de sesión no es accesible vía document.cookie]`
- [ ] 5.19 Extender `tests/integration/test_api.py` (existente) con un test que hace requests SIN cookie ni header `Authorization` contra `/health`, `/report`, `/events`, `/alerts` y confirma que los 4 siguen respondiendo `200` con el mismo shape de body que antes del change — `[Requirement: No regresión sobre endpoints existentes / Scenario: Endpoints de datos siguen siendo públicos sin sesión]`
- [ ] 5.20 Correr la suite completa existente (`pytest tests/`) y confirmar que ningún test de `tests/integration/test_api.py`, `tests/integration/test_redis_pubsub_bus.py`, `tests/integration/test_request_id.py` ni los de `tests/unit/` se rompió por los cambios de `main.py`/`settings.py`/`requirements.txt` — cubre el Success Criteria del proposal "Los tests existentes... siguen pasando"
- [ ] 5.21 Verificación manual de CORS: con el dashboard (`localhost:3000`) y la API (`localhost:8000`) corriendo en dev, hacer login desde el browser y confirmar en devtools que la cookie `session` se setea y se reenvía en requests subsiguientes cross-origin sin abrir `cors_allowed_origins` a `*` — según Testing Strategy de `design.md`, punto Manual/E2E

## Phase 6: Infra/Docker

- [ ] 6.1 Agregar `AUTH_SECRET_KEY` (comentado con placeholder, ej. `# AUTH_SECRET_KEY=change-me-in-production`) al servicio `geospectrum` en `deploy/docker/docker-compose.yml`, mismo estilo que `TIMESCALEDB_PASSWORD`
- [ ] 6.2 Confirmar que `deploy/docker/docker-compose.yml` no requiere ningún servicio nuevo — la tabla `users` vive en el `timescaledb` del perfil `storage` ya definido; documentar en un comentario junto a la definición del servicio que la migración 001 debe aplicarse manualmente (o vía el mecanismo que el equipo decida) antes de que `AuthService` pueda operar
- [ ] 6.3 Si el proyecto tiene `.env.example` o equivalente, agregar `AUTH_SECRET_KEY=` y `AUTH_TOKEN_EXPIRE_MINUTES=1440` ahí también, siguiendo el patrón de las variables de TimescaleDB ya documentadas
- [ ] 6.4 Verificar (`deploy/k8s/secret.yaml`, `deploy/k8s/configmap.yaml`) si el despliegue K8s existente necesita el nuevo secret `AUTH_SECRET_KEY` agregado para no romper un deploy a ese entorno — si el alcance del change no cubre K8s explícitamente, al menos dejar constancia explícita de que quedó pendiente, no aplicarlo en silencio
