# Tasks: Registro por invitación con emails (Resend + react-email) y onboarding guiado

Convenciones de este archivo:

- **Cada fase = un batch** (una sesión enfocada de sub-agente de `sdd-apply`). El orden de fases respeta el rollout de Decision 10: migración → backend → dashboard → verificación. NO adelantar tareas de una fase posterior.
- **AC** = criterio de aceptación, referenciando los escenarios de `specs/auth/spec.md` y `specs/dashboard-ui/spec.md` de este change.
- **Contrato de no-lockout** (transversal, no negociable): existencia de cuenta PRIMERO, invitación DESPUÉS. Bootstrap (`users` vacía) y auto-link por email quedan INTACTOS. La Fase 4 lo verifica con tests dedicados y la Fase 9 en producción.
- **Versiones npm pinneadas** (open question del design, resuelta el 2026-08-05 contra el registry):
  - `resend@6.18.1` (dependency)
  - `@react-email/components@1.0.12` (dependency; peer `react ^18 || ^19` → compatible React 19.2)
  - `driver.js@1.8.0` (dependency; vanilla JS, sin peer de React — el 1.3.x que esperaba el design quedó viejo)
  - `react-email@6.9.1` (devDependency, solo para preview `email:dev`; peers `react`/`react-dom` `^18 || ^19`)
  - `jose`: **ya está en `dashboard/package.json` (`^5.9.6`, lo usa `middleware.ts`) — NO instalar ni upgradear**; `verify-session.ts` reusa la existente.

## Phase 1: Migración 007 + settings (foundation)

- [x] 1.1 Crear `deploy/sql/migrations/007_invitations.sql` con el SQL EXACTO de design → Interfaces/Contracts: tabla `invitations` (id, email, role con CHECK, `token_hash` UNIQUE, `invited_by`/`accepted_by` con `ON DELETE SET NULL`, timestamps de estado derivado, `email_sent_at`), índice `idx_invitations_email` sobre `lower(email)`, `users.onboarding_completed_at`, backfill `UPDATE users SET onboarding_completed_at = now() WHERE ... IS NULL`, y rollback documentado en comentarios (formato de 001–006). AC: correr el archivo dos veces seguidas no falla (idempotente: `IF NOT EXISTS` en todo); el rollback comentado incluye `DROP TABLE` + `DROP COLUMN`.
- [x] 1.2 Aplicar la 007 contra el TimescaleDB local — **puerto 5433, NO 5432** (en 5432 hay un Postgres nativo de macOS que no es el del proyecto — memoria documentada). AC: `\d invitations` muestra la tabla; `users.onboarding_completed_at` existe y TODOS los usuarios preexistentes lo tienen NO nulo (backfill — sin esto los usuarios existentes verían el wizard).
- [x] 1.3 Agregar `invitation_expire_days: int = 7` a `src/config/settings.py` (Decision 9: con default, sin fail-fast). AC: el backend levanta sin la env definida; con `INVITATION_EXPIRE_DAYS=14` en env, el setting vale 14.

## Phase 2: Backend — modelos + `InvitationService` (CRUD)

- [x] 2.1 Crear `src/models/invitation.py`: `InvitationStatus` (str-Enum: pending/accepted/revoked/expired), `InvitationCreate` (`EmailStr` + `UserRole`), `InvitationPublic` (id, email, role, status derivado, invited_by, created_at, expires_at, accepted_at, email_sent_at — SIN token ni hash), `InvitationWithToken` (`InvitationPublic` + `token: str`) (Decision 8). AC: por construcción de tipos, un endpoint que responda `list[InvitationPublic]` no puede filtrar el token (escenario "El listado no expone tokens").
- [x] 2.2 Modificar `src/models/user.py`: `UserCreate.invitation_token: Optional[str] = None` y response model `MeResponse` (= shape actual de `/auth/me` + `onboarding_completed_at: Optional[datetime]`). AC: mypy pasa; ningún campo `role` del payload de register llega al INSERT.
- [x] 2.3 Crear `src/services/invitation_service.py` (parte 1): constante módulo-level `PENDING_PREDICATE_SQL = "accepted_at IS NULL AND revoked_at IS NULL AND expires_at > now()"`, excepciones (`InvitationRequiredError`, `InvalidInvitationError`, `InvitationAlreadyExistsError`, `InvitationAlreadyAcceptedError`, `CannotInviteHigherRoleError` — clases planas, sin HTTP), y clase `InvitationService(pool, expire_days)` con `create_invitation(email, role, invited_by)`: transaccional, guard de escalación con `role_level()` de `src/models/user.py`, 409 lógico si email ya en `users` o si existe pendiente vigente (la unicidad se garantiza acá, NO con índice parcial — Decision 1), token `secrets.token_urlsafe(32)`, persiste `hashlib.sha256(...).hexdigest()`, retorna `InvitationWithToken` con el claro UNA vez. AC: escenarios "Un admin crea una invitación exitosamente", "Un admin no puede invitar con rol superadmin", "Invitación rechazada si el email ya tiene cuenta", "…si ya existe una pendiente no expirada".
- [x] 2.4 `InvitationService` (parte 2): `list_invitations()` (status derivado de timestamps evaluado en la query), `revoke_invitation(id)` (setea `revoked_at`; excepción si ya aceptada), `resend_invitation(id)` (regenera token+hash+`expires_at`, resetea `email_sent_at` a NULL; excepción si aceptada o revocada; una expirada REVIVE — escenario "Reenviar una invitación expirada la revive"), `validate_token(token)` (lookup por hash; NO consume; excepciones distintas para "no existe" vs "existe pero no pendiente" → el endpoint las traduce a 404/410), `mark_email_sent(id)`. AC: escenarios "El listado refleja los cuatro estados", "Reenviar invalida el link anterior", "Token válido devuelve email y rol", "La validación no consume".

## Phase 3: Backend — consumo en `AuthService` + endpoints en `main.py`

- [x] 3.1 `src/services/auth_service.py`: `_consume_pending_invitation(conn, email, token=None) -> Optional[asyncpg.Record]` — un único `UPDATE invitations SET accepted_at = now() WHERE {PENDING_PREDICATE_SQL} AND (token_hash = sha256(token) | lower(email) = lower($email)) RETURNING id, email, role`, importando `PENDING_PREDICATE_SQL` de `invitation_service` (sin ciclo de imports). DEBE invocarse solo dentro de la transacción del caller. AC: es la ÚNICA primitiva de consumo; ambos caminos (Fase 3.2 y 3.3) la usan.
- [x] 3.2 `create_user()`: dentro de la transacción existente — (1) `_determine_bootstrap_role()` primero: tabla vacía → superadmin SIN token, código intacto; (2) tabla no vacía → sin token `InvitationRequiredError`; consumo por token devuelve None → `InvalidInvitationError`; email payload ≠ email invitación (case-insensitive) → error propio SIN consumir; rol del INSERT = rol de la invitación, `accepted_by` = usuario creado. AC: escenarios "Registro exitoso con invitación hereda el rol invitado", "El payload no puede pisar el rol", "Registro rechazado si el email no coincide", "No-lockout (3) bootstrap", "Fallo posterior en la transacción no quema la invitación" (el UPDATE revierte con el rollback).
- [x] 3.3 `resolve_or_create_google_user()`: ramas 1 (ya vinculado por `google_id`) y 2 (auto-link por email) **INTACTAS y evaluadas primero — no tocan `invitations`**; SOLO en la rama 3 (usuario nuevo): bootstrap OR `_consume_pending_invitation(conn, email)` por match de email (sin token — Decision 5) usando el rol de la invitación OR `InvitationRequiredError`. En el callback de `main.py`: `InvitationRequiredError` → `_google_error_redirect("google_no_invitation")` (redirect 302, nunca JSON ni 500 — el `Depends()` no debe resolver nada que explote fuera del try, lección documentada). AC: escenarios "No-lockout (2)", "No-lockout (4)", "Google con invitación pendiente crea la cuenta con el rol invitado", "Google con invitación expirada es rechazado", "Google con email no verificado no consume la invitación".
- [x] 3.4 `AuthService.get_onboarding_status(user_id)` (SELECT por PK) y `complete_onboarding(user_id)` (idempotente: `UPDATE users SET onboarding_completed_at = now() WHERE id = $1 AND onboarding_completed_at IS NULL`). AC: escenario "Completar onboarding persiste y es idempotente" (el timestamp de la primera llamada no se pisa).
- [x] 3.5 `src/main.py` (parte 1): instanciar `InvitationService` en `lifespan()` con el pool compartido y publicarlo en `app.state.invitation_service` (mismo patrón que `auth_service`); endpoints `POST /auth/invitations` (201, token en claro), `GET /auth/invitations` (200 lista), `DELETE /auth/invitations/{id}` (204; 409 si aceptada), `POST /auth/invitations/{id}/resend` (200, token nuevo), `POST /auth/invitations/{id}/mark-sent` (204) — todos con `require_min_role(UserRole.ADMIN)` existente (`deps.py` NO se toca), errores como `JSONResponse {"error": ...}` + métricas, matriz exacta de Decision 3. AC: escenarios de 401/403 por rol ("Un viewer o moderador no puede crear invitaciones", "Sin sesión no se puede crear invitación", "Un viewer no puede listar").
- [x] 3.6 `src/main.py` (parte 2): `GET /auth/invitations/validate?token=` público (200 `{email, role, expires_at}` / 404 desconocido / 410 conocido pero no pendiente — Decision 3); contrato nuevo de `POST /auth/register` (403 "invitation required" / 410 token inválido / 422 email mismatch / 409 duplicado / 201); `GET /auth/me` → `MeResponse` con `onboarding_completed_at` leído de la BASE (no del JWT — Decision 6); `POST /auth/me/onboarding-complete` (204, `get_current_user`, sin restricción de rol). AC: escenarios "Registro sin token es rechazado en un sistema no vacío", "Usuario nuevo tiene onboarding pendiente", "Sin sesión no se puede marcar onboarding". Nota spec vs design: la spec pide no distinguir causas en `validate`; el design (Decision 3) resuelve deliberadamente 404 vs 410 con rationale (token de 256 bits, sin riesgo de enumeración) — se implementa el design; ambos códigos son "token no válido" para el que no lo posee.

## Phase 4: Backend — tests (contra base real, no mocks)

Harness: pytest del proyecto (asyncio_mode=auto), mismo esquema que `tests/unit/test_auth_service.py` — **contra Postgres real** (lección documentada: los mocks del proyecto son ciegos a bugs de SQL y concurrencia). En local, el TimescaleDB del stack escucha en **5433**.

- [x] 4.1 Crear `tests/unit/test_invitation_service.py`: create (el hash persistido ≠ token en claro y la fila no contiene el claro — escenario "La base no contiene el token en claro"; guard de escalación admin→superadmin; 409 por pendiente vigente duplicada; SÍ permite crear si la anterior expiró), revoke (409 sobre aceptada; revocada no valida — escenario "Revocar una invitación pendiente"), resend (hash nuevo, `email_sent_at` reseteado, token viejo muerto, expirada revive), validate (200/404/410, N validaciones no consumen), mark_email_sent. AC: todos los escenarios de los Requirements "Creación", "Token solo como hash", "Listado", "Revocación", "Reenvío", "Validación pública" cubiertos.
- [x] 4.2 Extender `tests/unit/test_auth_service.py` — consumo y **concurrencia**: registro con token válido hereda el rol; sin token → `InvitationRequiredError`; bootstrap sin token preservado; **doble accept concurrente con `asyncio.gather` (dos `create_user` con el mismo token): exactamente uno gana, `users` queda con UNA fila, invitación `accepted` una vez** (escenario "Dos registros concurrentes — solo uno gana"); **password + Google en paralelo sobre la misma invitación: un solo camino gana** (escenario "Aceptación por password y por Google en paralelo"); fallo post-consumo (violación de constraint) revierte TODO y la invitación sigue `pending`. AC: los tres escenarios del Requirement "Consumo single-use y transaccional", verificados contra la base real.
- [x] 4.3 Tests de regresión de lockout en `test_auth_service.py` (o archivo propio): `resolve_or_create_google_user()` con usuario ya vinculado y con auto-link **no ejecuta ninguna query sobre `invitations`** aunque exista una pendiente para ese email (la invitación queda intacta); usuario nuevo con invitación → rol de la invitación + consumida (match case-insensitive `INVITADA@...` vs `invitada@...`); nuevo sin invitación → `InvitationRequiredError`. AC: escenarios "No-lockout (2)" y los dos MODIFIED de Google.
- [x] 4.4 Crear `tests/integration/test_invitations_api.py` (`TestClient`, patrón de `test_auth_api.py`): superficie completa `/auth/invitations*` — 401 sin sesión en los 5 endpoints protegidos, 403 con viewer y con moderador, 201/200/204 con admin y superadmin, admin no invita superadmin (y superadmin sí), `validate` accesible SIN cookie, matriz 404/410 de `validate`, `mark-sent` setea `email_sent_at`. AC: tabla de Decision 3 completa, código por código.
- [x] 4.5 Actualizar `tests/integration/test_auth_api.py` — **los tests del registro abierto ROMPEN POR DISEÑO** (previsto en el proposal, tarea de primera clase): reescribirlos para el contrato nuevo (matriz 403/410/422/409/201 de `POST /auth/register`); agregar: bootstrap con tabla vacía sin token → 201 superadmin; `GET /auth/me` incluye `onboarding_completed_at`; `POST /auth/me/onboarding-complete` idempotente y 401 sin sesión; callback de Google sin invitación → 302 a `/login?error=google_no_invitation` sin cookie y sin 500 (mock de Authlib como en los tests existentes de google-oauth); logins existentes password y Google sin regresión (escenarios No-lockout 1 y 2 a nivel API). AC: `pytest` verde completo.
- [x] 4.6 Gate de calidad backend: `pytest` completo + `black` + `ruff` + `mypy` (line-length 100, strict) sin errores en los archivos nuevos/modificados. AC: los tres linters limpios; ninguna prueba existente ajena al change rota.

## Phase 5: Dashboard — dependencias, sesión compartida, email y route de envío

- [x] 5.1 `dashboard/package.json`: agregar dependencies **exactas** `resend@6.18.1`, `@react-email/components@1.0.12`, `driver.js@1.8.0`; devDependency `react-email@6.9.1`; script `"email:dev": "react-email dev --dir emails"`. **NO tocar `jose` (ya está, `^5.9.6`). NO usar `npx create-email`** (scaffold standalone innecesario — Decision 4). Si npm reporta conflicto de peers con React 19.2 al instalar, resolver ahí y anotar la versión final en este archivo. AC: `npm install` limpio; `npm run build` sigue pasando; ninguna dep nueva aparece en un client bundle todavía. **Resultado: SIN conflicto de peers con React 19.2 — las cuatro versiones pinneadas se instalaron tal cual, sin `--legacy-peer-deps` ni `--force`, con `npm install --save-exact`. `npm run build` verde y `/api/invitations/send` figura como `ƒ` (dynamic, 125 B de bundle cliente): ni el SDK ni los templates llegan al cliente.**
- [x] 5.2 Crear `dashboard/lib/verify-session.ts`: extraer la verificación `jose.jwtVerify(token, AUTH_SECRET_KEY, HS256)` HOY duplicable de `dashboard/middleware.ts` a un helper `verifySession()` que retorna payload tipado `{sub, email, role, ...}` o `null`; refactorizar `middleware.ts` para usarlo Y agregar `'/invite'` a `PUBLIC_PATHS` (el matcher por prefijo cubre `/invite/xyz`). AC: escenario "Visitante sin sesión abre un link de invitación válido" (sin redirect a `/login`); las rutas de `(app)` siguen protegidas; `/login` sigue público — sin regresión del middleware.
- [x] 5.3 Crear `dashboard/emails/components/EmailLayout.tsx` (branding GeoSpectrum, reusable para transaccionales futuros) y `dashboard/emails/InvitationEmail.tsx` (props: `email`, `role`, `inviteUrl`, `expiresAt`; copy en español; CTA con el link `/invite/{token}` y la fecha de expiración visible). AC: `npm run email:dev` renderiza el template; escenario "el cuerpo del email incluye un link a `/invite/T` … y la fecha/plazo de expiración". **Cambio de alcance del usuario (2026-08-11): el email es BILINGÜE ES/EN en un solo template — bloque español, `<Hr>` divisor, bloque inglés, cada uno con su heading, rol, CTA y fecha; sin prop `locale` ni decisión de runtime (al invitar no se sabe el idioma del destinatario y un email es envío único). Fechas con `Intl.DateTimeFormat` de locale EXPLÍCITO: es-AR `17/08/2026` (campos numéricos — `dateStyle: 'short'` abreviaba el año a `17/8/26`), en-US `Aug 17, 2026`. El pie de `EmailLayout` también quedó bilingüe; el resto del layout es agnóstico de idioma. Verificado con `render()` de `@react-email/components` (no con `email:dev`, que es un server que no termina): link exacto, ambos headings, ambos roles, ambos CTA y ambas fechas presentes en el HTML.**
- [x] 5.4 Crear `dashboard/app/api/invitations/send/route.ts` (runtime **nodejs**, Decision 4): `verifySession()` → 401 sin/inválida, 403 si `role ∉ {admin, superadmin}` — ANTES de instanciar Resend; body `{invitationId, email, role, token, expiresAt}`; link `${process.env.INVITE_BASE_URL}/invite/${token}` (fallback al origin del request en dev); `resend.emails.send({from, to, subject, react: InvitationEmail(...)})`; error de Resend → 502 JSON contenido; éxito → `POST {API}/auth/invitations/{id}/mark-sent` reenviando el header `Cookie` del request, **best-effort** (catch + log, no reporta error al admin); responde `{sent: true}`. **Checklist de seguridad del proposal (verificar en esta tarea): `RESEND_API_KEY` SIN prefijo `NEXT_PUBLIC_`; el SDK `resend` y los templates NUNCA importados desde un client component (`rg "from 'resend'" dashboard/app dashboard/components` solo debe matchear la route).** AC: escenarios "Sin sesión el envío se rechaza sin tocar Resend", "Un viewer con sesión válida no puede enviar", "Falla de Resend devuelve error contenido sin romper la invitación". **Checklist CERRADO: `rg "from 'resend'" dashboard/app dashboard/components` matchea SOLO `dashboard/app/api/invitations/send/route.ts:21`; `rg "NEXT_PUBLIC_RESEND" dashboard/` → cero matches; `export const runtime = 'nodejs'` en la línea 26. Orden verificado leyendo el archivo: `verifySession()` L55 → 401 L58 → 403 L61 → `new Resend(...)` L94, o sea el cliente de Resend NO se instancia si la sesión falta o el rol no alcanza. `from` desde `RESEND_FROM` con default `GeoSpectrum <invitaciones@geospectrum.org>`. Envs nuevas documentadas en `dashboard/.env.example` y `dashboard/README.md` (RESEND_API_KEY, INVITE_BASE_URL, RESEND_FROM), todas server-side.**

## Phase 6: Dashboard — página de aceptación, UI admin y login

- [x] 6.1 `dashboard/lib/auth.ts`: tipar `onboarding_completed_at: string | null` en el retorno de `getMe()`; agregar helpers `createInvitation`, `listInvitations`, `revokeInvitation`, `resendInvitation` (contra el backend) y `sendInvitationEmail` (contra `/api/invitations/send`), con manejo de los códigos de la matriz de Decision 3. AC: tipos estrictos, sin `any`; los helpers propagan el status para que la UI distinga los dos pasos. **Resultado: tipos nuevos en `lib/types.ts` (`MeResponse extends UserPublic`, `Invitation`, `InvitationWithToken`, `InvitationStatus`, `InvitationValidation`); `getMe(): Promise<MeResponse | null>` (compatible con `useAuth` sin tocarlo — MeResponse es asignable a UserPublic). Los helpers lanzan `ApiStatusError` (clase nueva con `status: number`) en vez de Error pelado — la UI matchea la matriz de Decision 3 por código. Además de los 5 pedidos se agregaron `validateInvitationToken()` (404/410, sin credentials — es público) y `registerWithInvitation()` (403/409/410/422), que 6.2 necesita y ningún task listaba.**
- [x] 6.2 Crear `dashboard/app/invite/[token]/page.tsx` (fuera de `(app)`, criterio de `login/`): al montar llama `GET /auth/invitations/validate?token=`; 200 → muestra email + rol read-only (sin control para cambiar rol) y dos caminos: formulario de password (→ `POST /auth/register` con `invitation_token`, y tras el 201 encadena `login()` existente de `lib/auth.ts` + redirect a `/` — sin auto-cookie en register, Decision 5) y botón "Continuar con Google" (`window.location.href = ${API_URL}/auth/google/login`, SIN token — el consumo es por email); 404 → "invitación no válida"; 410 → "vencida/revocada — pedí un reenvío"; en error NO se renderiza ningún formulario. AC: escenarios "Visitante sin sesión abre un link válido", "Aceptación con password crea la cuenta y entra al dashboard", "Link con token inválido o expirado muestra error sin formulario". **Resultado: client page con `React.use(params)` (Next 15: params es Promise). Máquina de estados loading/valid/invalid(404)/gone(410)/error(red) — los tres estados de error renderizan SOLO el mensaje, sin form ni Google. Post-201 el redirect es `window.location.href = '/'` (navegación dura, no router.push: fuerza middleware + re-hidratación de useAuth con la cookie fresca). Si register devuelve 410 en pleno submit (carrera con revocación/expiración) el error lo dice y pide reenvío; 409 → "ya tenés cuenta, entrá por login". Estética espejo de `login/` (dark, card, header GeoSpectrum).**
- [x] 6.3 Crear `dashboard/app/(app)/admin/invitations/page.tsx`: form de creación (email + selector de rol **limitado a roles ≤ nivel del propio** según `getMe()`), orquestación en dos pasos mostrando el resultado de CADA paso por separado (creada+enviada / creada+email falló con acción "reenviar" como recuperación), tabla con estado `pending/accepted/revoked/expired` + badge "email sin confirmar" cuando `email_sent_at` es null, acciones revocar (con confirmación; oculta si `accepted`) y reenviar (regenera token y reintenta email con el link NUEVO). Gate client-side: si `role ∉ admin+` → redirect/denegación sin renderizar datos (el enforcement real es del backend). AC: los cuatro escenarios del Requirement "UI de gestión de invitaciones para admin+". **Resultado: patrón de `(app)/beta/page.tsx` (SWR + Card + denegación client-side); la key de SWR es `null` si no es admin+ → ni siquiera se dispara el fetch. Selector de rol con `<select>` NATIVO a propósito (no hay Select en ui/ y el DropdownMenu de Radix pelea con teclado — memoria del proyecto), opciones filtradas por `ROLE_LEVEL[r] <= ROLE_LEVEL[user.role]`. Feedback de pasos como lista `StepResult[]` con ✓/✗ por paso (aria-live); reenviar reusa el mismo paso-2 con el token NUEVO. Los DOS 409 de create (email con cuenta vs pendiente duplicada) se distinguen sniffeando `'pending'` en el mensaje del backend — mismo código HTTP, único discriminador disponible (anotado como deuda menor: si el backend cambia el copy, cae al mensaje genérico del otro 409, sin romper nada). Revocar con AlertDialog de confirmación; acciones visibles solo en `pending`/`expired` (accepted/revoked → el backend las rechaza con 409, ocultar > deshabilitar).**
- [x] 6.4 `dashboard/components/AppSidebar.tsx`: entrada "Invitaciones" hacia `/admin/invitations`, visible SOLO para admin+ (según rol de `getMe()`). AC: escenario "Un viewer no ve ni alcanza la sección" (mitad navegación; la mitad URL la cubre 6.3). **Resultado: agregada a `ADMIN_ROUTES` existente (icono MailPlus) — el gate `isAdmin` del sidebar ya cubría el grupo "Administración", cero lógica nueva.**
- [x] 6.5 `dashboard/app/login/page.tsx`: mensaje en español para `?error=google_no_invitation` ("esta plataforma es solo por invitación…") manteniendo el form y el botón de Google operativos; auditar y eliminar cualquier afordancia de registro abierto. AC: escenarios "Rechazo de Google por falta de invitación se muestra en /login", "/login no ofrece registro abierto". **Resultado: AUDITORÍA, sin cambios de código — el commit 3203e89 (cierre invitation-only) ya había dejado ambas cosas hechas: `GOOGLE_OAUTH_ERROR_MESSAGES.google_no_invitation` muestra "El acceso es por invitación y tu cuenta todavía no tiene una…" y el login es solo-Google sin ninguna afordancia de registro. Verificado con `rg`: cero matches de "registr/crear cuenta/sign up" en login+landing (fuera de la lista de espera de la beta, que es otra cosa) y `/auth/register` solo se referencia desde `lib/auth.ts` (helper gateado por invitación) — ningún componente lo llama sin token.**

## Phase 7: Onboarding — wizard + tour driver.js

- [x] 7.1 Agregar atributos `data-tour-id` sobre los elementos reales (Decision 7 — NUNCA anclar por clases ni estructura DOM): `map` (contenedor del mapa en la home), `nav-globe` (entrada Globo en `AppSidebar.tsx`), `area-selector` (`AreaSelector.tsx`), `alerts-bell` (`NotificationBell.tsx`). AC: los 4 atributos presentes en el DOM renderizado de la home autenticada. **Resultado: `map` en el `<div>` que envuelve título + `AdvancedSeismicMap` en `(app)/page.tsx`; `nav-globe` en el `<Link>` del sidebar vía campo opcional `tourId` en el array `routes` (tipado con interface `NavRoute` — sin tocar las demás entradas); `area-selector` y `alerts-bell` en el `DropdownMenuTrigger` de cada componente (Radix spreadea los data-attrs al botón real).**
- [x] 7.2 Crear `dashboard/components/onboarding/tour-steps.ts` (definición declarativa: selector `[data-tour-id=...]` + título + texto en español por paso: mapa → globo → áreas → alertas) y `useTour.ts` (hook `"use client"` que instancia `driver.js` con import dinámico en `useEffect` — client-only, sin problema de SSR; callbacks de fin y de cierre). AC: el tour recorre los 4 pasos resaltando el elemento real; cerrar el overlay dispara el callback. **Resultado: `tour-steps.ts` importa `DriveStep` type-only (no arrastra runtime al bundle); `useTour.ts` precarga `import('driver.js')` en `useEffect` y `startTour()` re-awaitea el mismo import si aún no resolvió (el cache de módulos dedupea) — sin la trampa del ref-en-deps documentada en memoria. Fin vs cierre: driver.js 1.8 dispara `onDestroyed` en ambos casos, pero pasa el estado PRE-reset en `opts.state` (verificado en el source de la 1.8.0) → `activeIndex === TOUR_STEPS.length - 1` distingue "terminó" de "cerró antes". `skipMissingElement: true` para resiliencia (AreaSelector renderiza null si /areas falla). Un `suppressCallbacksRef` evita que el destroy de limpieza al desmontar (navegación/logout) dispare `onClose` — eso es abandono y el abandono no persiste. CSS de driver.js importado estático (inerte, Next lo extrae).**
- [x] 7.3 Crear `dashboard/components/onboarding/OnboardingWizard.tsx`: modal shadcn/Radix (patrón `ui/` existente) con paso 1 (bienvenida + nombre, persiste vía el `PATCH` de perfil existente/`full_name`, prefill si ya hay valor) y paso 2 (área inicial reusando `AreaSelector`), luego lanza el tour de 7.2; botón "Saltar" visible en TODO momento. AC: pasos idempotentes (re-entrar con datos ya cargados no duplica nada); saltar desde cualquier paso cierra todo. **Resultado: Dialog de `ui/dialog.tsx` controlado. Prefill vía `getProfile()`; el PATCH solo se dispara si el nombre cambió y no está vacío (vacío = no tocar, para no pisar con null un nombre ya guardado). Si el PATCH falla: error inline y se queda en el paso 1 — reintentar o Saltar, nunca trabado. Paso 2 reusa `AreaSelector` con el mismo refresco que `AreaHeader` (`emitAreaChanged()` + `router.refresh()`). "Comenzar el tour" cierra el Dialog y arranca driver.js tras 250 ms (deja a Radix soltar el scroll-lock del body). Cerrar el Dialog con X/Escape/overlay = Saltar (dismissal explícita ≠ abandono; el abandono que no persiste es cerrar la pestaña — enumeración de la spec). Terminar el tour y cerrarlo con la X convergen en `onFinished()` (design: saltar y terminar convergen).**
- [x] 7.4 Crear `dashboard/components/onboarding/OnboardingGate.tsx` y montarlo en `dashboard/app/(app)/layout.tsx`: renderiza el wizard si `getMe().onboarding_completed_at === null`; completar el tour O saltar → `POST /auth/me/onboarding-complete` y desmonta; si el POST falla, el wizard se cierra IGUAL en esta sesión (nunca bloqueante — a lo sumo reaparece en el próximo login); abandono sin completar/saltar no persiste nada. AC: escenarios "Primer login dispara el tour", "Completar el tour lo apaga para siempre", "Saltar el tour también persiste", "Abandono — el tour vuelve a ofrecerse", "La falla del endpoint de persistencia no bloquea al usuario". **Resultado: gate `"use client"` montado dentro de `SidebarInset` (mismo layout donde viven las anclas del sidebar/header; el Dialog portalea a body igual). Hace su propio `getMe()` en vez de leer `useAuth()`: el provider tipa `user` como `UserPublic` (el body de login no trae `onboarding_completed_at`) y castear sería apostar a un shape no garantizado — un PK lookup extra vs fragilidad de tipos. `handleFinished` primero `setShowWizard(false)` y DESPUÉS `completeOnboarding().catch(() => {})`: la UX nunca espera a la red y el fallo del POST no reabre nada. Helper nuevo `completeOnboarding()` agregado a `lib/auth.ts` (no existía de Fase 6). Gate verificado: `npx tsc --noEmit` verde; ESLint no corre en el repo (sin `eslint.config.*`, `next lint` deprecado pide setup interactivo — queda para el gate 8.6).**

## Phase 8: Dashboard — tests (Vitest + Playwright)

- [ ] 8.1 Vitest para `lib/verify-session.ts`: token firmado válido → payload con rol; firma inválida/token expirado/cookie ausente → `null`; distingue roles admin/superadmin/viewer. AC: cubre las decisiones de 401 vs 403 de la route.
- [ ] 8.2 Vitest para la allowlist de `dashboard/middleware.ts`: `/invite/abc123` sin cookie → pasa sin redirect; `/login` sin cookie → pasa; una ruta de `(app)` (p. ej. `/`) sin cookie → redirect a `/login`; con cookie válida → pasa. AC: la allowlist nueva no abrió rutas protegidas (regresión) y `/invite/*` es público.
- [ ] 8.3 Vitest para `emails/InvitationEmail.tsx`: render (snapshot o assertions) verificando que el HTML contiene el `inviteUrl` exacto y la expiración. AC: escenario "el cuerpo del email incluye un link a `/invite/T`".
- [ ] 8.4 Vitest para `app/invite/[token]/page.tsx` (mock de fetch): estado 200 muestra email+rol y ambos caminos sin selector de rol; 404 y 410 muestran error SIN formulario ni botón Google. AC: escenarios de la página de aceptación.
- [ ] 8.5 Playwright E2E mínimo del onboarding (riesgo "tour acoplado a la UI" del proposal): con `onboarding_completed_at` null el wizard aparece; las 4 anclas `data-tour-id` existen en la página; "saltar" persiste y al recargar el wizard NO reaparece. AC: escenarios "Primer login dispara el tour" y "Saltar el tour también persiste", contra la UI real.
- [ ] 8.6 Gate de calidad frontend: `npm run build` (el build de verify usa este comando) + ESLint limpios; re-verificar con `rg` que `resend`/`@react-email` no aparecen importados fuera de `app/api/` y `emails/`, y que no existe ninguna env `NEXT_PUBLIC_RESEND*`. AC: build verde; checklist de fuga de secreto/SDK cerrado.

## Phase 9: Rollout + verificación (mapea a `sdd-verify` — checklist de Decision 10)

Orden ESTRICTO (el código viejo ignora la tabla nueva; la inversa rompería):

- [ ] 9.1 Aplicar `007_invitations.sql` contra el TimescaleDB de **producción** ANTES de deployar código. AC: tabla + columna presentes; el backfill dejó a TODOS los usuarios de prod con `onboarding_completed_at` no nulo; la app vieja sigue funcionando (migración aditiva).
- [ ] 9.2 Deploy del **backend a Railway** (sin secretos nuevos en Railway). Smoke inmediato: `POST /auth/register` sin token → 403 "invitation required" (el registro queda cerrado en este instante, comportamiento deseado); `POST /auth/login` de un usuario existente → 200. AC: registro cerrado + no-lockout (1) confirmados en prod.
- [ ] 9.3 Cargar en **Vercel** las env nuevas: `RESEND_API_KEY` (server-side, SIN `NEXT_PUBLIC_` — la carga la hace el usuario, `sdd-apply` no puede) e `INVITE_BASE_URL=https://geospectrum.org`; deploy del dashboard. AC: `/api/invitations/send` responde 401 sin sesión (no 500 por env faltante); la sección de invitaciones visible para admin.
- [ ] 9.4 Checklist manual de no-regresión en producción (los 4 puntos de Decision 10): (1) login password de usuario existente OK; (2) login Google de usuario existente OK, incluido un caso de auto-link si queda alguno disponible; (3) Google sin cuenta ni invitación → error claro en `/login`, sin 500; (4) ningún usuario existente ve el wizard de onboarding. AC: no-lockout verificado EN LA PANTALLA que usa el usuario (lección documentada del proyecto).
- [ ] 9.5 Flujo end-to-end de invitación en prod: crear invitación al **propio email** (si `geospectrum.org` sigue sin verificar en Resend, Resend solo permite enviar al email de la cuenta — Dependency del proposal; anotar el estado de la verificación de dominio en el verify-report), recibir el email, abrir `/invite/{token}`, completar el alta (al menos un camino; ideal: password en un intento y Google en otro con invitaciones separadas), confirmar rol heredado y wizard disparado en el primer login, y que revocar/reenviar funcionan desde la UI admin. AC: Success Criteria 1, 2 y 6 del proposal demostrados en prod.

## Resumen de dependencias entre fases

```
1 (migración+settings) → 2 (modelos+InvitationService) → 3 (AuthService+endpoints) → 4 (tests backend)
                                                                                        ↓
5 (deps+sesión+email+route) → 6 (páginas) → 7 (onboarding) → 8 (tests frontend) → 9 (rollout+verify)
```

Las fases 5–8 requieren el backend de 1–3 corriendo en local para probar de punta a punta, pero pueden empezar apenas la Fase 3 esté mergeada (los contratos están fijados en el design). La Fase 9 exige TODO lo anterior verde.
