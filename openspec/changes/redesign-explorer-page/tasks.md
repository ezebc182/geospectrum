# Tasks: Redesign Explorer Page

Todas las tareas operan sobre un único archivo: `dashboard/app/explore/page.tsx` (258 líneas). No se crean ni modifican otros archivos (ver design.md — sin extracción de `EmptyState`, sin tocar `AlertBanner.tsx`).

## Phase 1: Imports y header

- [x] 1.1 En `dashboard/app/explore/page.tsx:1-9`, agregar los imports nuevos: `Card` desde `@/components/ui/card`, `Button` desde `@/components/ui/button`, `Badge` desde `@/components/ui/badge`. Mantener los imports existentes (`useState`, `FilterPanel`, `AdvancedSeismicMap`, `EventsTable`, `seismicAPI`, `SeismicEvent`, íconos de `lucide-react`) sin cambios.
- [x] 1.2 En `dashboard/app/explore/page.tsx:100`, migrar el `<h1>` de `text-3xl font-bold text-gray-900 dark:text-white mb-2` a `text-3xl font-bold text-foreground mb-2`, replicando el patrón de `dashboard/app/page.tsx:74`. Conservar el texto "Explorador de Eventos Sísmicos" sin cambios.
- [x] 1.3 En `dashboard/app/explore/page.tsx:103`, migrar el `<p>` subtítulo de `text-gray-600 dark:text-gray-400` a `text-muted-foreground`. Conservar el texto "Búsqueda avanzada con múltiples fuentes de datos y filtros personalizados" sin cambios.

## Phase 2: Panel de Controles y Estadísticas (Card, contador, toggle, botón exportar)

- [x] 2.1 En `dashboard/app/explore/page.tsx:124`, reemplazar el `div` raíz del panel (`bg-white dark:bg-gray-900 border-2 border-gray-200 dark:border-gray-700 rounded-lg p-4`) por `<Card className="p-4">`, cerrando con `</Card>` en el `</div>` de cierre correspondiente (línea 179). No usar `CardHeader`/`CardContent` (decisión de design.md). Conservar el `div` interno `flex items-center justify-between flex-wrap gap-4` (línea 125) tal cual.
- [x] 2.2 En `dashboard/app/explore/page.tsx:127-131`, migrar el contador de resultados: labels (`Resultados: `, ` eventos`) de `text-gray-600 dark:text-gray-400` a `text-muted-foreground`, y el valor (`{eventos.length}`) de `text-gray-900 dark:text-white` a `text-foreground`. Mantener `font-bold text-lg` en el valor.
- [x] 2.3 En `dashboard/app/explore/page.tsx:134-136`, migrar el texto de rango de magnitud (`M{min} - M{max}`) de `text-gray-600 dark:text-gray-400` a `text-muted-foreground`. Mantener la condición `eventos.length > 0 &&` sin cambios.
- [x] 2.4 En `dashboard/app/explore/page.tsx:142`, migrar el contenedor del toggle Mapa/Lista de `border-2 border-gray-300 dark:border-gray-600` a `border-2 border-border`.
- [x] 2.5 En `dashboard/app/explore/page.tsx:145-149` (botón Mapa) y `156-160` (botón Lista), migrar la rama inactiva del ternario de `bg-white dark:bg-gray-900 text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800` a `bg-background text-muted-foreground hover:bg-muted`. Mantener la rama activa `bg-primary text-primary-foreground` sin cambios (ya migrada, es el fix previo del bug `bg-seismic-600`). Verificar que la clase `bg-seismic-600` no existe en ninguna rama.
- [x] 2.6 En `dashboard/app/explore/page.tsx:156`, migrar el separador interno del toggle (`border-l-2 border-gray-300 dark:border-gray-600`) a `border-l-2 border-border`.
- [x] 2.7 En `dashboard/app/explore/page.tsx:168-176`, reemplazar el `<button>` HTML de "Exportar CSV" por `<Button onClick={exportToCSV}>` de shadcn con variante `default` (sin especificar `variant`, ya que `default` es la variante por defecto de `buttonVariants`), conservando el ícono `<Download className="h-4 w-4" />` y el texto "Exportar CSV". Mantener la condición de visibilidad `eventos.length > 0 &&` (línea 168) y el `onClick={exportToCSV}` sin cambios de comportamiento.

## Phase 3: Banner de error, empty states y banner de fuentes de datos

- [x] 3.1 En `dashboard/app/explore/page.tsx:182-186`, migrar el banner de error: contenedor de `bg-red-50 dark:bg-red-900/20 border-2 border-red-200 dark:border-red-800` a `border-2 border-severity-critical/30 bg-severity-critical/10` (patrón de `dashboard/app/page.tsx:57`), y el texto de `text-red-800 dark:text-red-200` a `text-severity-critical`. No importar ni modificar `AlertBanner.tsx` (decisión de design.md). Mantener la condición `error &&` y el contenido `{error}` sin cambios.
- [x] 3.2 En `dashboard/app/explore/page.tsx:198-206` (empty state de mapa), migrar: contenedor de `bg-gray-50 dark:bg-gray-900 border-2 border-dashed border-gray-300 dark:border-gray-700` a `border-2 border-dashed border-border bg-muted`; ícono `Search` de `text-gray-400` a `text-muted-foreground`; título "No hay resultados" de `text-gray-900 dark:text-white` a `text-foreground`; texto de ayuda de `text-gray-600 dark:text-gray-400` a `text-muted-foreground`. Conservar textos, ícono `Search` y la condición `eventos.length > 0 ? <AdvancedSeismicMap .../> : (...)` sin cambios.
- [x] 3.3 En `dashboard/app/explore/page.tsx:220-228` (empty state de lista), aplicar el mismo mapeo de clases que en 3.2, con ícono `List` en vez de `Search`. Conservar la condición `eventos.length > 0 ? <EventsTable .../> : (...)` sin cambios.
- [x] 3.4 En `dashboard/app/explore/page.tsx:235`, migrar el contenedor del banner de fuentes de `bg-blue-50 dark:bg-blue-900/20 border-2 border-blue-200 dark:border-blue-800` a `border-2 border-severity-low/30 bg-severity-low/10` (mismo vocabulario `--severity-*`, tono informativo — ver rationale en design.md).
- [x] 3.5 En `dashboard/app/explore/page.tsx:236-238`, migrar el título "Fuentes de Datos Utilizadas" de `text-blue-900 dark:text-blue-100` a `text-foreground`.
- [x] 3.6 En `dashboard/app/explore/page.tsx:241-247`, reemplazar el `<span>` de cada chip de fuente (`bg-blue-100 dark:bg-blue-900 text-blue-800 dark:text-blue-100 rounded-full text-sm font-medium`) por `<Badge variant="secondary">`. Mantener el `key={source}` y el texto `{source.toUpperCase()}` sin cambios, iterando sobre `filters.sources.map(...)` igual que hoy.
- [x] 3.7 En `dashboard/app/explore/page.tsx:249-251`, migrar el texto de ayuda ("Los eventos pueden estar deduplicados...") de `text-blue-700 dark:text-blue-200` a `text-muted-foreground`. Mantener la condición `eventos.length > 0 &&` del banner completo (línea 234) sin cambios.

## Phase 4: Verificación

- [x] 4.1 Ejecutar `cd dashboard && npm run build` (test_command de `openspec/config.yaml`) y confirmar que compila sin errores ni warnings de tipos nuevos. **Confirmado**: build limpio (`✓ Compiled successfully`), `/explore` 4.85 kB / 124 kB First Load JS.
- [x] 4.2 Ejecutar `rg -n "text-gray-|bg-gray-|border-gray-|bg-green-|bg-red-|bg-blue-|dark:" dashboard/app/explore/page.tsx` y confirmar cero coincidencias (criterio de cierre del proposal y del spec). **Confirmado**: cero coincidencias.
- [x] 4.3 Ejecutar `rg -n "bg-seismic-600" dashboard/app/explore/page.tsx` y confirmar cero coincidencias (no regresión del bug ya corregido, Requirement "Consistencia del toggle Mapa/Lista"). **Confirmado**: cero coincidencias.
- [x] 4.4 Navegación manual en `/explore`, estado "con resultados + vista Mapa": ejecutar una búsqueda que retorne eventos, confirmar que el panel de Controles/Estadísticas (contador, rango de magnitud), el toggle (Mapa activo con `bg-primary`), el botón "Exportar CSV" y el banner de fuentes se ven con la nueva paleta y sin regresión funcional. Confirmar que `AdvancedSeismicMap` se renderiza. **Confirmado con captura real** (Playwright, 49 eventos, M2.5-M4.5): panel, toggle, botón exportar y banner de fuentes con paleta correcta; mapa renderizado con marcadores y leyenda.
- [x] 4.5 Navegación manual en `/explore`, estado "con resultados + vista Lista": hacer click en el toggle "Lista", confirmar que `view` cambia, el botón "Lista" pasa a `bg-primary`, `EventsTable` se renderiza, y el resto del panel permanece consistente. **Confirmado con captura real**: toggle Lista activo en `bg-primary`, tabla de 49 eventos renderizada correctamente.
- [ ] 4.6 Navegación manual en `/explore`, estado "sin resultados": ejecutar una búsqueda que retorne 0 eventos, confirmar que aparece el banner de error con tokens `--severity-critical` mostrando "No se encontraron eventos con los filtros especificados", que el empty state correspondiente a la vista activa se muestra (ícono, título "No hay resultados", texto de ayuda), que el botón "Exportar CSV" NO se renderiza, y que el banner de fuentes NO se renderiza.
- [ ] 4.7 Navegación manual en `/explore`, estado "error de red": forzar o simular un fallo de `seismicAPI.searchEvents` (ej. cortando el backend momentáneamente) y confirmar que el banner de error muestra el mensaje de excepción con el mismo estilo `--severity-critical`, sin resultados parciales en `eventos`.
- [ ] 4.8 Repetir la verificación visual de 4.4-4.6 con dark mode activo (clase `dark` en el árbol), confirmando contraste adecuado en header, panel, toggle, banner de error, empty states y banner de fuentes sin depender de clases `dark:` explícitas.
- [ ] 4.9 Ejecutar `exportToCSV()` con eventos presentes (click en "Exportar CSV") y confirmar que el CSV descargado conserva las columnas exactas: ID, Fecha UTC, Latitud, Longitud, Profundidad, Magnitud, Tipo Mag, Lugar, Sentido, Revisado, Fuentes — sin cambios de formato respecto al comportamiento pre-migración.
