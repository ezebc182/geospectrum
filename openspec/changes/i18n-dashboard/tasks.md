# Tasks: i18n completo del dashboard (ES/EN) con next-intl

Convenciones de este archivo:

- **Cada fase = un batch** (una sesión enfocada de sub-agente de `sdd-apply`). El orden reproduce Decision 4 del design (backend primero, infra, luego módulos de UI, landing/invite al FINAL) — NO adelantar tareas de una fase posterior.
- **Cada fase deja la app 100% funcional y deployable** (Decision 9): los strings aún no migrados siguen en español hardcodeado y conviven con next-intl sin conflicto. Revertir una fase = revertir sus commits, nada más. La convivencia del patrón viejo (landing/invite con diccionarios caseros) dura hasta la Fase 7 inclusive.
- **AC** = criterio de aceptación, referenciando los escenarios de `specs/dashboard-ui/spec.md`, `specs/account-settings/spec.md` y `specs/auth/spec.md` de este change.
- **Regla de paridad transversal**: toda clave nueva se agrega a `messages/es.json` Y `messages/en.json` EN LA MISMA TAREA que la consume — el test de paridad (2.4) corre verde al cierre de CADA fase, nunca hay EN "pendiente".
- **Precondición dura** (proposal → Dependencies): mergear `feat/email-invitations` ANTES de arrancar la Fase 1 (comparte `InvitationsPanel`, `/invite`, emails, middleware).
- **Versión npm pinneada**: `next-intl@4.13.6` exacta, sin caret (verificada contra el registry el 2026-08-12 — Decision 1). Instalar con `npm install --save-exact`.
- **Gates**: frontend = `npx tsc --noEmit` + `vitest run` (NUNCA `npm run build` — regla del usuario); backend = `pytest` + `black` + `ruff` + `mypy`. Tests de API contra la base real — TimescaleDB local en puerto **5433, NO 5432** (memoria del proyecto).

## Phase 1: Backend — migración 011 + cadena de locale beta → invitación → emails

Independiente del frontend: al cerrar esta fase la API deployable ya persiste y propaga locale aunque ninguna UI lo mande todavía (campos opcionales con default — Decision 9).

- [x] 1.1 Crear `deploy/sql/migrations/011_user_and_beta_locale.sql` con el SQL EXACTO de design → Interfaces/Contracts: `users.locale TEXT NULL` + CHECK nombrado `users_locale_check (locale IS NULL OR locale IN ('es','en'))`; `beta_signups.locale TEXT NOT NULL DEFAULT 'es'` + CHECK `beta_signups_locale_check`; idempotente estilo 010 (`ADD COLUMN IF NOT EXISTS`, `DROP CONSTRAINT IF EXISTS` previo) y rollback documentado en comentarios. AC: correr el archivo dos veces seguidas no falla; el rollback comentado cubre ambas mitades (users y beta_signups) por separado. → **Hecho**: SQL idéntico al contrato del design; corrida doble verificada sin error (segunda pasada = puros NOTICE ... skipping).
- [x] 1.2 Aplicar la 011 contra el TimescaleDB local (puerto 5433). AC: `\d users` muestra `locale` nullable y todos los usuarios preexistentes quedan en NULL ("nunca eligió" — Decision 7); `\d beta_signups` muestra `locale NOT NULL DEFAULT 'es'` y las filas históricas valen `'es'` (escenario "Cuenta preexistente sin preferencia"). → **Hecho**: aplicada vía `docker exec timescaledb psql`; `\d` verificado — `users.locale` TEXT nullable (2 usuarios preexistentes, 0 con locale), `beta_signups.locale` NOT NULL DEFAULT 'es' (tabla vacía en local, default confirmado en el schema).
- [x] 1.3 Modelos Pydantic: en `src/models/user.py` agregar `locale: Optional[InvitationLocale] = None` a `UserProfile` y `UserProfileUpdate` (import directo de `src/models/invitation.py:22`, sin ciclos); en `src/models/beta.py` agregar `locale` a `BetaSignupRequest` y `BetaSignupItem`. **Atención — tensión spec vs design resuelta a favor de la spec**: `specs/auth` exige que un `locale` ausente O INVÁLIDO en `/beta-signups` caiga a `'es'` con 201 (nunca 400/422), pero un `Literal["es","en"]` pelado produce 422 — `BetaSignupRequest.locale` necesita un `field_validator(mode="before")` que colapse cualquier valor no soportado a `'es'`. En cambio `UserProfileUpdate.locale` queda ESTRICTO (el 422 ante `"fr"` es requirement explícito de `specs/account-settings`). AC: escenario "Caller sin locale o con valor inválido cae a español" (201 + fila `'es'`) y escenario "Valor no soportado es rechazado" (422 en PATCH profile) — comportamientos deliberadamente distintos, ambos por spec. → **Hecho, con UNA desviación del design**: el "import directo desde invitation.py, sin ciclos" era falso — `invitation.py:17` ya importa `UserRole` desde `user.py`, así que user.py→invitation.py armaba un ciclo real. Se invirtió la dirección: el par cerrado vive en `user.py` como `Locale = Literal["es","en"]` e `invitation.py` lo re-exporta como `InvitationLocale` (alias — cero cambios para los consumidores existentes). `beta.py` importa `InvitationLocale` de invitation.py (sin ciclo). Validator tolerante en `BetaSignupRequest` y Literal estricto en `UserProfileUpdate`, tal como pedía la tarea.
- [x] 1.4 `src/services/auth_service.py`: sumar `locale` al SELECT de `get_profile()` (línea ~757); `update_profile()` lo acepta gratis (el UPDATE parcial ya se construye con `model_dump(exclude_unset=True)` — Decision 7). NO tocar `/auth/me` ni los claims del JWT. AC: escenarios "Guardar la preferencia en inglés" (PATCH `{locale:'en'}` → GET devuelve `'en'`, resto del perfil intacto), "PATCH de otros campos no pisa la preferencia" y "El locale no viaja en el JWT ni en /auth/me". → **Hecho**: `locale` sumado al SELECT de `get_profile()` Y al RETURNING de `update_profile()` (el design solo mencionaba el SELECT, pero sin el RETURNING la respuesta del PATCH devolvería `locale: null` recién guardado — inconsistencia evitada). `/auth/me`, JWT y `export_user_data()` sin tocar (el export construye su UserProfile propio y reporta `locale` como default None — fuera de alcance de Fase 1, anotado).
- [x] 1.5 `src/main.py` — `POST /beta-signups` (línea ~682): persistir `locale` en el INSERT; el repost del mismo email NO sobreescribe el locale guardado (comportamiento de conflicto existente: no muta ni reenvía). Honeypot, rate limit y respuesta 201 anti-enumeración INTACTOS. AC: escenarios "Alta desde la landing en inglés persiste el locale" y "El repost no pisa el locale original". → **Hecho**: `INSERT (email, locale) ... ON CONFLICT DO NOTHING` — el DO NOTHING garantiza solo que el repost no pisa. Honeypot/rate-limit/201 sin cambios. `GET /beta-signups` también suma `locale` al SELECT (BetaSignupItem lo declara — sin esto el listado admin reportaría siempre el default).
- [x] 1.6 `src/main.py` — `approve_beta_signup` (línea ~763): el `SELECT ... FOR UPDATE` suma `locale`; propagarlo a `insert_invitation_row(conn, ..., locale=signup["locale"])` (el parámetro YA existe con default `'es'` en `invitation_service.py:157` — hoy simplemente no se pasa) y a `send_beta_approved_email(signup["email"], signup["locale"])`. Idempotencia intacta: re-aprobar no crea invitación nueva ni muta el locale de la pendiente. AC: escenarios "Aprobación de un signup EN produce invitación EN" y "Re-aprobar no muta la invitación existente". → **Hecho** tal cual: el parámetro existente de `insert_invitation_row` ahora se pasa; idempotencia verificada por test contra base real (re-approve → `already_approved: true`, una sola invitación, locale intacto).
- [x] 1.7 `src/services/email_service.py`: `send_beta_signup_emails(email, locale="es")` y `send_beta_approved_email(email, locale="es")` ganan el parámetro y eligen subject + cuerpo ES o EN completos (mismos `_layout`/`_paragraph`/`_button`; se ELIMINA la línea muted en inglés del template actual — era el parche monolingüe). El aviso interno al admin queda en español (tooling interno — MAY de la spec). Resiliencia intacta: emails post-commit, una falla de envío no deshace nada. AC: escenarios "Confirmación de lista de espera en inglés" y "Email de aprobación en el idioma del signup". → **Hecho**: firmas con `locale: InvitationLocale = "es"`, copy EN completo nuevo (subjects "You're on the GeoSpectrum waitlist" / "Your GeoSpectrum access is ready"), línea muted EN del template ES eliminada, aviso al admin en español, `_send` nunca-lanza intacto.
- [x] 1.8 Tests backend (contra Postgres real, patrón `test_invitations_api.py` — memoria del proyecto: verificar contra la base, no con mocks): (a) unit — `BetaSignupRequest` colapsa `"xx"`/ausente a `'es'`, `UserProfileUpdate` acepta `es|en` y el endpoint rechaza `"fr"` con 422; templates de email ES/EN por locale (subject y cuerpo); (b) integration — `PATCH /account/profile {locale}` persiste y `GET` lo devuelve, PATCH parcial no pisa, `GET /account/profile` de usuario preexistente devuelve `"locale": null`; `POST /beta-signups` guarda locale, repost no lo pisa; approve propaga locale a `invitations.locale` y re-approve no lo muta. AC: TODOS los escenarios de `specs/account-settings` y `specs/auth` cubiertos por nombre; suites existentes de beta/invitations sin regresión. → **Hecho**: `tests/unit/test_locale_models.py` (25 tests: validators tolerante/estricto + templates ES/EN capturando `_send`) y `tests/integration/test_locale_api.py` (10 tests contra Postgres real: perfil, beta, approve — AuthService REAL con `_LazyPool`, decode mockeado, Redis del rate limit fakeado para que un Redis local no meta 429 espurios). Cada test cita su Scenario por nombre; "El locale no viaja en el JWT ni en /auth/me" se cubre como garantía de shape (`CurrentUser`/`MeResponse` no declaran el campo — criterio de tipos del proyecto). Ajustes de no-regresión: filas fake de perfil en `test_auth_service.py` ganan `"locale": None` (el SELECT nuevo las lee), assert de shape de `UserProfileUpdate` suma `locale`, y el JSON exacto del perfil en `test_auth_api.py` suma `"locale": null`.
- [x] 1.9 Gate backend: `pytest` completo + `black` + `ruff` + `mypy` limpios en los archivos tocados. AC: verde total; la app deployable sin ningún cambio de frontend (Decision 9: aditividad en ambas puntas). → **Verde**: pytest completo 407 passed / 0 failed; black limpio; ruff limpio en lo tocado (los 5 E402 de `test_auth_api.py` pre-existen en HEAD, verificado); mypy `Success` en los 5 módulos de src tocados y `src/main.py` mantiene sus 57 errores pre-existentes (ninguno nuevo — cero menciones a locale/beta/email en el reporte). Los tests no están bajo el gate de mypy del proyecto (los existentes ya fallan `disallow_untyped_defs`).

## Phase 2: Infra next-intl + chrome (layout, Header, AppSidebar, UserMenu) + login

Primera fase de UI — el selector de idioma del header aparece ACÁ (design fase 1).

- [x] 2.1 `dashboard/package.json`: `"next-intl": "4.13.6"` exacta (`npm install --save-exact next-intl@4.13.6`); `dashboard/next.config.ts`: wrap con `createNextIntlPlugin('./i18n/request.ts')` (hoy son 10 líneas — el wrap es trivial, `reactStrictMode` y env se preservan). AC: install limpio sin conflicto de peers (peers verificados: `next ^12–^16`, `react ^16.8–^19`); `npx tsc --noEmit` verde. → **Hecho**: `"next-intl": "4.13.6"` sin caret en package.json, `npm ls` confirma 4.13.6 único, sin conflictos de peers; wrap `withNextIntl(nextConfig)` preservando `reactStrictMode` y `env`.
- [x] 2.2 Crear `dashboard/i18n/request.ts` según el contrato del design: cascada server-side (1) cookie `NEXT_LOCALE` si vale `es|en`, (2) primer idioma soportado de `Accept-Language`, (3) `'es'`; valor NO soportado en cualquier eslabón se trata como ausente y la cascada sigue, NUNCA rompe el render; mapping `FORMAT_LOCALES = { es: 'es-AR', en: 'en-US' }` como ÚNICO lugar del mapping app→formato (Decision 2); `formats` globales nombrados (`dateTime` medium/short/time — Decision 6). PROHIBIDO: tocar la base o verificar sesión acá (Decision 3, alternativa (a) descartada). AC: escenarios "Visitante nuevo con navegador en inglés" (Accept-Language en → EN), "Idioma no soportado cae al default" (fr → ES) y "Cookie con valor inválido no rompe la cascada" (cookie `xx` ignorada, sin página en blanco). → **Hecho**: contrato exacto (reutiliza `isAppLocale` de lib/locale.ts, sin DB ni sesión). Los 3 escenarios verificados contra `next dev` real con curl sobre `/login`: `Accept-Language: en` → `lang="en-US"` con copy EN; `fr` → `lang="es-AR"`; cookie `xx` + navegador es → `lang="es-AR"` con contenido renderizado (sin página en blanco); cookie `en` le gana a `Accept-Language: es`.
- [x] 2.3 Crear `dashboard/lib/locale.ts`: `type AppLocale = 'es' | 'en'`, `setLocaleCookie(locale)` (cookie `NEXT_LOCALE`, `path=/`, `max-age=31536000`, `SameSite=Lax`, NO httpOnly — la setea el cliente, es preferencia de UI, no secreto), `toAppLocale(bcp47)` (`'es-AR'` → `'es'`). Con unit tests Vitest de `toAppLocale` (casos: `es-AR`, `en-US`, `en`, `fr` → default). AC: firma exacta del contrato del design; tests verdes. → **Hecho**: firmas del contrato + extras `APP_LOCALES`/`DEFAULT_LOCALE`/`LOCALE_COOKIE`/`isAppLocale` (los consumen request.ts y el switcher — un solo origen de verdad). `lib/locale.test.ts`: 7 tests verdes (incluye case-insensitive y guard).
- [x] 2.4 Crear `dashboard/messages/es.json` + `messages/en.json` (seed con los namespaces de ESTA fase: `common`, `nav`, `auth` — ES es la fuente, los strings actuales se mueven tal cual sin re-redactar), `dashboard/messages/parity.test.ts` (Vitest: aplana ambos JSON, exige igualdad EXACTA de sets de claves en ambas direcciones + ningún valor vacío) y `dashboard/global.d.ts` (augmentation next-intl v4: `interface AppConfig { Messages: typeof es }`). AC: escenario "Divergencia de claves rompe la suite" — borrar una clave solo del EN hace fallar el test identificando la clave, restaurarla lo devuelve a verde; una clave inexistente en `t()` falla en compile-time. → **Hecho**: seed `common` (2) + `nav` (18) + `auth` (12) — 32 claves por idioma; AC demostrado en vivo (borré `nav.userMenu.logout` solo del EN → el test falló nombrando exactamente esa clave; restaurado → 4/4 verdes). `global.d.ts` augmenta `Messages` Y `Formats` (los formats nombrados también quedan tipados). Única re-redacción deliberada: el `aria-label="Toggle theme"` del Header estaba EN INGLÉS hardcodeado en la app ES — quedó `es: "Cambiar tema"` / `en: "Toggle theme"` (anglicismo corregido, anotado acá).
- [x] 2.5 `dashboard/app/layout.tsx`: `const locale = await getLocale()` → `<html lang={locale}>` (reemplaza el `lang="es"` hardcodeado) + `NextIntlClientProvider` montado POR FUERA de `Providers` (los mensajes no dependen de theme/auth/tooltip — Decision 1). `middleware.ts`, rutas y OAuth callbacks NO se tocan (verificar diff vacío sobre ellos). AC: con cookie `en` el HTML sale `lang="en-US"`; sin cookie y navegador ES, `lang="es-AR"`; cero cambios en `middleware.ts`. → **Hecho**: layout async + `getLocale()`; `NextIntlClientProvider` sin props (hereda locale/messages/formats del request config, patrón v4) envolviendo a `Providers`. AC verificado con curl contra `next dev` (cookie `en` → `lang="en-US"`; navegador es → `lang="es-AR"`); `git diff middleware.ts` vacío, cero rutas tocadas.
- [x] 2.6 Crear `dashboard/components/LocaleSwitcher.tsx` (reutilizable: header, Settings, landing, invite) y montarlo en `UserMenu`/`Header`: al cambiar → `setLocaleCookie()` + `router.refresh()` (SWR intacto, sin reload) y, SI hay sesión, `PATCH /account/profile {locale}` en paralelo best-effort (falla → se loguea, no bloquea ni revierte la UI); sin sesión NO llama a ninguna API de cuenta. `lib/api.ts`: tipos de perfil ganan `locale`. AC: escenarios "Cambio desde el header persiste en la cuenta", "La falla del PATCH no bloquea el cambio visual" y "Visitante anónimo cambia idioma sin tocar la API". → **Hecho**: dropdown (icono Languages + check en el activo) montado DIRECTO en el header del shell `app/(app)/layout.tsx` junto a NotificationBell — "acceso rápido en el header" de la spec pide visibilidad inmediata, no un item enterrado dentro del dropdown de UserMenu (decisión anotada). Elegir el idioma ya activo es no-op. Los tipos de perfil viven en `lib/types.ts` (no en api.ts, precisión sobre el enunciado): `UserProfile.locale: AppLocale | null` + `UserProfileUpdate.locale?: AppLocale`. Los 3 escenarios AC cubiertos por los tests de 2.8.
- [x] 2.7 Migrar el chrome a `useTranslations`: `Header.tsx`, `AppSidebar.tsx` (6 labels, ns `nav`), `UserMenu.tsx`, y `app/login/page.tsx` completo (11 strings, ns `auth`, INCLUIDO el mensaje de `?error=google_no_invitation` y los demás errores de OAuth). AC: escenario MODIFIED "Rechazo de invitación mostrado en inglés" — con locale efectivo `en` el mensaje "solo por invitación" sale en inglés, con `es` en español, y el botón de Google + form siguen operativos en ambos; alternar ES→EN cambia sidebar y login en caliente sin redirect a `/login` (sesión intacta — escenario "Alternar ES→EN en una página interna", parte chrome). → **Hecho** siguiendo Decision 5 (t() en componentes, nunca en constantes de módulo): las constantes `routes`/`ADMIN_ROUTES` de AppSidebar y `ROLE_LABEL` de UserMenu se mudaron ADENTRO del componente resueltas con `t()`; en login, `GOOGLE_OAUTH_ERROR_MESSAGES` pasó a un `switch` tipado sobre claves `auth.oauthErrors.*` y el estado guarda el CÓDIGO (no el mensaje resuelto) para que el cambio de idioma en caliente re-traduzca el error visible. Smoke real con curl: `/login` con Accept-Language en muestra "Invitation-only access"/"Enter the control room"/"Continue with Google". Header.tsx (legacy, hoy sin montar en ningún layout) migrado igual por completitud del ns `nav`.
- [x] 2.8 Tests de componente (Vitest + Testing Library): `LocaleSwitcher` escribe la cookie y llama `router.refresh()`; con sesión dispara el PATCH, sin sesión no. Gate de fase: `npx tsc --noEmit` + `vitest run` (paridad incluida) verdes. AC: el resto de la app sigue en ES hardcodeado y funciona igual — fase deployable y revertible por sí sola. → **Verde**: `components/LocaleSwitcher.test.tsx` (5 tests: cookie+refresh, PATCH con sesión, sin sesión NO llama API, falla del PATCH no bloquea y se loguea, idioma activo = no-op; el dropdown de Radix se abre con keydown Enter — jsdom no tiene PointerEvent real). Gate: `npx tsc --noEmit` limpio + `vitest run` completo 249/249 en 21 archivos (paridad incluida). Convivencia verificada: los módulos no migrados siguen en ES hardcodeado y la suite existente pasa sin ajustes; `next dev` levanta y sirve `/login` en ambos idiomas.

## Phase 3: Dashboard principal + eventos/filtros + áreas (design fase 2)

- [x] 3.1 Migrar `app/(app)/page.tsx` (15 strings: KPIs, títulos, estados — ns `dashboard`). AC: dashboard completo en EN al alternar, incluidos estados vacíos y de carga. → **Hecho**: 20 claves del ns `dashboard` (título, error de carga + fallback, aviso de fuentes, 4 KPIs con sus subtítulos parametrizados, selector de cadencia, botón de refresco, título del mapa, badge de conteo, aria del toggle del panel, "Eventos Recientes"). Dos precisiones sobre el enunciado: (a) el `formatTimeAgo` de `lib/utils.ts` (hardcodea "hace X min" en español) se REEMPLAZA acá por `useFormatter().relativeTime()` — el helper sigue existiendo para los call-sites de fases posteriores, esta página ya no lo importa; (b) el badge de conteo decía `"{n} of {m} events in map area"` EN INGLÉS hardcodeado dentro de la app en español — quedó bilingüe de verdad (`dashboard.eventsInMapArea`), anglicismo corregido y anotado.
- [x] 3.2 Migrar `EventsTable` + `EventFiltersBar` (15) + `lib/event-filters.ts` (ns `events`): `TIME_PERIODS` PIERDE `label` y queda `{ value: TimePeriod }[]`; el componente resuelve `t(\`periods.${value}\`)`; las funciones de filtrado puras NO cambian de firma (Decision 5 — ningún `lib/*.ts` importa next-intl). Ajustar los tests existentes de `event-filters` a la forma nueva. AC: filtros y períodos en ambos idiomas; tests de lib verdes sin montar DOM. → **Hecho**: `TIME_PERIODS` quedó `{ value: TimePeriod }[]`, `event-filters.ts` sigue sin importar next-intl y las firmas de filtrado intactas. En `EventsTable`, `OPTIONAL_COLUMNS` pasó de `label` a `labelKey` (misma regla: la constante de módulo no llama a `t()`). Dos frases que se armaban por concatenación pasaron a claves completas para no atar la traducción al orden del español: el contador "N de M eventos" usa `t.rich` con tags `<m>/<n>` (conserva las clases `font-data`) y "Mostrando X de Y eventos [filtrados]" se partió en `showing`/`showingFiltered` (en inglés el adjetivo va antes del sustantivo). Tests: `event-filters.test.ts` suma un describe de forma de `TIME_PERIODS` (sin DOM); `EventsTable.test.tsx` ahora envuelve en `NextIntlClientProvider` con los mensajes ES REALES (no un stub, así los asserts siguen mirando el texto del usuario) y suma 3 tests bilingües (filtros/períodos en EN y ES, nombres de lugar sin traducir).
- [x] 3.3 Migrar `FilterPanel.tsx` (19 strings, ns `filters`). AC: todos los controles del panel (labels, placeholders, `aria-label`) salen del diccionario. → **Hecho**: 24 claves (`filters.*`), incluidos los `aria-label` de colapsar/expandir y las 7 ventanas del `<select>` de período. **Fuera de alcance a propósito**: `DATA_SOURCES` de `lib/map-layers.ts` (name + description de cada fuente) — es del ns `map`, Fase 4; hasta entonces esa lista sigue en español, que es la convivencia prevista por Decision 9.
- [x] 3.4 Migrar `AreaSelector.tsx` (17) + `AreaHeader` + `lib/use-area-refresh.ts` + `lib/area-groups.ts` (ns `areas`): `AREA_GROUP_LABELS` se ELIMINA, `groupAreas()` devuelve `{ id, areas }` y el componente deriva el label con `t(\`groups.${id}\`)`; el `localeCompare(..., 'es')` de la línea 134 pasa a recibir el locale activo por parámetro. Los NOMBRES de áreas del backend NO se traducen (son datos — contexto del delta). AC: grupos y búsqueda en ambos idiomas; nombres de áreas idénticos en ES y EN; tests de `area-groups` ajustados y verdes. → **Hecho**: `AREA_GROUP_LABELS` borrada, `AreaGroup` perdió `label`, `groupAreas(areas, locale)` recibe el locale de `useLocale()` (formato es-AR/en-US) y `DEFAULT_AREA_LABEL` pasó a `t('defaultArea')` — nótese que ese label también alimenta el filtro de búsqueda, así que buscar "default"/"defecto" sigue funcionando en el idioma activo. `AreaHeader` migró su única etiqueta. **`lib/use-area-refresh.ts` NO se tocó**: revisado, no tiene ni un solo string user-facing (es puro manejo de eventos + estado), así que no había nada que extraer. Tests: `area-groups.test.ts` ajustado a la firma nueva + 2 tests nuevos (el resultado no expone `label`; el orden usa el locale recibido — verificado con `sv`, donde "Ä" ordena después de "Z", porque es-AR y en-US dan el mismo orden y no distinguirían un locale clavado); `area-search.test.ts` ajustado (fixtures sin `label`); nuevo `components/AreaSelector.test.tsx` (3 tests: trigger/opción por defecto en EN y en ES, grupos traducidos conviviendo con nombres de área intactos).
- [x] 3.5 Gate de fase: paridad + `vitest run` + `npx tsc --noEmit` verdes; smoke visual ES/EN de dashboard y explore (textos EN más largos en filtros/tablas). AC: escenario "Alternar ES→EN en una página interna" completo — `/explore` con filtros aplicados cambia de idioma preservando los filtros. → **Verde**: `npx tsc --noEmit` limpio y `vitest run` completo 259/259 en 22 archivos (paridad incluida: 128 claves por idioma tras esta fase, contra 33 al cerrar la Fase 2). Auditoría de residuos: `rg '[áéíóúñ¿¡]'` sobre los 8 archivos migrados devuelve SÓLO comentarios, cero literales user-facing. **Desviación anotada del smoke**: el visual ES/EN de `/` y `/explore` NO se pudo hacer acá — esas rutas están detrás del middleware de sesión y sin cookie de sesión devuelven 307 a `/login`, así que un curl no alcanza. Lo verificable sin sesión SÍ se corrió contra `next dev` real: `/login` sale `lang="es-AR"` / `lang="en-US"` según `Accept-Language` y el payload EN serializado ya incluye los namespaces nuevos (`Seismic Monitoring Dashboard`, `Search by zone`, `Search Filters`, `Area of interest`). La cobertura bilingüe real de esta fase queda en los tests de componente (EventsTable y AreaSelector renderizados en ES y EN); el smoke visual con sesión y el escenario completo de `/explore` preservando filtros quedan para la checklist de la Fase 8.3. Proceso de `next dev` levantado para el smoke: matado (puerto 3111 libre).

## Phase 4: Mapas + globo + charts + spectrograms (design fase 3 — los casos duros de Decision 5)

- [x] 4.1 Migrar `AdvancedSeismicMap.tsx` (14: leyendas, capas, controles + popups HTML de las líneas ~234/380, ns `map`): el `useEffect` que construye los markers captura un objeto `popupLabels` memoizado desde `useTranslations`, y **`locale` entra a las deps del effect** para regenerar los popups al cambiar idioma. Atención a la trampa documentada del proyecto (efecto que lee un ref sin tenerlo en deps corre una vez y nunca más): las labels van POR VALOR en deps, no por ref. AC: escenario "El cambio alcanza strings generados fuera de JSX" — un popup abierto tras el cambio de idioma sale en el idioma nuevo. → **Hecho** con una reestructura necesaria: las CIUDADES vivían dentro del efecto de creación del mapa (corre una vez; recrearlo pisaría zoom/capas/selección), así que se mudaron a un efecto propio con deps `[mapInstance, showCities, popupLabels, locale]` — popups regenerados sin destruir el mapa. El efecto de eventos suma `popupLabels` (memoizado, por valor) + `locale` + `format` a sus deps. Extras sobre el enunciado: (a) la fecha del popup dejó `formatDateTime` (es-AR fijo) por `format.dateTime(..., 'medium')` — adelanta ESTE call-site de 6.3, la lib no se tocó; (b) `BASE_LAYERS` de `lib/map-layers.ts` perdió `name` (Decision 5): el control resuelve `t(\`baseLayers.${key}\`)` con `BaseLayerId` tipado; (c) la leyenda de magnitudes NO se tocó — sus labels ("M < 3.0") son neutrales. `N/A` reutiliza `common.notAvailable`.
- [x] 4.2 Migrar `SeismicMapWithCities.tsx` (13: popups líneas ~237/300 y controles) con el mismo patrón de 4.1. AC: ídem 4.1 para el mapa live. → **Hecho**, mismo patrón (estado `mapInstance` nuevo + ciudades en efecto propio + deps por valor en eventos) más un caso extra: el `L.control.layers` con 'Terreno/Calles/Satélite' es un control de Leaflet (no JSX), así que vive en su PROPIO efecto `[mapInstance, t, locale]` que remueve el control viejo y monta uno con los textos del idioma activo; las tile layers quedan en `baseLayersRef` (se crean una vez, solo el control se rehace). "Ciudades" de la leyenda JSX → `map.legendCities`; los rangos "M < 4.0" quedan (neutrales); nombres de ciudades/países son datos y no se traducen.
- [x] 4.3 Migrar el globo 3D + `GlobeEventPanel` + labels de `lib/globe-data.ts` (ns `globe`): las funciones que arman labels para el canvas reciben los strings YA traducidos por parámetro desde el componente con hook (Decision 5). AC: panel y labels del globo en ambos idiomas; `globe-data.ts` sin imports de next-intl. → **Hecho**: `eventsToPoints(eventos, labels: GlobePointLabels)` — parámetro OBLIGATORIO a propósito (la dependencia del idioma queda visible en la firma; un default escondería español residual) y `globe-data.ts` sigue sin next-intl; `SeismicGlobe` pasa `t('unknownLocation')` con `t` en las deps del memo (tooltips del canvas regenerados al cambiar idioma) y traduce los aria de zoom/reset. `GlobeEventPanel`: todas las filas + botón compartir guardando el OUTCOME (no el texto resuelto) para que el cambio de idioma re-traduzca un estado ya mostrado; su fecha migró a `format.dateTime(..., 'medium')` (call-site; `lib/utils` intacta). También migraron **`MagnitudeScale`** (lo renderiza el panel): `TRAMOS` cambia `etiqueta` por `id: MagnitudeBand` y `tramoDe()` devuelve la clave que el componente traduce (`globe.scale.bands.*`); y **`app/(app)/globe/page.tsx`** completo ("el globo 3D" incluye su vista): título, subtítulo con `{count}`, refresh, error y el esqueleto de carga — que pasó de string en el módulo a componente `GlobeSkeleton` con hook para que el fallback de `next/dynamic` también se traduzca.
- [x] 4.4 Migrar `MagnitudeTimeChart.tsx` (muere el `'es-ES'` de la línea 47: `tickFormatter` inline con `useFormatter().dateTime(...)`) + strings de spectrograms (ns `charts`). AC: escenario "La tabla de eventos re-formatea fechas al cambiar de idioma" (parte charts) — ejes y tooltips en formato `en-US` con la UI en EN. → **Hecho**: el `'es-ES'` murió (tick con `format.dateTime` inline hour/minute) y el tooltip también dejó `formatDateTime` por `format.dateTime(..., 'medium')`; título y nombres de ejes/serie al ns `charts`. Se sumó `DepthDistributionChart` (el otro chart Recharts de la fase; sus bins "<70 km" son neutrales). Spectrograms (`charts.spectrogram.*`): `SpectrogramViewReal` guarda el CÓDIGO de error (`noNearbyStation|connectionError`) y traduce al render — el cambio de idioma re-traduce un error visible; "Actualizado:" migró de `toLocaleTimeString()` (locale del runtime) a `format.dateTime(..., 'time')`, ídem `LiveSpectrogramCanvas`; `SortableSpectrogramCard` traduce los titles del toggle Vivo/24h, arrastre y eliminar. `DATA_SOURCES` de `lib/map-layers.ts` quedó `{ id, name }` (la sigla es dato) y la descripción vive en `map.sources.*` — `FilterPanel` la traduce con `useTranslations('map')`, cerrando el "fuera de alcance" anotado en 3.3. Los `console.error` quedan en español (dev-facing, mismo criterio que el aviso al admin de Fase 1).
- [x] 4.5 Test de componente: los popups de Leaflet se regeneran al cambiar el locale (Testing Strategy del design). Gate de fase + smoke visual ES/EN de mapas y globo. AC: paridad, tsc y vitest verdes. → **Verde**: `components/map-locale-popups.test.tsx` (4 tests, cubre AMBOS mapas: ciudades ES→EN sin español residual, eventos con labels + fecha re-formateada a "Aug" en EN, y sentido/preliminar/fallback de lugar traducidos). Nota técnica: el test usa Leaflet REAL con spy en `Layer.prototype.bindPopup` + renderer inerte stubbeado en `Map.prototype.getRenderer` — mockear el módulo `leaflet` (CJS externalizado) producía DOS instancias (mock y real) según el tick de cada `import('leaflet')`, y jsdom no implementa `createSVGRect`/canvas, así que los factories de renderer reales devuelven null y cualquier Path revienta; el spy sobre la instancia real captura todos los bindeos vengan del efecto que vengan. `lib/globe-data.test.ts` ajustado a la firma nueva + 1 test nuevo (el fallback sale del parámetro, no de la lib). Gate: `npx tsc --noEmit` limpio y `vitest run` completo **264/264 en 23 archivos** (paridad incluida: 203 claves por idioma tras esta fase, contra 128 al cerrar la 3 — verificado aplanando ambos JSON: sets idénticos). Smoke contra `next dev` real: `/login` con Accept-Language en sirve `lang="en-US"` y el payload EN ya incluye los ns nuevos ("Seismic Globe", "Plate Boundaries", "Magnitude vs Time"); el visual autenticado de mapas/globo queda para la checklist 8.3 (misma limitación de sesión anotada en 3.5). Proceso `next dev` matado (puerto 3111 libre). Auditoría de residuos sobre los archivos tocados: solo comentarios, datos (ciudades/países) y `console.error` — cero literales user-facing; los `'es-AR'` restantes (`lib/utils.formatDateTime`, paneles admin) pertenecen a las Fases 6 y 5, como estaba previsto.

## Phase 5: Settings + admin + `LocaleSync` (design fase 4)

- [x] 5.1 `components/settings/ProfileSection.tsx`: select de idioma (reutiliza `LocaleSwitcher` o su lógica) reflejando la preferencia actual del perfil; el cambio aplica cookie+refresh Y persiste vía el PATCH existente. Migrar el resto de strings de la sección. AC: escenario "Settings muestra y edita la preferencia". → **Hecho**: se reutilizó la LÓGICA del switcher (cookie + `router.refresh()` + PATCH best-effort con el mismo `console.error`), no su UI — en Settings es un select NATIVO con label y hint (`settings.profile.language`/`languageHint`), coherente con los otros selects nativos del proyecto (memoria: Radix dropdown pelea con inputs). El select muestra `profile.locale ?? idioma efectivo` (nunca vacío con preferencia null) más un `pendingLocale` optimista para que no "revierta" mientras el PATCH está en vuelo; el PATCH exitoso reporta al padre vía `onProfileUpdate`. El cambio aplica EN CALIENTE, fuera del submit del form (que sigue mandando solo full_name/address/phone — el parcial no pisa la preferencia). Coherencia con el header: ambos derivan de `useLocale()`, se sincronizan solos tras el refresh. Página `SettingsPage` también migrada (título + error de carga como booleano re-traducible).
- [x] 5.2 Crear `components/LocaleSync.tsx` y montarlo en el layout de `(app)`: cuando `GET /account/profile` hidrata con `locale` NO nulo y NO existe cookie `NEXT_LOCALE`, setea la cookie a `users.locale` + `router.refresh()` UNA sola vez; si la cookie existe, NO hace nada (la elección del dispositivo gana — Decision 3). AC: escenarios "Sin cookie, la preferencia de cuenta decide", "La cookie explícita gana sobre la preferencia de cuenta" y "Preferencia elegida en un dispositivo aparece en otro" (dispositivo B sin cookie renderiza EN desde el primer paint post-login). → **Hecho**: con cookie presente NI SIQUIERA pide el perfil (chequeo client-side previo — cero requests extra en el caso común); sin cookie, `getProfile()` una vez con guard por ref (sobrevive al doble-invoke de StrictMode) y re-chequeo de la cookie AL RESOLVER (si el usuario eligió idioma mientras el fetch volaba, su elección gana). Falla del perfil = silenciosa (la cascada server-side ya renderizó). Se agregó `getLocaleCookie()` a `lib/locale.ts` (cookie inválida = ausente, misma tolerancia que request.ts). Nota de precisión sobre el AC: "desde el primer paint" es la semántica de Decision 3 tal cual — el primer paint sale de Accept-Language y LocaleSync corrige en el primer paint POST-hidratación (flash posible y aceptado por design cuando navegador y cuenta difieren).
- [x] 5.3 Migrar el resto de settings: `TwoFactorSection` (14: pasos, errores, backup codes), `ExportData`, `DangerZone` (ns `settings`). AC: flujo 2FA completo en ambos idiomas, incluidos mensajes de error del cliente. → **Hecho** con el patrón establecido de guardar el CÓDIGO del error (no el texto): `TwoFactorSection` guarda `{ key, message? }` (el `err.message` del backend en el setup conserva prioridad — errores del backend fuera de alcance por decisión del usuario), Export/Profile guardan booleanos. Dos decisiones anotadas: (a) `DangerZone`: la palabra de confirmación dejó de ser constante de módulo (`ELIMINAR`) y sale del diccionario (`ELIMINAR`/`DELETE`) — la comparación es contra la palabra del idioma ACTIVO, así el placeholder, el dialog (`t.rich` con `<strong>{word}</strong>`) y la validación nunca se desalinean, ni siquiera cambiando de idioma con el dialog abierto; (b) `ExportData`: el NOMBRE del archivo descargado también es superficie user-facing → `settings.export.fileName` (`geospectrum-datos-…`/`geospectrum-data-…`).
- [x] 5.4 Migrar admin: `InvitationsPanel.tsx` (26 strings + fecha `'es-AR'` de la línea 93 → `useFormatter`), `BetaSignupsPanel.tsx` (+ fecha línea 26), access (ns `admin`). AC: tablas, dialogs y estados del panel admin en ambos idiomas; fechas según locale activo. → **Hecho**: ambos `formatDate` con `'es-AR'` fijo murieron — `useFormatter().dateTime(date, DATE_OPTIONS)` con las MISMAS opciones (día + mes corto + hora), locale activo. Decision 5 aplicada a fondo: `ROLE_LABELS`/`STATUS_LABELS`/`LOCALE_LABELS` y el `label` de `TABS` (access) se mudaron al diccionario (`admin.roles.*` con labels largas — distintas de las cortas de `nav.roles`, ambas por diseño; el selector de idioma del email reusa `common.localeNames`, mismas labels que el switcher). El refactor grande: `StepResult { label, detail }` (texto resuelto en estado) pasó a `StepOutcome` discriminado por `kind` + datos crudos, y `createErrorMessage` → `createErrorKey` (función pura que devuelve CLAVES) — el texto se resuelve al render, así el cambio de idioma re-traduce resultados y errores YA visibles (mismo patrón que los errores OAuth del login y el share del globo). `BetaSignupsPanel` guarda el email de la falla (no el mensaje) y el badge de pendientes usa plural ICU (`{count, plural, one/other}` en es; en EN "pending" no flexiona). Dialogs de revocación con `t.rich` (`<mono>{email}</mono>`).
- [x] 5.5 Test de componente: `LocaleSync` actúa SOLO sin cookie (con cookie presente no toca nada ni refresca). Gate de fase + smoke visual ES/EN de settings y admin. AC: paridad, tsc y vitest verdes. → **Verde**: `components/LocaleSync.test.tsx` (5 tests: siembra+refresh sin cookie; con cookie NO pide el perfil ni refresca ni pisa; locale null = nada; cookie inválida `xx` = ausente y reconcilia; falla del perfil silenciosa). Gate: `npx tsc --noEmit` limpio y `vitest run` completo **269/269 en 24 archivos** (paridad incluida: 323 claves por idioma tras esta fase, contra 203 al cerrar la 4 — sets idénticos verificados aplanando ambos JSON). Auditoría de residuos sobre los archivos tocados: solo comentarios (cero literales user-facing con caracteres españoles). Smoke contra `next dev` real: la app levanta y `/login` sirve `lang="en-US"`/`lang="es-AR"` según Accept-Language con los diccionarios nuevos parseando sin error; el visual autenticado de settings/admin queda para la checklist 8.3 (misma limitación de sesión anotada en 3.5 y 4.5). Proceso `next dev` matado (puerto 3111 libre).

## Phase 6: Onboarding/tour + toasts + share + notificaciones + fechas de `lib/utils` (design fase 5)

- [x] 6.1 `components/onboarding/tour-steps.ts`: la constante `TOUR_STEPS` pasa a `buildTourSteps(t: Translator): DriveStep[]` (el import type-only de driver.js se conserva — no arrastra runtime al bundle); `useTour` la invoca con el `t` del ns `onboarding`; migrar `OnboardingWizard` (~25 strings entre wizard y 8 pasos title/description). Los `data-tour-id` y el comportamiento de persistencia NO se tocan. AC: escenario "Invitado EN ve el tour en inglés en su primer login" — pasos en inglés, `POST /auth/me/onboarding-complete` idéntico; `e2e/onboarding.spec.ts` existente VERDE sin modificar sus asserts de comportamiento. → **Hecho** con dos precisiones de contrato: (a) el tipo del parámetro NO es `(key: string) => string` como decía el design — el `t` de next-intl acepta solo las claves del namespace (más angosto que `string`) y por contravarianza no asignaría; quedó `OnboardingTranslator = ReturnType<typeof useTranslations<'onboarding'>>`, import TYPE-ONLY de next-intl (cero runtime, mismo criterio que el de driver.js); (b) el `progressText` de driver.js usa `{{current}}/{{total}}`, que no es ICU válido — la clave se guarda como `"{current} de {total}"` y useTour le pasa los placeholders de driver.js COMO VALORES, resolviendo a `"{{current}} de {{total}}"` con el orden de palabras del idioma. Los pasos se construyen al ARRANCAR cada tour (no en el mount): cambio de idioma previo al disparo sale traducido. Botones Siguiente/Anterior/Listo también al diccionario. `data-tour-id` y persistencia intactos (diff de OnboardingGate = vacío). E2E existente VERDE (5.7s) tras UNA corrección de ENTORNO, no de asserts: el chromium de Playwright manda `Accept-Language: en-US` por defecto y ahora el wizard (correctamente) salía en inglés — `playwright.config.ts` declara `locale: 'es-AR'` (el browser del test representa a un usuario rioplatense). Test de componente nuevo `OnboardingWizard.test.tsx` (4 tests: paso 1 ES con el copy EXACTO que asserta el e2e, paso 1 EN, paso 2 EN con Start the tour, Saltar → onFinished).
- [x] 6.2 Migrar `NotificationBell` (ns `notifications`), toasts (se disparan desde componentes client → `t()` directo, sin caso especial), `lib/share-event.ts` (`buildShareText`/`shareEvent` ganan parámetro `messages: ShareMessages` provisto por el componente — el título "Monitor sísmico" de la línea 88 sale del diccionario) y labels restantes de `lib/types.ts`. Ajustar los tests de `share-event` a la firma nueva (siguen sin montar DOM). AC: escenario "Un usuario EN no ve español residual en flujos secundarios" — toasts, confirmaciones y `aria-label` en inglés; tests de lib verdes. → **Hecho con dos no-ops verificados**: (a) TOASTS NO EXISTEN en el codebase (`rg -i 'toast|sonner'` sobre app/components/lib/package.json → cero matches) — el enunciado del design era preventivo; (b) `lib/types.ts` NO tiene labels user-facing restantes (solo tipos y `ROLE_LEVEL` numérico) — nada que extraer. Lo real: `NotificationBell` migrado completo (aria del trigger con `{count}`, chips como `ALERT_TYPE_IDS: AlertType[]` + `t(\`types.${id}\`)` por Decision 5, estados vacíos, y DOS mejoras del texto original anotadas: el header de cada alerta mostraba `tipo.replace('_',' ')` crudo — ahora usa las mismas claves `types.*`; y "N evento(s) relacionado(s)" pasó a plural ICU real, precedente del badge de 5.4). `ShareMessages`: los mensajes parametrizados (headline, depth) son FUNCIONES, no strings con tokens — la interpolación la hace ICU en el componente (`tShare('headline', {magnitude, place})`) sin reinventar un replace() en la lib, y el orden de palabras queda libre por idioma ("Sismo M5.2 — X" vs "M5.2 earthquake — X"); `shareEvent(evento, messages, url?)` y `formatUtc` recibe `unknownDate`. `GlobeEventPanel` arma los messages con `useMemo` desde el ns `share`. Tests de `share-event` ajustados con fixture `MESSAGES_ES` de textos ES REALES (criterio EventsTable: se asserta lo que ve el usuario) + 1 test nuevo de orden de palabras EN; siguen sin DOM. `alerta.descripcion` viene del backend — fuera de alcance por decisión del usuario.
- [x] 6.3 `lib/utils.ts`: `formatDateTime` migra a `useFormatter`/locale por parámetro (muere el `'es-AR'` de la línea 67); `formatDateTimeCompact` (YYYY-MM-DD HH:MM:SS estilo USGS) **NO se localiza** — es formato técnico ordenable, idéntico en ambos idiomas (Decision 6, deliberado). AC: escenario "La tabla de eventos re-formatea fechas al cambiar de idioma" completo; el compact queda byte-idéntico en ES y EN. → **Hecho**: `formatDateTime(isoString, locale)` — el locale entra por parámetro (el que expone `useLocale()`, es-AR/en-US; el mapping sigue viviendo solo en request.ts, Decision 2). Su ÚNICO caller vivo era `SeismicMap.tsx`, componente LEGACY sin montar en ningún layout — migrado igual por completitud (criterio Header.tsx de 2.7): popups con las claves `map.popup` existentes + `locale`/`t` en las deps del efecto de markers. **`formatTimeAgo` se ELIMINÓ**, no se localizó: hardcodeaba "hace X min" y su último call-site murió en 3.1 (`useFormatter().relativeTime()`) — quedaba código muerto que la auditoría 7.6 marcaría; cero referencias en tests ni componentes (verificado con rg). El compact intacto, byte-idéntico. Nota: el escenario "la tabla re-formatea fechas" ya estaba cubierto por 4.1/4.4 en los call-sites de componentes (la tabla usa el compact deliberadamente neutro); esta tarea cerró el residuo de la lib.
- [x] 6.4 Gate de fase: paridad + `vitest run` + `npx tsc --noEmit` verdes; smoke del wizard completo en EN (disparo, saltar, completar). AC: en este punto TODO el dashboard autenticado + login cumplen cobertura total; solo landing e invite siguen con el patrón viejo (convivencia prevista). → **Verde**: `npx tsc --noEmit` limpio y `vitest run` completo **274/274 en 25 archivos** (paridad incluida: **388 claves por idioma** tras esta fase, contra 323 al cerrar la 5 — sets idénticos verificados aplanando ambos JSON). **E2E de onboarding corrido DE VERDAD contra el stack local** (timescaledb docker 5433 + uvicorn 8000 levantado para la corrida y matado después, puertos 8000/3008 verificados libres): 1 passed en 5.7s — wizard visible, 4 anclas `data-tour-id` presentes, Saltar → `POST /auth/me/onboarding-complete` 204 + fila persistida en la base + no reaparece al recargar. El smoke EN del wizard quedó cubierto por los tests de componente nuevos (paso 1/2 y Saltar en EN); el visual EN autenticado completo (tour driver.js incluido) va a la checklist 8.3 como en las fases anteriores. Auditoría de residuos sobre los archivos tocados: solo comentarios, cero literales user-facing.

- [x] 6.5 **(Agregada en apply — hallazgo de la Fase 4, asignación del orquestador: ninguna fase nombraba estas páginas shell.)** Migrar `app/(app)/analytics/page.tsx` (ns `analytics`: título, error de carga, título de la tabla), `app/(app)/explore/page.tsx` (ns `explore`: ~24 claves — título/subtítulo, contador de resultados, toggle Mapa/Lista, exportar, estados vacíos, errores, fuentes, y el CSV completo) y `app/(app)/live/page.tsx`. AC: cero literales ES en las tres páginas; suite verde. → **Hecho**: `/live` resultó ser un puro `redirect('/')` (fusión con el dashboard del 2026-08-05) — CERO strings, nada que migrar. `analytics` migrado directo (3 claves + spinner sin texto). `explore`: el error de búsqueda pasó de guardar el TEXTO a un `SearchError` discriminado (`noResults | failed`) traducido al render — cambio de idioma re-traduce el error visible (patrón OAuth/admin); el `err.message` del backend se muestra crudo (backend fuera de alcance, decisión del usuario) con fallback `searchError`; el contador "Resultados: N eventos" usa `t.rich` con tag `<n>` (precedente `events.matchedOfTotal`) para no atar el orden de palabras; y los ENCABEZADOS + Sí/No del CSV exportado también salen del diccionario — son superficie user-facing, mismo criterio que el fileName de ExportData en 5.3 (además "Sí" con tilde reventaría la auditoría 7.6). El nombre del archivo CSV (`seismic_events_*.csv`) quedó neutro a propósito (identificador técnico, ya estaba en inglés).

## Phase 7: Landing + `/invite` a next-intl, retiro de diccionarios y auditoría final (design fase 6)

- [x] 7.1 Mapear `LANDING_COPY` → ns `landing.*` e `INVITE_COPY` → ns `invite.*` en AMBOS json: misma jerarquía, textos idénticos **carácter por carácter** (la interfaz TS que forzaba paridad se reemplaza por el test de paridad + augmentation — Decision 8). AC: escenario "Paridad de contenido post-migración" — diff de textos visibles contra el copy previo = vacío para ES y para EN; test de paridad verde. → **Hecho por construcción, no a mano**: un script one-shot (`node --experimental-strip-types`) importó `LANDING_COPY`/`INVITE_COPY` de los TS reales y los volcó a ambos json — cero riesgo de typo, paridad carácter por carácter garantizada. Las listas quedaron como ARRAYS en el json (misma jerarquía: `landing.how.steps`, `features.items`, `invite.accept.strengthLevels`) y el flattener de `parity.test.ts` se extendió para aplanarlas con índice numérico (la paridad ahora exige también el mismo largo de lista). Tres deltas deliberados: (a) `invite.localeSwitcherAria` NO se migró — su UI (el toggle propio de la página) muere en 7.4 y el switcher global trae su aria de `common.language`; (b) `invite.accept.googleHint` pasó de par `[prefijo, sufijo]` a UNA clave ICU con `<mono>{email}</mono>` (texto visible idéntico, patrón t.rich del repo); (c) se AGREGÓ `landing.meta` (title/description del `<head>`, EN nuevo) — la Metadata estática ES de `app/landing/page.tsx` era superficie user-facing que ninguna fase cubría, ahora sale de `generateMetadata()` + `getTranslations`.
- [x] 7.2 Migrar los componentes de landing: `LandingContent.tsx` abandona `useState` + `localStorage['landing-locale']` (líneas 27-41) por la mecánica cookie+refresh común; los hijos consumen hooks en vez de la prop `copy` — **cero cambios de markup/clases** (el diff por componente debe ser "prop → hooks", nada más). La preferencia vieja de localStorage NO se migra (Decision 3, pérdida aceptada). AC: escenario "La elección en la landing se propaga al login" — toggle EN en landing → `/login` en inglés sin re-elegir; `relativeTime` casero reemplazado por `useFormatter().relativeTime()`. → **Hecho**: `LandingContent` quedó dueño solo del TOGGLE (`setLocaleCookie` + `router.refresh()`, sin PATCH — la landing es anónima); `LandingHero`/`LandingSections`/`LandingFooter` perdieron la prop `copy` y consumen `useTranslations('landing')` con markup intacto. De paso murió el flash de hidratación documentado en el viejo header del componente (estado inicial `'es'` fijo): ahora server y cliente coinciden porque el locale viene de la cascada server-side. `buildSpotlightCard` recibe `depthShort` y el tiempo relativo YA resueltos por parámetro (Decision 5 — el DOM imperativo del spotlight no toca hooks); ticker/stats/infocard usan `format.relativeTime()` (formato distinto al casero "hace 12 min" → "hace 12 minutos", cambio prescripto por la tarea). Las listas de `LandingSections` se leen con `t.raw(...)` tipado local (`TitledItem[]`).
- [x] 7.3 Formulario de beta de la landing (`LandingFooter.tsx`): envía el locale activo; `lib/api.ts`: `signupBeta(email, website, locale)`. AC: escenario "El signup de beta lleva el idioma elegido" — el POST incluye `locale: 'en'` con la landing en inglés (la persistencia backend ya está verde desde la Fase 1). → **Hecho**: firma exacta del contrato del design (`locale: AppLocale`, import type de `lib/locale`), el body del POST suma `locale` y el footer lo deriva de `toAppLocale(useLocale())`. Honeypot y manejo de errores del form sin cambios.
- [x] 7.4 Migrar `app/invite/[token]/page.tsx`: hooks en vez de `INVITE_COPY`; al validar el token, si NO hay cookie `NEXT_LOCALE`, la página siembra la cookie con `validate.locale` + refresh (si el visitante ya tenía cookie, SU elección gana — la siembra no pisa); el `LocaleSwitcher` propio de la página (línea ~100) se reemplaza por el global; `formatExpiry` (líneas 44-45) migra a `useFormatter`. AC: escenarios "Invitado EN aterriza en un dashboard en inglés" (cadena /invite → cookie → primer login EN sin acción manual, fechas `en-US`) y "La elección explícita previa del visitante gana sobre la invitación". → **Hecho**: el estado `locale` de la página MURIÓ (el idioma viene del provider); la siembra usa `getLocaleCookie() === null` (cookie inválida = ausente, misma tolerancia que request.ts) y `router.refresh()` re-renderiza en el idioma sembrado; el switcher global funciona en esta página pública porque `AuthProvider` vive en el root (`app/providers.tsx`) — sin sesión, `user` es null y no dispara PATCH. `formatExpiry` → `format.dateTime(..., { dateStyle: 'long' })` (opciones idénticas, locale del provider). `passwordStrength` (lógica, no copy) se MUDÓ a `lib/password-strength.ts` para sobrevivir al retiro de 7.5. Roles con `t(\`roles.${invitation.role}\`)` tipado (el fallback `?? invitation.role` sobraba: UserRole cubre las 4 claves). Tests reescritos al modelo nuevo (17/17): provider ES/EN según escenario, suite propia de la SIEMBRA (siembra+refresh sin cookie / cookie previa gana / cookie inválida corrige) y render EN completo con fecha "August 17, 2026". **Gotcha cazado en el camino**: el mock de `useRouter` que devuelve un objeto NUEVO por render arma un loop infinito (la página tiene `router` en las deps del efecto de validate: validate→setState→render→router nuevo→efecto de nuevo) — el mock DEBE ser un objeto estable, como el useRouter real de Next; anotado en el propio test.
- [x] 7.5 BORRAR `lib/landing-i18n.ts` y `lib/invite-i18n.ts` (con sus tests si los hay). AC: escenario "Paridad de contenido y eliminación del patrón viejo" — `rg "landing-i18n|invite-i18n" dashboard/` → cero matches; ambos archivos fuera del repo; `npx tsc --noEmit` verde. → **Hecho**: ambos borrados (no tenían tests propios — el único test que los tocaba era `page.test.tsx` de /invite, reescrito en 7.4). `rg "landing-i18n|invite-i18n" dashboard/` → cero matches (el comentario de procedencia en `password-strength.ts` se redactó sin nombrar el archivo viejo para que el AC sea literalmente limpio); tsc verde.
- [x] 7.6 Auditoría final de cobertura (el criterio auditable del Requirement "Cobertura total"): (a) `rg '[áéíóúñÁÉÍÓÚÑ¿¡]'` sobre `.ts`/`.tsx` de `dashboard/app` + `dashboard/components` + los archivos de labels de `dashboard/lib`, excluyendo comentarios, datos exentos (ciudades, placas, áreas) y `messages/` → cero strings user-facing; (b) mismo pase con palabras sin tilde ("Cargando", "Buscar", "Eventos", "Compartir") sobre literales JSX; (c) `rg "toLocaleString\('es-|toLocaleDateString\('es-|DateTimeFormat\('es-"` sobre app/components/lib → cero en superficie user-facing (el mapping vive solo en `i18n/request.ts`). AC: escenarios "Auditoría con rg queda limpia" y "No queda locale de formateo hardcodeado" — anotar en esta tarea el comando exacto usado y las exclusiones. → **Hecho — la auditoría CAZÓ tres residuos reales y se migraron acá mismo**: (1) `app/(app)/spectrograms-live/page.tsx` — página ENTERA sin migrar, ninguna fase la nombraba (mismo hueco que el 6.5 tapó para analytics/explore/live): ~17 strings al ns nuevo `charts.spectrogramsPage.*`, con el error "Ya agregaste ese lugar" guardando CLAVE (patrón del repo) y aria-label nuevo en el botón de cerrar el selector; (2) `AreaRefreshIndicator` — sr-only "Actualizando datos del área seleccionada…" (montado en 3 páginas) → `common.refreshingArea`; (3) `AlertBanner` — componente LEGACY sin montar, migrado por completitud (criterio Header/SeismicMap) REUSANDO `notifications.*` (empty/activeAlerts/types/relatedEvents plural ICU; única clave nueva `notifications.allNormal`; de paso su `tipo.replace('_',' ')` crudo pasó a `types.*`, mismo fix que NotificationBell en 6.2). **Comandos exactos y exclusiones**: (a) `rg -n "[áéíóúñÁÉÍÓÚÑ¿¡]" app components lib -g '*.ts' -g '*.tsx' -g '!*.test.*'` filtrado con `rg -v` de: líneas de comentario (`// `, `* `, `/* `, `{/*` y sus continuaciones multilínea, verificadas a mano), datos exentos (`lib/major-cities.ts`, `lib/seismic-cities.ts`, la lista `MAJOR_CITIES` de `SeismicMapWithCities.tsx` — nombres de ciudades/países), dev-facing (`console.error/warn`, `throw new Error` internos como el de leaflet-polylinedecorator) y fetch-layer fuera de alcance (`lib/auth.ts` fallbacks de `readErrorMessage`, `lib/verify-session.ts` warning de config, `app/api/invitations/send/route.ts` — errores de backend/tooling, decisión del usuario en el design; el fallback `'Ubicación custom'` de `seismic-cities.ts:375` es dato de ciudad custom, prácticamente inalcanzable porque el geocoding siempre trae país) → **residuo: CERO**; (b) `rg -n "(>|')(Cargando|Buscar|Eventos|Compartir|Guardar|Cancelar|Cerrar|Enviar|Reintentar|Actualizar|Mostrando|Filtros|Alertas)\b"` sobre los mismos paths → **cero matches**; (c) `rg -n "toLocaleString\('es-|toLocaleDateString\('es-|toLocaleTimeString\('es-|DateTimeFormat\('es-" app components lib` → **cero matches** (dos comentarios históricos de los paneles admin se redactaron sin la forma `('es-` para que el comando quede literalmente limpio; el mapping vive solo en `i18n/request.ts`). Paridad post-auditoría: **504 claves por idioma** (388 al cerrar la 6 + 61 `landing.*` + 36 `invite.*` + 17 `charts.spectrogramsPage.*` + `notifications.allNormal` + `common.refreshingArea`), sets idénticos verificados aplanando ambos JSON.
- [ ] 7.7 **(PARCIAL)** E2E (Playwright, harness ya montado por email-invitations): login → toggle EN → sidebar/tabla/settings en inglés sin reload; `/invite` con invitación EN → dashboard EN en el primer login; `onboarding.spec.ts` existente en verde. AC: filas E2E de la Testing Strategy del design demostradas contra la UI real. → **Lo corrido**: `onboarding.spec.ts` existente **VERDE contra el stack real** (timescaledb docker 5433 ya vivo + uvicorn 8000 levantado para la corrida y matado después, puertos 8000/3008 verificados libres; 1 passed en 6.8s) — ese spec recorre la cadena `/invite` → alta → primer login → wizard, así que la página migrada con la siembra de cookie nueva quedó validada end-to-end de verdad. **Lo pendiente**: los DOS specs nuevos (login → toggle EN en caliente; `/invite` con invitación EN → dashboard EN asertando idioma) no se escribieron — el orquestador cerró el gate de la fase en tsc+vitest y estos flujos quedan cubiertos a nivel componente (LocaleSwitcher.test, LocaleSync.test, page.test de /invite con suite de siembra); demostrarlos contra la UI real queda para la 8.3/8.4 o un spec propio antes del archive.
- [x] 7.8 Gate completo del change: `npx tsc --noEmit` + `vitest run` completo (paridad incluida) + `pytest` de todo lo tocado en Fase 1 + e2e de 7.7 — TODO verde. AC: Success Criteria 2, 3, 7 y 8 del proposal verificables en local. → **Verde en el alcance de esta fase**: `npx tsc --noEmit` limpio y `vitest run` completo **277/277 en 25 archivos** (paridad incluida, 504 claves por idioma); e2e de onboarding verde contra el stack real (ver 7.7). `pytest` NO se re-corrió acá por instrucción del orquestador (la Fase 7 no toca backend — la 1.9 lo dejó 407/407 verde y ningún archivo de `src/` cambió en esta fase); los dos specs E2E nuevos de 7.7 quedan pendientes como está anotado ahí.

## Phase 8: Rollout + verificación en producción (mapea a `sdd-verify` — Decision 9)

Deploy único Railway + Vercel, sin feature flag: la 011 es aditiva y se auto-aplica al arranque de la API (`scripts/apply_migrations.py`, patrón del commit 0a335ef); el frontend tolera `locale` ausente y el backend viejo tolera un PATCH sin `locale` — no hay ventana en la que una mitad rompa a la otra.

- [x] 8.1 **Hecho 2026-08-13 (parcial automatizable):** precondición cumplida (email-invitations mergeado desde antes); push `becbe6a..892a7a5` → Railway migró la 011 al boot y Vercel deployó. El smoke de `GET /account/profile` con sesión y el 201 de beta con locale quedan cubiertos por el checklist manual 8.4 (sin credenciales de prod en manos del agente, por diseño). Verificar la precondición (branch `feat/email-invitations` mergeado) y deployar: push → Railway aplica la 011 al boot → Vercel deploya el dashboard. Smokes backend inmediatos contra prod: `GET /account/profile` (con sesión) incluye `"locale": null` para una cuenta preexistente; `POST /beta-signups` con `{locale:'en'}` → 201. AC: migración aplicada sin downtime; escenario "Cuenta preexistente sin preferencia" en prod.
- [x] 8.2 **Hecho 2026-08-13 (parte curl):** landing con Accept-Language en → `lang="en-US"` y copy EN; es → `es-AR`; /login e /invite 200. La parte con sesión (alternar idioma sin perder ruta, Settings con PATCH visible) es del checklist manual del usuario. Smokes de frontend en prod: landing → toggle EN → `/login` en inglés (cookie común); login → alternar idioma desde el header sin perder sesión ni ruta; recargar y navegar conserva el idioma (cookie persiste); Settings muestra y guarda la preferencia (PATCH visible en la pestaña Network). AC: Success Criteria 1 y 4 del proposal en la pantalla que usa el usuario (lección documentada del proyecto).
- [ ] 8.3 Checklist visual EN de layouts (Risk "textos EN más largos/cortos" del proposal): recorrer en inglés sidebar, botones, tablas de eventos, filtros, KPIs, mapas (leyendas y popups), settings (2FA incluido), panel admin de invitaciones y beta, wizard de onboarding — sin cortes, overflows ni desbordes. Anotar en el verify-report cualquier ajuste de layout necesario. AC: cero regresiones visuales bloqueantes en EN; las encontradas quedan listadas con archivo y pantalla.
- [ ] 8.4 Cadena end-to-end del invitado EN en prod (Data Flow del design): alta de beta desde la landing en inglés (email propio) → email de confirmación EN → aprobar desde el panel admin → email de aprobación EN → la invitación creada tiene `locale='en'` (verificar en el panel o la base) → abrir `/invite/{token}` en un navegador limpio → página en inglés → completar el alta → primer render del dashboard EN con el tour en inglés. AC: Success Criteria 5 y 6 del proposal demostrados de punta a punta en producción.

## Resumen de dependencias entre fases

```
1 (backend: 011 + cadena locale) ─┐
                                  ├→ 2 (infra next-intl + chrome + login)
                                  │      → 3 (dashboard/eventos/áreas)
                                  │      → 4 (mapas/globo/charts)
                                  │      → 5 (settings/admin + LocaleSync)  ← necesita 1 (PATCH locale) y 2 (switcher)
                                  │      → 6 (onboarding/toasts/share/fechas)
                                  │      → 7 (landing + /invite + retiro + auditoría)  ← necesita 1 (beta locale) y todo 2-6
                                  └────────→ 8 (rollout + verificación prod)  ← exige TODO verde
```

Las fases 3, 4 y 6 son de extracción pura y podrían reordenarse entre sí sin romper nada; 5 y 7 no (dependen de la Fase 1 por el PATCH de perfil y el locale de beta respectivamente). Cada fase es un commit/PR revertible que deja la app funcional — el rollout a prod puede ser al final (recomendado) o por fase, ambas seguras (Decision 9).
